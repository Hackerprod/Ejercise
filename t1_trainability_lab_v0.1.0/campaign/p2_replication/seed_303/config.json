{
  "alpha": null,
  "alpha_definition": "not used for pointer replacement",
  "batch_size": 128,
  "dataset_splits": "10000/2000/2000 fixed generated mappings",
  "dimension": 64,
  "early_stopping": false,
  "execution": "training executes exactly H required transitions per sample; evaluation uses min(H,R) transitions",
  "experiment_reason": "T1.2 P2 pointer-slot replacement diagnostic",
  "learning_rate": 0.0003,
  "max_steps": 5000,
  "memory_reader": "differentiable key-addressed source-key mask, one weighted destination read per round",
  "optimizer": "AdamW",
  "phase": "B+C",
  "residual": "pointer slot replacement: pointer_next=reader_output; no residual, alpha, or learned gate",
  "rounds": 4,
  "seed": 303,
  "slots": 1,
  "task": "pointer_chasing",
  "transition": "pointer_replacement",
  "weight_decay": 0.0001
}
