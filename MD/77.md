Sí: confirmo ADDR-ALG como cerrado, y el dato del 68% no debilita el diagnóstico; lo hace más fuerte. Pero detecto un confound nuevo que no quiero arrastrar a R1.2: posición/orden absoluto.

Cierre formal

T2-NOBYPASS-1-R1.1-ADDR-ALG: CLOSED / DIAGNOSTIC PASS_STRONG

Queda demostrado:

W
v
	​

(e
VALUE
	​

):32/32
FORCED
F
	​

=FORCED
A
	​

=139/139

Por tanto el value path está cerrado.

Y la ruptura es específicamente:

AVOID
atomic
	​

:rank=1, H=.336, mass=.889

versus

AVOID
joint
	​

:rank≈3.22, H=1.572, gap<0.

Además:

L>Fpara q
A
	​

 en 109/139.

Eso es una evidencia muy fuerte de pérdida de role binding bajo composición.

El 68% de los aciertos

Que 25/37 AVOID correctos todavía tengan:

s
A
	​

(L)>s
A
	​

(F)

significa precisamente que “decode correcto” todavía no equivale a “binding correcto”.

El mixture lexical consigue caer en el prototipo F pese a que el addressing bruto favorece L.

Así que en R1.2 voy a añadir un control explícito de separación entre argumentos; no volveremos a aceptar NB2=139/139 como prueba de role binding si el query sigue mirando preferentemente el otro VALUE.

Pero hay un confound posicional importante

En atomic:

AVOID VALUE_F

el VALUE está en posición 1.

En el joint normal:

AT_LEAST VALUE_L AND AVOID VALUE_F

VALUE_L sigue estando en posición 1;

VALUE_F pasa a posición 4.

Y el key path actual utiliza estados contextualizados que incluyen el embedding posicional entrenable.

Eso encaja demasiado bien con el resultado:

FLOOR mantiene exactamente la posición conocida → 139/139.

AVOID cambia de posición → colapsa.

q_A acaba prefiriendo con frecuencia VALUE_L, que ocupa precisamente la posición conocida.

Por tanto todavía no podemos separar:

competencia entre dos VALUEs

de:

overfit a posici
o
ˊ
n absoluta.

Había dicho que después de esta rama no haría más diagnóstico; retiro esa parte concreta porque estos resultados revelan un confound nuevo que antes no habíamos aislado.

T2-NOBYPASS-1-R1.1-POS-ALG: AUTHORIZED

Cero entrenamiento. Seed6703 congelado.

Sólo tres controles.

P1 — atomic position shift

Evaluar los mismos:

AVOID VALUE_n

pero en el encoder asignar artificialmente sus posiciones como 3,4 en vez de 0,1, sin cambiar tokens ni local context.

También hacer el control FLOOR.

Si:

AVOID
0,1
	​

=32/32

pero:

AVOID
3,4
	​

≪32/32,

tenemos evidencia causal directa de dependencia de posición absoluta.

P2 — joint second-clause position remap

Sobre el joint normal mantener exactamente los tokens:

AT_LEAST L AND AVOID F

pero para el key path únicamente, darle a AVOID,F los position embeddings equivalentes a posiciones 0,1.

El value path sigue lexical y sin posición.

Medir:

exact AVOID;

F>L;

entropy;

target rank/gap.

Si AVOID salta de 37/139 hacia ~139 y F_beats_L sube de 30, el culpable está localizado.

P3 — reverse-order joint

Evaluar:

AVOID F AND AT_LEAST L

mismos 139 pares.

Predicción bajo hipótesis posicional:

AVOID debería mejorar radicalmente al volver a ocupar el primer clause/posición temprana;

FLOOR debería degradarse al convertirse en segundo clause.

Si el patrón se intercambia con el orden, la prueba sería extremadamente fuerte.

Ramas predeclaradas para R1.2
Rama POS — el fallo sigue la posición

Si P1/P2/P3 confirman esto:

R1.2-POSFREE-KV

Fresh seed 6704.

Mantener intacto el value path validado:

v
t
	​

=W
v
	​

(e
t
	​

).

Cambiar sólo las keys para eliminar posición absoluta:

k
t
	​

=LN(SiLU(W
k
	​

[e
t−1
	​

,e
t
	​

,e
t+1
	​

]))

sin position_embedding[t] en el role-addressing path.

Entonces:

a
r,t
	​

=softmax(q
r
⊤
	​

k
t
	​

/4)
r
r
	​

=
t
∑
	​

a
r,t
	​

W
v
	​

(e
t
	​

).

La semántica de la asociación:

AVOID → VALUE siguiente

puede transferirse de posición 0/1 a posición 3/4 porque la key depende de contexto relativo local, no de coordenada absoluta.

El mode head puede conservar posición si la necesita. Sólo la ruta de role addressing se hace position-free.

No añadiría joint examples.

Rama COMP — posición no explica el fallo

Si atomic AVOID funciona igualmente desplazado y reverse-order no rescata AVOID:

entonces ya sí tenemos un problema genuino de competencia entre roles.

En ese caso R1.2 usaría una key todavía más específica para la relación operador→argumento:

k
t
	​

=g(e
t−1
	​

,e
t
	​

)

para todos los tokens, sin máscaras.

Es decir, el candidato VALUE lleva explícitamente en su key el token inmediatamente anterior:

[AT_LEAST,VALUE
L
	​

]

versus:

[AVOID,VALUE
F
	​

].

Los queries de rol aprenden sobre esa relación local, no sobre el hidden global de posición.

Sigue sin haber:

label de cláusula;

máscara VALUE;

attention target;

joint training.

Gate nuevo para R1.2: RB

Esto sí lo añadiría debido al hallazgo 25/37.

Antes de declarar argument-binding resuelto, exigir:

s
F
	​

(L)>s
F
	​

(F)

y:

s
A
	​

(F)>s
A
	​

(L)

en los 139/139 joint.

Llamémoslo:

NB2-RB

No exijo one-hot ni entropy baja. Sólo que cada role query prefiera su propio VALUE frente al competidor.

Después:

NB2-DEC

decode(r
F
	​

)=L,decode(r
A
	​

)=F

139/139.

Así no repetimos:

decode correcto por mezcla ≠ binding correcto.

Lo que NO autorizo

más steps sobre seed6703;

temperatura;

entropy loss;

joint examples;

cambiar W_v;

tocar el codebook;

NB3/NB4.

El value path ya está demostrado:

VALUE representation solved
	​


No debemos tocarlo.

Estado final

ADDR-ALG: CLOSED / PASS_STRONG diagnóstico.

Value path: CLOSED / SOLVED.

Residual: role-addressing under joint composition.

Pero antes de entrenar R1.2:

R1.1-POS-ALG: AUTHORIZED, frozen.

La razón es muy concreta: FLOOR conserva la posición atómica y pasa 139/139; AVOID cambia de posición y se rompe. Si el patrón se invierte al invertir el orden, tendremos una explicación mucho más precisa que “competition”: absolute-position overfitting in the addressing path. Y R1.2 puede eliminar exactamente esa dependencia sin añadir supervisión ni capacidad innecesaria.