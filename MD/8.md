**Sí. Autorizo repetir la receta de annealing en las semillas 101, 303, 404 y 505. El piloto `C1-lossnorm-anneal-seed202` queda `CLOSED / PASS_STRONG`; el cierre multisemilla permanece pendiente.**

Revisé `364d99c`, la función del scheduler, su aplicación antes de cada `optimizer.step()` y el JSON final. No he reejecutado los checkpoints ni reconstruido independientemente toda la curva temporal; la descripción de esa trayectoria procede de tu reporte.

## 1. El piloto cumple el criterio en el checkpoint final

Los valores publicados son:

|  H | Límite de conservación | Error final con annealing | Margen respecto al límite |
| -: | ---------------------: | ------------------------: | ------------------------: |
|  2 |            0.000262616 |           **0.000020134** |          13.0x por debajo |
|  4 |            0.000434738 |           **0.000029551** |          14.7x por debajo |
|  6 |            0.001448047 |           **0.000110474** |          13.1x por debajo |

El final también conserva las matrices discretas y registra, para la tarea transformada, error máximo `0.0000170448`, top-1 `1.0`, error de payload `0.0` y pase de los criterios de salida y de delta. El mejor checkpoint está en step11000, pero **el final del step12000 pasa por sí mismo**.

Por tanto, no estamos aprobando una compensación entre tareas ni seleccionando retrospectivamente una ventana favorable. **La intervención mejora la conservación de RAW sin sacrificar la tarea nueva.**

## 2. Qué permite concluir sobre la causa

El resultado respalda fuertemente la hipótesis de que la receta anterior necesitaba una fase de ajuste más fino. El scheduler implementado mantiene `3e-4` hasta step500 y después decae por coseno hasta `3e-6`; esa tasa se aplica a todos los grupos del optimizador antes del update. No introduce congelación tardía ni cambios de arquitectura.

Matizaría únicamente la expresión "confirma la hipótesis exacta":

> La intervención demuestra que este calendario de learning rate resuelve el fallo de conservación en el piloto. La estabilización que reportas es coherente con updates tardíos demasiado grandes, pero no demuestra que hayan desaparecido todos los conflictos de gradientes ni aísla cada mecanismo interno del optimizador.

No hace falta investigar más esa distinción antes de replicar. El piloto ya respondió la pregunta práctica que planteamos.

**Tampoco necesitamos que las otras semillas mejoren tanto como 202.** Solo deben cumplir los mismos gates fijados antes de esta receta.

## 3. Replicación autorizada: una receta única para las cinco semillas

La serie será:

```text
C1-lossnorm-anneal:
101, 202, 303, 404, 505
```

La 202 ya está completada. Ejecutar las otras cuatro desde el **mismo estado inicial compuesto original de C1**, con optimizador nuevo en cada corrida. No partir del checkpoint annealed de 202 ni continuar los finales de las campañas con learning rate constante.

Mantener:

| Elemento                 | Configuración congelada                                            |
| ------------------------ | ------------------------------------------------------------------ |
| Inicialización           | Misma base U0-A + mismo corrector C0 `network_trainable`           |
| Datos                    | Mismos splits y hashes                                             |
| Variación entre réplicas | Semilla de entrenamiento y orden de minibatches                    |
| Presupuesto              | 12,000 supersteps                                                  |
| Learning rate            | `3e-4` hasta 500; coseno hasta `3e-6` en 12000                     |
| Pérdida                  | Normalización por coordenada ya corregida                          |
| Replay ALU               | Mismo calendario 5:1                                               |
| Lecturas                 | BLEND histórico; SELECT con STE donde está declarado               |
| Resto                    | Mismos parámetros de AdamW, clipping, arquitectura y congelaciones |
| Resultado principal      | Checkpoint final del step12000                                     |

Los checkpoints de origen y las semillas de generación de los splits permanecen fijos en el runner; las réplicas evalúan robustez de **esta fase de fine-tuning conjunto**, no cinco entrenamientos independientes desde cero.

### Precaución concreta: el valor por defecto sigue siendo `constant`

El parser conserva:

```python
--lr-schedule
choices=("constant", "cosine")
default="constant"
```

Por tanto, **las cuatro invocaciones deben llevar explícitamente `--lr-schedule cosine`**. Cambiar el nombre de la carpeta a "anneal" no cambia el entrenamiento.

Por ejemplo, desde la raíz del laboratorio:

```powershell
python scripts/train_u0c_c1_joint.py `
  --seed 101 `
  --steps 12000 `
  --lr-schedule cosine `
  --output-dir campaign/u0c_c1_lossnorm_anneal_seed101_12000
```

Aplicar la misma invocación a 303, 404 y 505 con sus directorios propios. El horizonte debe seguir siendo 12000: la función calcula el descenso utilizando `total_steps`, de modo que modificar el presupuesto también modificaría la curva del scheduler.

## 4. Los criterios no cambian después del éxito del piloto

Para cada semilla, conservar el preflight de igualdad del `state_dict` inicial y reproducción de la evaluación de step0.

En el final, RAW debe cumplir por separado:

$$
e_{\mathrm{RAW,final}}(H)
\leq
e_{\mathrm{RAW,step0}}(H)+10^{-5},
\qquad H\in\{2,4,6\}.
$$

La tarea transformada y las tareas discretas mantienen sus límites ya establecidos. **No se exige alcanzar el nuevo récord de precisión de seed202**, ni que el mejor checkpoint coincida con el final, ni que cada punto de validación mejore monótonamente.

El checkpoint de mejor validación se sigue reportando, pero no reemplaza al final. Tampoco se compensará una regresión H4 con una mejora H6.

Si una semilla falla, se registra y se conserva su corrida. No se modifica su learning rate particular ni se prolonga solamente esa semilla hasta hacerla pasar.

**No hace falta otra auditoría previa, otro barrido de hiperparámetros ni introducir una pérdida adicional.** La siguiente acción es replicar, no seguir optimizando el piloto.

## 5. Qué se podrá cerrar después

Si las cuatro réplicas cumplen, el resultado será:

> **La receta C1 con normalización por coordenada y annealing conserva conjuntamente las siete tareas en cinco semillas de optimización, desde una inicialización preentrenada común y bajo el presupuesto declarado.**

Eso es más preciso que afirmar que SELECT y BLEND nunca pueden interferir o que la arquitectura ya generaliza a cualquier tarea.

La campaña anterior con learning rate constante debe permanecer archivada como `2/5` en conservación final. No se sustituye ni se mezcla con la nueva.

```text
C1-lossnorm, learning rate constante:
conservación final 2/5; campaña archivada.

C1-lossnorm-anneal, seed202:
CLOSED / PASS_STRONG.

C1-lossnorm-anneal, seeds101/303/404/505:
AUTORIZADAS.

C1-lossnorm-anneal multisemilla:
PENDIENTE.

Siguiente etapa tras la replicación:
programas mixtos con routing oráculo.

Router aprendido:
después de validar composición.
```

**El piloto ya justifica repetir la receta sin más cambios. Ahora corresponde comprobar que el annealing estabiliza también las otras trayectorias, no convertir el éxito de la peor semilla anterior en una garantía anticipada de 5/5.**
