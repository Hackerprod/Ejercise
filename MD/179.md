Confirmo `ER64-SCALING-GATE Stage 2 = CLOSED / PASS`. No necesito que repitas ni recomputes esa corrida. Los ratios están bien orientados, MEMORY_SAFETY bloquea resultados parciales y el chequeo de anomalía RSS sigue integrado dentro de INFERENCE_COST; con los valores publicados tampoco dispara.

Pero todavía no autorizo ejecutar los 8,000 updates de Stage 3. No por ninguna duda sobre el cost-gate, sino porque después de la auditoría Fable dejamos predeclarado este orden:

ER64 cost-gate
→ diagnósticos baratos sin entrenamiento
→ revisar resultados
→ ER64 Quality-A

Saltarlo ahora sería cambiar el árbol después de haberlo fijado.

### Cierre de Stage 2

La evidencia publicada sustenta:

TRAINING_COST
    R_train = 0.9783955917420218
    PASS <= 1.25

INFERENCE_COST
    K1 token = 1.02217
    K1 window = 0.93754
    K4 token = 0.98058
    K4 window = 1.02547
    PASS <= 1.25

MEMORY_SAFETY
    24/24
    PASS

MEMORY_RUNTIME_ANOMALY
    no

Además, del reporte:

* K1 RSS: 546,840,576 - 532,750,336 = +14,090,240 B
* K4 RSS: 544,563,200 - 533,536,768 = +11,026,432 B

muy por debajo de los 128 MiB que habíamos usado como límite técnico. El código confirma que esa anomalía se comprueba dentro de classify_inference_cost().

El primer intento cortado por timeout del harness queda correctamente fuera de la evidencia canónica.

## Antes de Quality-A: tres diagnósticos autorizados

Los haría ahora porque no entrenan nada y pueden cambiar de forma importante cómo interpretemos ER64.

### 1. OMEGA-RANK-SVD-DIAGNOSTIC

Usar los cuatro checkpoints R1@2000 ya congelados:

seed13 K1
seed13 K4
seed14 K1
seed14 K4

Para cada matriz ligada entrenada:

$$
E\in\mathbb{R}^{50257\times128}
$$

obtener la SVD y construir aproximaciones truncadas de rango 32 y 64:

$$
E_r=P_r\Sigma_rQ_r^\top.
$$

Puede almacenarse como:

$$
C_r=P_r\Sigma_r^{1/2},
\qquad
U_r=\Sigma_r^{1/2}Q_r^\top.
$$

Sin entrenamiento, sustituir únicamente el embedding/readout ligado y evaluar exactamente el mismo validation primario.

Reportar:

$$
\delta_{\text{SVD}r,K,i}
=
NLL_{\text{SVD}r,K,i}
-
NLL_{\text{R1},K,i}.
$$

No hay PASS/FAIL formal; es diagnóstico.

Su poder interpretativo es fuerte:

* SVD64 ≈ R1 pero fresh ER64 luego falla → sospecha de dinámica de optimización.
* SVD32 malo y SVD64 bueno → evidencia de umbral de capacidad/rango.
* SVD64 ya malo → la solución densa encontrada necesita más rango para ser aproximada directamente.

No factorizar ni continuar esos checkpoints entrenando: ésa sería otra intervención.

### 2. OMEGA-EXPANDED-FROZEN-VALIDATION

Congelar antes de ER64 Quality-A un validation secundario más amplio.

No tocar test.

Regla que fijaría:

split = WikiText-2 validation
reconstrucción = misma regla de documentos
dedupe = misma regla
elegibilidad = >=513 GPT-2 tokens
selección = TODOS los elegibles, en orden original
por documento = primeros 513 tokens

Evaluar, sin entrenamiento nuevo:

R1 K1/K4 seed13/14 @2000
ER32 K1/K4 seed13/14 @2000

El validation histórico de 8 documentos sigue siendo primary y no cambia ninguna clasificación anterior.

Este nuevo conjunto queda como:

SECONDARY_FROZEN_VALIDATION

y después se podrá aplicar a ER64 sin haberlo escogido tras ver ER64.

### 3. OMEGA-READOUT-CACHE-SURVIVAL-AUDIT

Sin entrenamiento. Comparar:

R1/F dense
ER32
ER64

con el mismo recurrent core.

Aquí corregiría el nombre de la métrica anotada en IDEAS_FUTURAS.md.

Si \(C\) es coste/ciclos/latencia:

$$
R_{\text{eviction}}
=
\frac{C_{\text{posthead}}-C_{\text{warm}}}
     {C_{\text{cold}}-C_{\text{warm}}}.
$$

Interpretación:

0 → comportamiento warm: el core sobrevivió al readout
1 → comportamiento cold: el readout lo desalojó completamente

Y si queremos una métrica denominada survival:

$$
S_{\text{survival}}=1-R_{\text{eviction}}.
$$

No fijaría todavía un threshold de PASS sin antes diseñar bien cómo medir cold/warm/posthead y controlar ruido/afinidad. Esta primera unidad puede ser unmeasurement audit.

## Sobre el cambio en Step2 histórico

Encontré una cuestión de provenance que no invalida Stage 2, pero debe quedar documentada.

d75cb63 modificó run_step2_benchmark.py y run_step3_cache_extension.py de la unidad CPU-FASTPATH ya cerrada para eliminar la configuración de threads a nivel de módulo.

Eso está bien para el código actual, pero:

> los resultados históricos de Step2/Step3 pertenecen a sus hashes y commits originales, no a estos nuevos bytes.

El reporte ER64 hace lo correcto al registrar el hash actual de Step2 como dependencia. No hay que repetir Step2 ni editar sus resultados antiguos. Sólo añadiría una nota de trazabilidad para impedir que en el futuro alguien diga que el viejo run se produjo con el código refactorizado.

## Qué sí autorizo ya respecto a Stage 3

Puedes implementar y probar el runner de:

OMEGA-CORE-LM-0-ER64-QUALITY-SCOPING-A

reutilizando literalmente el protocolo ER32 Quality-A:

seed13 ER64-K1 0→2000
seed13 ER64-K4 0→2000
seed14 ER64-K1 0→2000
seed14 ER64-K4 0→2000

CPU FP32 eager
B=8
same corpus traversal
same teacher
same loss
same optimizer
same 8-doc primary validation
boundaries 0/500/1000/1500/2000
test prohibited

Y las mismas reglas:

$$
\delta_{K,i}=NLL_{ER64,K,i}-NLL_{R1,K,i}
$$

con ceiling:

$$
\delta_{K,i}(2000)\le0.10.
$$

Pero el comando que ejecuta entrenamiento real debe permanecer bloqueado hasta que me reportes los tres diagnósticos anteriores.

No porque necesitemos que “pasen”; pueden producir cualquier resultado. Necesitamos verlos porque precisamente los predeclaramos como información barata capaz de distinguir capacidad, optimización y beneficio físico real antes de quemar otros 8,000 updates.

ER64-SCALING-GATE Stage 1
    CLOSED / PASS

ER64-SCALING-GATE Stage 2
    CLOSED / PASS

SVD32/SVD64 DIAGNOSTIC
    GO

EXPANDED FROZEN VALIDATION
    GO

CACHE-SURVIVAL AUDIT
    GO

ER64 Quality-A implementation/tests
    GO

ER64 Quality-A real 8000 updates
    HOLD until the three diagnostics are reviewed

Así que no revisaría más el cost-gate: está cerrado. Lo siguiente no es otro benchmark del mismo tipo, sino extraer toda la información barata disponible de los checkpoints y del hardware antes del próximo bloque grande de entrenamiento.