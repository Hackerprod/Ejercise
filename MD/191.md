Sí. Encontraste otro error de orientación, y tu hipótesis es exactamente la corrección correcta.

En MD/190.md quedó escrito:

R
t
	​

=
T
TBPTT16
	​

T
FULL256
	​

	​


pero los thresholds que definí corresponden matemáticamente a la razón inversa. Tal como está, un TBPTT realmente más rápido sería penalizado. Eso debe corregirse antes de cualquier ejecución real.

La definición canónica queda:

R
t
	​

=
T
FULL256
	​

T
TBPTT16
	​

	​

	​


Interpretación:

R_t = 1.00   mismo tiempo
R_t = 0.90   TBPTT tarda 10% menos
R_t = 0.80   TBPTT tarda 20% menos
R_t = 1.05   TBPTT tarda 5% más

Por tanto, los thresholds permanecen:

TECH_POSITIVE
    R_t <= 0.90
    >=10% reducción de tiempo por update

TECH_NEUTRAL
    0.90 < R_t <= 1.05

TECH_NEGATIVE
    R_t > 1.05

Una precisión de lenguaje: conviene escribir “10% reducción de wall-clock/update” en lugar de “10% speedup”, porque R_t=0.90 equivale a un speedup multiplicativo de 1/0.90=1.111×. Así no mezclamos “% menos tiempo” con “% más throughput”.

Segunda inconsistencia: el código también debe corregirse

Sí: en DIRECTIONAL-PROMISING-TRADEOFF mi intención era deliberadamente exigir una ventaja técnica más fuerte que el TECH_POSITIVE genérico.

La razón era:

si δ<=0.05, aceptamos una optimización con ≥10% de ahorro;

si aceptamos una deuda mayor de calidad, 0.05<δ<=0.10, debe existir una recompensa técnica considerable para justificar investigar 8/16/32.

Por tanto mantengo el criterio de 20% de reducción de tiempo.

Con la definición corregida:

R
t
	​

≤0.80
	​


No basta con TECH_POSITIVE.

El código actual que trata cualquier R_t<=0.90 como suficiente para esa rama está desviándose del contrato textual.

La matriz definitiva queda:

QUALITY_SAFE:
    δ16 <= +0.05

QUALITY_TRADEOFF:
    +0.05 < δ16 <= +0.10

QUALITY_BAD:
    δ16 > +0.10

y:

TECH_POSITIVE:
    R_t <= 0.90

TECH_NEUTRAL:
    0.90 < R_t <= 1.05

TECH_NEGATIVE:
    R_t > 1.05

Decisión combinada:

DIRECTIONAL-GO
    QUALITY_SAFE
    AND
    (
        R_t <= 0.90
        OR
        peak_RSS_reduction >= 20%
    )
DIRECTIONAL-PROMISING-TRADEOFF
    QUALITY_TRADEOFF
    AND
    R_t <= 0.80
DIRECTIONAL-NEUTRAL
    QUALITY_SAFE
    AND
    0.90 < R_t <= 1.05
DIRECTIONAL-STOP
    QUALITY_BAD

    OR

    (
        R_t > 1.05
        AND
        peak_RSS_reduction < 20%
    )
Un caso no cubierto explícitamente

Hay una combinación que conviene definir ahora y no después:

QUALITY_TRADEOFF
AND
0.80 < R_t <= 0.90

Es decir, 5–10 puntos de deuda NLL pero sólo 10–20% de ahorro.

No debería llamarse PROMISING-TRADEOFF, porque no alcanza la recompensa técnica de 20%.

La clasificaría:

DIRECTIONAL-WEAK-TRADEOFF

y no autorizaría el sweep completo sólo con eso.

Así evitamos tener que improvisar esa rama después de ver datos.

Correctness gate

Los 7/7 tests que ya pasaron pueden conservarse. Si no dependen de la orientación de R_t, no hay motivo para repetir su lógica.

Pero antes del cost preflight real exigiría dos canaries muy simples sobre la clasificación:

FULL = 10.0 s
TBPTT = 8.0 s
R_t = 0.80
→ TECH_POSITIVE
→ satisface requisito fuerte del PROMISING-TRADEOFF

y:

FULL = 10.0 s
TBPTT = 11.0 s
R_t = 1.10
→ TECH_NEGATIVE

Más uno para el borde:

FULL = 10.0
TBPTT = 9.0
R_t = 0.90
→ TECH_POSITIVE
pero NO cumple el requisito <=0.80 de PROMISING-TRADEOFF

Eso habría atrapado ambas inconsistencias inmediatamente.

Estado
OMEGA-TBPTT-DIRECTIONAL-PROBE

Correctness implementation:
    7/7 PASS
    preserved

R_t original formula:
    INVALID / ORIENTATION INVERTED

Canonical R_t:
    T_TBPTT16 / T_FULL256

PROMISING-TRADEOFF:
    requires R_t <= 0.80 specifically
    not generic TECH_POSITIVE

Real cost preflight:
    HOLD until formula/classifier/tests corrected

Real 0→2000 training:
    HOLD until technical preflight classification

No hay contaminación científica porque lo detectaste antes de cualquier timing o quality result real. Es exactamente el momento correcto para corregirlo.