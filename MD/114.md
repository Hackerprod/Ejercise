Sí. Confirmo formalmente:

$$
\boxed{\text{T4-NOBYPASS-2-RCSEP3-BG: DEVELOPMENT CLOSED / PASS\_STRONG}}
$$

El commit `ed1bdef2bd950956019cb3017c925d7e3c6073ea` confirma exactamente la cadena que necesitábamos: static audit `13/13`, fresh-init sobre `7101–7105`, `D1/D2/D3=true` en 5/5, gradiente de `LINK` ahora estrictamente positivo, certificados BG positivos y `29760/29760` triple-joint.

Y sí:

$$
\boxed{\text{PREPARAR T4 FRESH 7201–7205: AUTHORIZED}}
$$

Todavía **no autorizo training 720x**. Primero freeze + manifests + checker, exactamente como hicimos antes de las baterías fresh anteriores.

## Freeze definitivo de T4

Desde este momento `7101–7105` son permanentemente:

$$
\boxed{\text{development/diagnostic}}
$$

No se cambia nada más mirando sus resultados.

Congelar:

$$
k_t=LN(SiLU(W_k[e_{t-1},e_t]))
$$

tres queries:

$$
q_F,q_A,q_M
$$

pure HARDPTR:

$$
j_r=\arg\max_{t<length}s_{r,t}
$$

shared:

$$
r_r=W_v(e_{j_r})
$$

y:

$$
L=
L_{behavior}^{F/A}
+
L_{ref}^{F/A/M}
+
L_{sep}^{3+BG}.
$$

Con:

$$
L_{sep}^{3+BG}
=
\frac19
\left[
\sum_r\sum_{r'\ne r}L_{r\leftarrow r'}
+
\sum_rL_{r\leftarrow BG}
\right].
$$

Exactamente:

* 6 términos role→role;
* 3 términos role→BG;
* 38 contextos BG;
* `32×38` en cada término BG;
* coeficiente `1.0`;
* sin mask;
* sin gate;
* sin Stage B;
* sin soft mixture.

Y congelar también:

* 5000 updates;
* AdamW;
* `wd=0`;
* LR `1e-3→1e-5`;
* 96 atomic examples;
* `multi_clause_train=0`;
* local BG negatives activos;
* ningún joint example en training;
* mismo decoder/codebook;
* mismo evaluator RAW/CANON;
* misma tie policy.

## Fresh manifests 7201–7205

Usaría cinco dominios completamente nuevos:

$$
7201,7202,7203,7204,7205.
$$

Cada uno:

* nueva permutación `ARG_i→VALUE_{\pi(i)}`;
* nuevos physical token IDs;
* fresh-init futura;
* `OP_X/OP_Y/OP_Z`;
* roles `{FLOOR,AVOID,MATCH}`;
* 96 atomic train rows;
* cero multi-clause.

### Role assignment

Precomprometer con un RNG independiente:

$$
batch\_seed=7200.
$$

Tomar las 6 permutaciones de `{F,A,M}` sobre `{OP_X,OP_Y,OP_Z}` y:

```python
random.Random(7200).sample(all_six_permutations, 5)
```

Asignarlas, en orden, a `7201..7205`.

Guardar explícitamente cuál sexta permutación quedó fuera.

No modificar la selección después de verla.

## E(L,F)

Mantener **exactamente el algoritmo development ya congelado**:

$$
candidate=(seed+L+F+1)\bmod32
$$

incrementando hasta que:

$$
E\ne L,\qquad E\ne F.
$$

No diseñar un nuevo selector para fresh.

## Test sellado

Por seed:

$$
992\text{ pares }(L,F)
$$

por:

$$
6\text{ órdenes}
$$

=

$$
5952
$$

casos.

Total fresh:

$$
\boxed{29760}
$$

triple-clause cases.

El checker independiente debe verificar antes de training:

* exactamente 5952 casos/seed;
* seis órdenes exactos por `(L,F,E)`;
* `E≠L,F`;
* mappings correctos;
* cinco role assignments válidos;
* physical IDs únicos y disjuntos de 680x/690x/700x/710x;
* train `32F+32A+32M`;
* `multi_clause_train=0`;
* cero contaminación con test.

## Freeze artifact

Quiero además un artifact pequeño de recipe freeze que incluya hashes de:

* modelo T4;
* trainer;
* `RCSEP3-BG`;
* HARDPTR evaluator;
* manifest generator;
* checker;
* decoder/runtime usado.

Y flags explícitos:

```text
queries=3
shared_w_v=true
rcsep_role_terms=6
rcsep_background_terms=3
background_contexts=38
multi_clause_train=0
local_background_negative_contexts=true
hardptr_mask=false
stage_b=false
gate=false
soft_mixture=false
```

Una vez escrito ese freeze, sus hashes no cambian mirando `720x`.

# Fresh acceptance contract

No se ejecuta todavía, pero lo dejo congelado **antes de ver resultados**.

Atomic por seed:

$$
F=A=M=32/32.
$$

Certificados:

$$
G_{F,A},G_{F,M},
G_{A,F},G_{A,M},
G_{M,F},G_{M,A}>0
$$

y:

$$
G_{F,BG},G_{A,BG},G_{M,BG}>0.
$$

También mantener separado:

$$
G_{r,ARG\rightarrow LINK}>0
$$

como sanity causal.

End-to-end sobre los `29760`:

* pointer F/A/M = exacto;
* decode F/A/M = exacto;
* RAW F/A/M = exacto;
* CANON F/A/M = exacto;
* RAW≡CANON F/A/M = exacto;
* nueve márgenes estrictamente positivos;
* `six_order_outcome_equality=true`;
* `mismatch_count=0`.

Ejecutaremos **los cinco seeds completos aunque uno falle**.

### Veredicto futuro

Sólo:

$$
\boxed{\text{5/5 exactos}\Rightarrow T4\ FRESH/PASS\_STRONG}
$$

Cualquier fallo:

$$
\boxed{\text{T4 FRESH: VALID FAIL}}
$$

sin tuning usando `720x`.

## Claim si fresh pasa

La formulación sería:

> **RCSEP3-BG scales systematic role binding to three simultaneously active operator→argument relations across independent fresh lexical domains, with zero multi-clause training and exact composition across all six clause orders.**

Con una precisión importante: el rechazo de background **no es generalización espontánea**; fue entrenado localmente mediante BG negatives. Lo genuinamente unseen será la **composición simultánea de los tres bindings**.

### Dirección concreta

$$
\boxed{\text{T4 DEVELOPMENT CLOSURE: CONFIRMED}}
$$

$$
\boxed{\text{RCSEP3-BG: FROZEN}}
$$

$$
\boxed{\text{7201–7205 MANIFEST PREPARATION: AUTHORIZED}}
$$

Generá/sellá los cinco manifests fresh + recipe freeze + checker independiente. **Cero training hasta reportarme esos hashes y el resultado del checker.**
