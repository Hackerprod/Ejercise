Confirmo T7 STAGE-A DEVELOPMENT: PASS y autorizo únicamente la etapa B: calibración NULL base sobre los cinco cores 7701–7705 ya congelados. No hace falta otro diagnóstico de A.

Revisé el commit 13a123f03225… y el consolidado: registran A1/A2/A3 completos, la verificación exhaustiva de los 640 targets, los checkpoints finales y B/C todavía sin iniciar. Mi comprobación es del contenido versionado; no he reejecutado tus entrenamientos ni recalculado sus hashes.

T7 STAGE-A DEVELOPMENT: CLOSED / PASS
	​

T7 STAGE-B DEVELOPMENT — BASE NULL CALIBRATION: AUTHORIZED
	​


C y la evaluación multi-cláusula siguen en HOLD.

1. Objetivo de B: fijar NULL antes de que exista NOOP en el modelo

La etapa B debe establecer, para cada query, un umbral entre los argumentos de su propio rol y todos los candidatos negativos del dominio base.

Los certificados positivos de A indican que ese intervalo existe en la geometría medida. Ahora corresponde calcularlo mediante el procedimiento congelado, comprobar su representación numérica y guardar los umbrales asociados a cada core.

No se entrena nada en B. No se necesita backward, optimizador ni una nueva pérdida.

Cargar exclusivamente los cinco checkpoints finales registrados en:

campaign/t7_noop_none_lexical_stage_a_development/
    training/seed_<seed>/final.pt

Esos son los checkpoints identificados en el consolidado de A.

Mantener sus pesos congelados y reutilizar la misma vista base y el mismo mapping efectivo del vocabulario. No reordenar embeddings ni tratar los IDs físicos como índices internos.

2. Catálogos permitidos y fórmula exacta

Para cada seed, reconstruir mediante el forward del core congelado:

Las 128 relaciones atómicas: cuatro roles por 32 argumentos.

Los 40 contextos BG base: cuatro START→OP, 32 ARG→LINK y cuatro LINK→OP.

Los scores deben proceder del modelo cargado, no de números redondeados copiados del resumen de A.

Para cada rol r:

P
r
min
	​

=
i=0,…,31
min
	​

s
r
	​

(OP
r
	​

,ARG
i
	​

),
N
r
max
	​

=max
	​

r
′

=r
j=0,…,31
	​

max
	​

s
r
	​

(OP
r
′
	​

,ARG
j
	​

),
z∈S
4
	​

max
	​

s
r
	​

(z)
	​

.

El máximo negativo considera 96 argumentos de otros roles y los 40 contextos BG. No incluye candidatos NOOP.

Aplicar el calibrador ya congelado:

b
r
	​

=N
r
max
	​

+
2
P
r
min
	​

−N
r
max
	​

	​

.
	​


Conservar el orden aritmético y la conversión al dtype de inferencia de esa implementación. No sustituirla por otra expresión algebraicamente equivalente ni introducir tolerancias nuevas.

Corrección semántica de las posiciones

Mantener el mismo criterio ya auditado:

Contexto	Posición cuyo score se utiliza
OP_r→ARG_i	1: el argumento
START→OP_r	0: el operador inicial
ARG_i→LINK	1: LINK
LINK→OP_r	1: el operador

START sigue siendo el prefijo de embedding cero del forward; no se crea un token BOS.

3. Gates de calibración

Para los 20 intervalos —cinco seeds por cuatro roles—, exigir valores finitos y:

P
r
min
	​

>N
r
max
	​

.

Después de almacenar el umbral en el dtype real de inferencia, comprobar:

P
r
min
	​

−b
r
stored
	​

>0,b
r
stored
	​

−N
r
max
	​

>0.
	​


Registrar por rol:

Campo	Contenido
P_min	Score positivo mínimo y argumento que lo produce
N_max	Score negativo máximo, rol o contexto que lo produce
interval_width	P
r
min
	​

−N
r
max
	​


b_midpoint	Resultado de la fórmula antes del almacenamiento
b_stored	Valor efectivamente utilizado en inferencia
storage_dtype	Dtype del umbral
Holguras	P
r
min
	​

−b
r
stored
	​

 y b
r
stored
	​

−N
r
max
	​


El gate es estrictamente >0; no exigir una holgura heredada de T6 ni de otra seed.

Guardar un único vector de cuatro constantes por modelo. No habrá umbrales específicos para subconjuntos, tamaños activos, órdenes o batches.

Los nuevos umbrales deben vincularse al hash del core y del manifest correspondiente. No copiar b
r
	​

 de ninguna campaña anterior.

4. Inmutabilidad y lectura de los valores almacenados

Comprobar el core antes de calibrar y después de terminar B.

Mantener separados los dos tipos de evidencia:

SHA256
archivo checkpoint
before
	​

=SHA256
archivo checkpoint
after
	​


y:

hash
estado de tensores
before
	​

=hash
estado de tensores
after
	​

.

No exigir que el hash del archivo sea igual al hash del estado de tensores: son representaciones diferentes. La igualdad requerida es antes/después dentro de cada esquema.

Después de guardar las calibraciones, volver a leerlas, reconstruir los cuatro valores en el dtype de inferencia y verificar que coinciden con los valores almacenados previstos y siguen dentro de sus intervalos.

No hace falta reserializar ni sobrescribir los checkpoints de A. La salida de B puede consistir exclusivamente en archivos de calibración y resultados.

5. Sanity funcional permitido: sólo las mismas filas atómicas

Autorizo comprobar la conexión de NULL usando las 128 filas atómicas base de cada seed, sin abrir ninguna instrucción multi-cláusula.

En una fila de rol r:

r→ARG
i
	​

,

mientras las otras tres queries deben devolver:

r
′

=r→NONE.

Los conteos esperados en los cinco modelos son:

640 decisiones de rol presente,
1920 decisiones de rol ausente,
2560 decisiones de query en total.

Exigir el argumento correcto para el rol presente, NULL explícito para los ausentes y 640/640 salidas atómicas estructuradas completas.

La canonicalización sólo se aplica a presencia predicha y utiliza el índice decodificado por el modelo. VALUE_0 continúa siendo distinto de ausencia; no usar vectores cero ni codebook[-1].

Este sanity utiliza datos del catálogo de calibración. Es una comprobación de implementación, no evidencia nueva de generalización composicional.

6. Límites y regla de decisión

Durante B:

No añadir ni inicializar e
N
	​

, ni siquiera para medir sus scores.

No ampliar el vocabulario a 38 filas ni el catálogo BG a 42.

No ejecutar los 34 contextos NOOP.

No abrir evaluación multi-cláusula base o aumentada.

No generar 780x.

Si los cinco cores conservan su integridad, los 20 intervalos pasan tras almacenamiento y el sanity atómico es correcto:

T7 STAGE-B DEVELOPMENT: PASS
	​

.

Eso dejará preparado el sistema de referencia para la futura intervención sobre NOOP, pero no autoriza automáticamente C.

Si un intervalo no existe, reportarlo sin buscar otro threshold. Si hay discrepancias de mapping, carga, dtype o procedencia, detenerse como defecto de implementación y conservar la evidencia; no convertirlo en un fallo científico del binder.

Entrega

Usar un directorio exclusivo, por ejemplo:

campaign/t7_noop_none_lexical_stage_b_development/

Entregar las cinco calibraciones y un consolidado con sus hashes, los 20 intervalos completos, las holguras mínimas, el sanity atómico y la comprobación de inmutabilidad. Mantener ejecución secuencial, sin delegación ni spawning.

La autorización es exclusivamente calibrar y congelar NULL desde los catálogos base. El propósito es que, cuando se entrene NOOP en C, ni el core ni los umbrales puedan moverse para compensar su comportamiento.