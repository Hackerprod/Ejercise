**Sí: confirmo `T1-CTRL-2 piloto: CLOSED / PASS` para la receta publicada, M32 y H0–H4. El siguiente paso no sería añadir otra decisión oráculo ni aumentar la memoria: haría una evaluación congelada de las 1,024 parejas numéricas posibles.**

El resultado demuestra la composición que buscábamos. Lo pendiente es determinar cuánto generaliza la comparación aprendida fuera de la selección de referencias usada durante entrenamiento. **Podemos responder esa pregunta sin entrenar nada y sin cambiar el ejecutor.**

Revisé `b5c59505d9cea4e8d5e304a9dd7a7279644ce5d3`, `train_u0c_ctrl2.py`, sus helpers y los resultados publicados. No he reejecutado los checkpoints, recalculado sus hashes ni corrido los tests.

## 1. El piloto cumple su objetivo

Los resultados publicados confirman las tres mediciones:

| Medición                                     |                                    Resultado |
| -------------------------------------------- | -------------------------------------------: |
| CTRL-1 congelado + ajuste oráculo + ejecutor |                                16,955/16,955 |
| CTRL-2 sobre R real después de COPY          |                                16,955/16,955 |
| CTRL-1 + CTRL-2, decisiones aprendidas       | 16,955/16,955, tanto salida como trayectoria |

También coinciden los desgloses: INCREASE `7,851/7,851`, KEEP `2,000/2,000` y DECREASE `7,104/7,104`. El seleccionado y el final cumplen; ambos corresponden al step5000.

La interfaz mantiene la separación solicitada: el nuevo MLP recibe únicamente R y el embedding de la referencia. El dispatch ejecuta ADD, SUB o KEEP desde la **acción elegida**, sin volver a comparar x con b para corregirla. CTRL-1 y el ejecutor permanecen congelados.

Por tanto, la afirmación respaldada es:

> **El sistema aprende cuándo terminar la navegación y, después, qué operación aplicar al valor recuperado, utilizando dos políticas neuronales conectadas con las primitivas congeladas.**

La secuencia de fases —navegar, recoger, ajustar, emitir— continúa definida por el runtime. No es todavía un router general que decide libremente todo el programa. Eso no reduce el pase del piloto: delimita qué decisión hemos retirado del oráculo.

## 2. El ajuste de referencias es válido, pero cambió el currículo

Añadir los anchors a `reference_values(x)` no fue solamente corregir un evaluador. **Cambió la distribución de entrenamiento y la distribución principal de evaluación.** Es una modificación legítima, pero deben conservarse por separado el intento anterior, su control fallido y la receta ampliada que ahora pasa.

El código actual utiliza:

$$
B(x)=\{0,8,16,20,24,31,x\}
\cup
\{x-1,x+1\text{ cuando son válidos}\}.
$$

Esa es la receta aprobada por este piloto.

### El control con b=13 sí contiene parejas no admitidas por esa política

Confirmé que:

$$
13\notin B(10),\qquad13\notin B(25).
$$

Pero hay que describirlo como **generalización a esas dos combinaciones**, no como una referencia numérica globalmente desconocida: la misma política incluye 13 cuando x es 12, 13 o 14.

Tampoco es necesario que todos los controles causales sean OOD. Por ejemplo, `(20,20)` pertenece al currículo, y aun así es un control válido para comprobar KEEP al cambiar la referencia. **Intervención causal y generalización fuera de distribución son dos propiedades distintas.**

La cautela adicional es que el nuevo control se eligió durante una investigación que ya había utilizado los resultados anteriores para modificar el currículo. Lo registraría como **diagnóstico exploratorio positivo**, no como un test final completamente ciego. Adaptar análisis o decisiones a resultados observados puede sobreajustar la evaluación; precisamente por eso conviene fijar ahora una prueba completa en lugar de seguir escogiendo referencias individuales. ([arXiv][1])

Esto no invalida el pase funcional. Limita la amplitud de la afirmación OOD.

## 3. La cobertura numérica explica cuál es la siguiente pregunta

Enumeré la regla de `reference_values()` publicada. El resultado es:

| Conjunto                         | Parejas \((x,b)\) |
| -------------------------------- | ----------------: |
| Dominio completo: \(32\times32\) |         **1,024** |
| Admitidas por la política actual |           **270** |
| Excluidas por esa política       |           **754** |

Este conteo procede de enumerar la función, **no de ejecutar el modelo ni de leer las parejas efectivamente muestreadas durante entrenamiento**. Para distinguir visto/no visto de forma definitiva, debe contrastarse con los labels guardados de la corrida.

Las 16,955 evaluaciones aportan variedad de memorias, trayectorias y payloads. Pero no son 16,955 relaciones numéricas distintas: el dataset repite cada R recuperado para varias referencias del mismo episodio, y utiliza la misma política de referencias en los splits.

**El dominio que falta comprobar es pequeño y finito.** Antes de añadir una tercera red o repetir cinco entrenamientos, conviene saber si CTRL-2 aprendió una comparación suficientemente general o una solución muy ajustada a esos 270 pares admitidos.

No estoy afirmando que haya memorizado la tabla. Los resultados actuales no permiten distinguir completamente ambas posibilidades.

## 4. Dos correcciones del evaluador, sin reentrenar ni reabrir el piloto

### El `pass` del control de referencia es demasiado débil

Actualmente se comprueba:

```python
intervention_b_pass = (
    len(intervention_b) == 3
    and {entry["predicted_action"] for entry in intervention_b}
        == set(ADJUSTMENT_NAMES)
)
```

Esto exige que aparezcan las tres acciones, **pero no que cada una corresponda al caso correcto**. Una permutación de las tres respuestas también pasaría. El JSON actual sí contiene las respuestas individuales correctas, por lo que el resultado informado no queda invalidado; el problema está en la aserción que protegerá ejecuciones futuras.

Debe incluir:

```python
intervention_b_pass = (
    len(intervention_b) == 3
    and all(
        entry["predicted_action"] == entry["expected_action"]
        for entry in intervention_b
    )
)
```

Añadir un test donde las tres clases estén presentes pero asignadas a casos incorrectos. Ese test debe fallar.

### La descripción del currículo en los manifests quedó antigua

El runner todavía escribe:

```text
references={0,31,x,x-1 if valid,x+1 if valid}
```

aunque la función también incluye `8,16,20,24`.

Los hashes identifican exactamente los archivos, pero no garantizan que una descripción interna sea correcta.

No sobrescribiría silenciosamente los artefactos originales. Añadiría una corrección de metadatos y, para las próximas evaluaciones, registraría los anchors y la tabla de parejas admitidas en formato estructurado. La evidencia principal de exposición al entrenamiento debe ser el contenido de los labels, no una cadena descriptiva.

**Ninguno de estos dos ajustes exige otra corrida de entrenamiento.**

## 5. Siguiente gate autorizado: `T1-CTRL-2-G`, cobertura numérica congelada

Mantener exactamente el checkpoint final actual, CTRL-1, el ejecutor, M32, H0–H4, codebooks y políticas de lectura. No añadir anchors ni modificar pesos durante esta campaña.

La evaluación tendrá dos capas complementarias.

### A. Tabla completa con R canónico: aislar el comparador

Para cada:

$$
(x,b)\in\{0,\ldots,31\}^2,
$$

construir:

$$
R=E_{\mathrm{value}}(x),\qquad
B=E_{\mathrm{value}}(b),
$$

y obtener la acción del controlador.

Esta es una prueba del componente, no del programa completo. Utilizar los embeddings numéricos correctos:

```text
token_embedding[VALUE_BASE + x]
token_embedding[VALUE_BASE + b]
```

La comparación entera se utiliza únicamente para calcular la etiqueta esperada.

Registrar las 1,024 predicciones, la matriz de confusión y el margen firmado de la acción correcta:

$$
\gamma=z_{a^*}-\max_{a\ne a^*}z_a.
$$

Separar parejas presentes y ausentes en el entrenamiento real. La política actual incluye todas las igualdades \(x=b\); por tanto, **no habrá un grupo KEEP OOD por pareja numérica bajo esa definición**, aunque sí puedan existir contextos de recuperación nuevos. Esa ausencia debe registrarse como tal, no inventarse un balance OOD artificial.

**Para afirmar “tabla canónica completa resuelta”, el resultado debe ser 1,024/1,024.** Si aparece un error, se conserva: no convierte retroactivamente el piloto en fallo, pero impide esa afirmación nueva.

### B. Todas las referencias sobre los R realmente recuperados

Reutilizar los **2,000 episodios base del test**, conservar el R real después de COPY y evaluar las 32 referencias para cada uno:

$$
2000\times32=64\,000
$$

casos de ajuste contextual.

Es válido reutilizar cada prefijo de navegación una sola vez porque CTRL-1 no recibe b y permanece congelado. El runner actual ya utiliza ese tipo de reutilización del estado de navegación para las referencias de un mismo episodio. **No reemplazar el R real por su embedding canónico en esta segunda capa.**

Las mediciones vuelven a ser:

| Condición                                      | Qué localiza                                     |
| ---------------------------------------------- | ------------------------------------------------ |
| Ajuste oráculo sobre el R real                 | Capacidad del ejecutor en la evaluación ampliada |
| CTRL-2 sobre ese mismo R y cada referencia     | Generalización de la decisión                    |
| Acción predicha ejecutada mediante la ALU real | Composición completa                             |

Si alguna navegación falla, debe permanecer contabilizada. No eliminarla para fabricar un dataset de clasificación más limpio ni reparar R desde el target.

Esta capa distingue situaciones que la tabla canónica no separa:

```text
Canónico correcto, R real incorrecto:
sensibilidad a la representación recuperada.

Ambos incorrectos en la misma pareja:
problema de generalización del comparador.

Acción correcta, resultado incorrecto:
problema del ejecutor o del transporte.
```

Son interpretaciones diagnósticas, no resultados anticipados.

## 6. Congelar la evaluación antes de mirar dónde falla

El manifest nuevo debe contener las 1,024 parejas y todas las referencias contextuales desde el principio. No elegir otra b después de observar un error.

Para la partición visto/no visto:

$$
S_{\mathrm{train}}
=
\{(x,b)\text{ presentes en los labels de entrenamiento guardados}\}.
$$

Usar ese conjunto real, no solo `reference_values()`. La función sirve como comprobación de consistencia.

Reportar por separado las parejas ya utilizadas en los diagnósticos de desarrollo. La nueva campaña caracteriza el checkpoint congelado sobre todo el dominio; no debe presentarse como si cada punto fuera un holdout nunca consultado.

En los casos contextuales conservaría el gate previamente establecido de `≥99.9%` para decisión, éxito final y trayectoria, desglosado por clase y distancia, **además de la tabla por pareja**. Una media global no debe ocultar que una combinación concreta falla repetidamente.

Las 64,000 evaluaciones no son 64,000 grafos independientes. Son 32 objetivos de ajuste aplicados a 2,000 estados recuperados. El conteo debe conservar esa organización.

**No autorizo fine-tuning dentro de esta campaña.** Si aparecen errores, el resultado es un mapa de límites del checkpoint actual. Añadir sus parejas al entrenamiento y volver a medir sería otra receta, con otro alcance.

## 7. Por qué esto precede a M64 o a más semillas

M64 cambiaría la calidad de los payloads y haría más difícil separar una comparación defectuosa de una representación degradada.

Cuatro semillas nuevas medirían robustez del entrenamiento, pero podrían repetir la misma falta de cobertura. **Primero conviene saber qué problema numérico resuelve realmente la receta; después tiene sentido medir si distintas inicializaciones lo resuelven de forma reproducible.**

Tampoco añadiría todavía otro controlador. Ya existe una pregunta pendiente dentro del actual que puede resolverse exhaustivamente sin nuevos módulos.

Si esta evaluación pasa, la siguiente inversión razonable será la replicación del entrenamiento de CTRL-2 con la receta y los datos congelados. No hace falta anticipar ahora otra familia de decisiones ni ampliar simultáneamente memoria y control.

## Resolución

```text
T1-CTRL-2 piloto, seed2201:
CLOSED / PASS en la receta publicada.

Controles causales:
respuestas observadas correctas;
fortalecer la aserción del control de referencia.

Generalización numérica completa:
todavía no demostrada.

T1-CTRL-2-G:
AUTORIZADO, evaluación congelada 32×32
y referencias completas sobre los R reales del test.

M64, más entrenamiento y otro controlador:
fuera de esta campaña.
```

**El avance ya logrado es real: la operación aplicada al dato recuperado la decide una red, no un sufijo aritmético fijo. Ahora debemos comprobar que esa decisión cubre el dominio numérico declarado, en vez de inferirlo a partir de una distribución parcial y unos pocos controles escogidos.**

[1]: https://arxiv.org/abs/1506.02629?utm_source=chatgpt.com "Generalization in Adaptive Data Analysis and Holdout Reuse"
