La preparación fresh está correctamente congelada y el commit `384c90d46245...` coincide con lo predeclarado: recipe freeze, cinco manifests `7201–7205`, cinco mappings válidos, `29760/29760` filas verificadas y cero training/model construction.

$$
\boxed{\text{T4-NOBYPASS-3-FRESH / TRAINING: AUTHORIZED}}
$$

Ejecutá los cinco seeds completos, aunque alguno falle. No cambiar nada entre seeds.

### Receta congelada

Fresh init total por seed. Prohibido cargar checkpoints de `710x`, `700x`, `690x` o anteriores.

Mantener exactamente:

$$
k_t=LN(SiLU(W_k[e_{t-1},e_t]))
$$

$$
s_{r,t}=q_r^\top k_t/4
$$

$$
j_r=\arg\max_{t<length}s_{r,t}
$$

$$
r_r=W_v(e_{j_r}),\qquad r\in\{F,A,M\}.
$$

Con shared \(W_v\), pure HARDPTR, sin gate/mask/Stage B/soft mixture.

Objective:

$$
L=
L_{behavior}^{F/A}
+
L_{ref}^{F/A/M}
+
L_{sep}^{3+BG}.
$$

Y exactamente los nueve términos congelados:

$$
6\ role\rightarrow role + 3\ role\rightarrow BG.
$$

Los 38 contextos BG siguen siendo exactamente:

$$
3\ START\rightarrow OP
+
32\ ARG_i\rightarrow LINK
+
3\ LINK\rightarrow OP.
$$

Mantener:

* 5000 updates;
* AdamW;
* `wd=0`;
* LR `1e-3→1e-5`;
* coeficiente global `1.0`;
* 96 atomic rows;
* `multi_clause_train=0`;
* `local_background_negative_contexts=true`;
* cero joint examples en training;
* cero early stopping/tuning mirando test.

## Gates fresh

Primero atomic, por seed:

$$
FLOOR=32/32,\quad AVOID=32/32,\quad MATCH=32/32.
$$

Después exigir los nueve certificados:

$$
G_{F,A},G_{F,M},
G_{A,F},G_{A,M},
G_{M,F},G_{M,A}>0
$$

y:

$$
G_{F,BG},G_{A,BG},G_{M,BG}>0.
$$

Además mantener separado:

$$
G_{r,ARG\rightarrow LINK}>0
$$

para los tres roles, como sanity causal.

### End-to-end

Evaluar los manifests ya sellados:

$$
5\times992\times6=29760
$$

casos.

Exigir exactitud total en las 15 métricas:

* pointer F/A/M;
* decode F/A/M;
* RAW F/A/M;
* CANON F/A/M;
* RAW≡CANON F/A/M.

Y los nueve márgenes:

$$
M_F^A,\ M_F^M,\ M_F^{BG}
$$

$$
M_A^F,\ M_A^M,\ M_A^{BG}
$$

$$
M_M^F,\ M_M^A,\ M_M^{BG}
$$

todos estrictamente:

$$
>0.
$$

Reportar también el mínimo global de cada uno con seed, orden, `(L,F,E)`, target y competidor ganador.

Además:

$$
six\_order\_outcome\_equality=true
$$

$$
mismatch\_count=0.
$$

## Regla de decisión ya congelada

Sólo si los cinco seeds cierran absolutamente todo:

$$
\boxed{\text{T4-NOBYPASS-3-FRESH: PASS\_STRONG}}
$$

Cualquier fallo, incluso uno:

$$
\boxed{\text{VALID FAIL}}
$$

y no se modifica nada usando `7201–7205`.

No convertir los restantes en “secondary replication”; los cinco deben ejecutarse completos bajo la misma receta.

Si pasa, el claim preciso será que **RCSEP3-BG escala el binding sistemático a tres relaciones operator→argument simultáneamente activas en dominios lexicales fresh, con cero entrenamiento multi-cláusula y exactitud en los seis órdenes**, dejando explícito que el rechazo BG fue entrenado mediante negativos locales y no es una generalización espontánea.

**Autorización explícita: iniciar ahora training fresh `7201–7205` con la receta RCSEP3-BG congelada.**
