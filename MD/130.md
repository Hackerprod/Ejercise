La ruta la defino ahora: supervisión explícita de NONE, manteniendo congelados el binder base y sus umbrales. Como primera candidata, entrenaría únicamente el embedding del nuevo operador NOOP. No hace falta pedir a opencode otro listado de opciones.

Confirmo el cierre del audit como DESIGN AUDIT COMPLETE, no como validación funcional de T7. El código consultado confirma que build_training_batch acepta sólo los cuatro roles y que objective_n4 no contiene una ruta NOOP/NONE.

También comprobé la distinción histórica de T3: se calcula noop_details, pero las condiciones de las filas NOOP permanecen en cero y no utilizan los estados producidos por su encoder. Eso conserva el resultado observado de invariancia de T3, pero no sustenta que NOOP hubiera aprendido su irrelevancia mediante supervisión atómica efectiva. Dejemos esa precisión en T7 sin modificar los artefactos históricos.

1. Primera candidata: aprender el nuevo símbolo, no rehacer el binder

La propuesta será:

T7-NOOP-NONE-LEXICAL
	​


con tres etapas separadas:

Etapa	Qué se modifica	Qué queda fijo
A. Binder base	Entrenamiento atómico de los cuatro roles, con la receta T6	Receta y catálogos base
B. Calibración	Se calculan los cuatro b
r
	​

 mediante midpoint	Pesos del binder
C. Aprendizaje NOOP	Sólo el nuevo embedding e
N
	​

	Binder base completo y los cuatro b
r
	​


En la futura campaña, la etapa A utilizará modelos nuevos de T7. No reutilizaremos checkpoints 750x/760x.

Después de la etapa B, añadiríamos una fila nueva al vocabulario:

e
N
	​

=e
OP
NOOP
	​

	​

∈R
16
.

Durante la etapa C, únicamente esos 16 escalares serían entrenables. Permanecen congelados los embeddings anteriores, las cuatro queries, el transformador local de keys, su normalización, W
v
	​

, el decoder y los umbrales.

La motivación es experimental: así no podemos resolver el problema moviendo NULL, alterando la separación de los cuatro roles o modificando su representación de valores. Probamos si el nuevo símbolo puede aprender a representar una instrucción irrelevante dentro de la geometría ya disponible.

No afirmo todavía que 16 parámetros basten. Es una primera hipótesis acotada, no una garantía.

2. Target de entrenamiento: cuatro NONE

Cada ejemplo atómico:

[OP
NOOP
	​

,ARG
D
	​

]

tendrá como target:

(F,A,M,H)=(NONE,NONE,NONE,NONE).

No necesita un target numérico para D: ese valor forma parte de una cláusula que no debe producir ninguna de las cuatro referencias.

Tampoco necesita un opcode nuevo de C1 ni una condición constante entregada al supervisor para aparentar éxito.

La pérdida debe depender de los scores reales producidos por el encoder para NOOP. Ésa es la diferencia decisiva respecto a la ruta desconectada que identificaste.

Los negativos que vamos a supervisar

Usar exactamente los 34 contextos nuevos ya formalizados:

U={OP
NOOP
	​

→ARG
D
	​

:D=0,…,31}
∪{START→OP
NOOP
	​

,LINK→OP
NOOP
	​

}.

Los 32 ejemplos atómicos cubren el argumento NOOP y el operador al comienzo. El contexto LINK→OP_NOOP se añade como negativo local de estructura, no como instrucción multi-cláusula.

Para ese último contexto se supervisa sólo el score de la posición OP_NOOP. No convertir [LINK,OP_NOOP] en una instrucción completa con una semántica inventada, ni introducir accidentalmente START→LINK como nuevo negativo.

3. Pérdida exacta: cada candidato NOOP debe perder contra NULL

Para cada query r y cada contexto z∈U:

ℓ
r,z
	​

=softplus(s
r
	​

(z;e
N
	​

)−b
r
	​

).

La pérdida de esta etapa será:

L
NOOP→NONE
	​

=
4⋅34
1
	​

r∈{F,A,M,H}
∑
	​

z∈U
∑
	​

softplus(s
r
	​

(z;e
N
	​

)−b
r
	​

)
	​


Es una media sobre 136 comparaciones, sin términos ocultos y sin pasar por el argmax durante el entrenamiento.

Esta forma utiliza softplus(x)=log(1+e
x
). 
PyTorch Documentation
 Puede interpretarse como la pérdida logística de la comparación binaria «NULL debe ganar a este candidato»:

−logσ(b
r
	​

−s
r
	​

(z)).

La dirección del gradiente es explícita:

∂s
r
	​

(z)
∂ℓ
r,z
	​

	​

=σ(s
r
	​

(z)−b
r
	​

)>0.

Por tanto, minimizarla ejerce presión para bajar el score del candidato respecto al NULL fijo.

No basta entrenar SELF > NOOP. Eso permitiría que NOOP quedara por debajo del argumento correcto, pero por encima de NULL, produciendo falsos positivos cuando el rol está ausente. Aquí entrenamos directamente la desigualdad necesaria:

s
r
	​

(z)<b
r
	​

	​

.

No se añade una quinta query, una salida NOOP ni un gate lexical.

4. Qué protocolo de calibración estamos eligiendo

Elijo calibración base sin incorporar NOOP, con supervisión posterior del nuevo símbolo.

El orden será:

entrenar binder base→calibrar b
r
	​

 sin NOOP→congelar ambos→entrenar e
N
	​

.

Los scores NOOP no participan en el cálculo inicial ni en una actualización posterior de los umbrales.

Eso elimina una explicación alternativa del eventual éxito:

«El sistema dejó de activar roles porque ampliamos el máximo negativo y movimos NULL después de observar al distractor».

Aquí los umbrales no se moverían.

Pero debemos describir honestamente la otra mitad del protocolo: NOOP sí recibiría supervisión explícita contra esos umbrales. Un eventual PASS no sería rechazo espontáneo de un operador desconocido. Sería composición de un operador aprendido atómicamente como irrelevante, con un binder y una regla de ausencia preservados.

La calibración expandida queda fuera de esta primera candidata. Si esta versión falla, no se incorpora silenciosamente como rescate.

5. Por qué congelar el resto proporciona una comparación limpia

El core consultado construye keys desde el embedding actual y el anterior, y sus values desde el embedding actual.

Por esa localidad, si sólo cambia la nueva fila e
N
	​

, una instrucción que no contiene NOOP no depende de ese parámetro. Con el mismo mapeo de los tokens anteriores y el mismo camino de operaciones, su salida debería permanecer idéntica.

Al insertar NOOP, los contextos realmente nuevos son justamente los de U. Los contextos ARG_D→LINK ya pertenecen al catálogo base.

Así obtenemos una descomposición verificable:

comportamiento sin NOOP: preservado
rechazo de contextos nuevos: aprendido mediante L
NOOP→NONE
	​

.

Esto sería mucho más fácil de atribuir que reentrenar todas las queries, embeddings y umbrales a la vez.

Precaución importante de autograd

Congelar los pesos no significa ejecutar el camino NOOP dentro de torch.no_grad().

Los parámetros del core tendrán requires_grad=False, pero el cálculo de scores debe conservar el grafo hasta e
N
	​

. PyTorch puede propagar gradientes a una entrada entrenable a través de operaciones cuyos otros parámetros estén congelados; no_grad() deshabilitaría precisamente ese registro. 
PyTorch Documentation
+1

El optimizador futuro debe recibir exclusivamente e
N
	​

. No depender sólo de que «las demás filas suelen tener gradiente cero».

6. Siguiente unidad concreta: preflight de supervisión, no campaña
T7-NOOP-NONE-SUPERVISION-PREFLIGHT: AUTHORIZED
	​


Autoriza implementar un módulo aislado y pruebas sintéticas. Todavía no autoriza entrenamiento real, carga de checkpoints históricos para probar la hipótesis, manifests de campaña ni checkpoints nuevos.

El preflight debe comprobar:

Catálogo y pérdida. Exactamente 34 contextos y una matriz de diferencias [4,34]; media sobre 136 términos, con orientación score - b. El tratamiento de START debe ser el mismo del forward existente.

Conexión de gradiente. Con un fixture sintético determinista, ejecutar un backward real y demostrar que la pérdida depende de e
N
	​

. Verificar también sobre los scores sintéticos la derivada esperada. No aceptar un flag escrito a mano como evidencia.

Congelamiento. Sólo la fila nueva tiene parámetros entrenables. Las filas antiguas, queries, key network, W
v
	​

 y b
r
	​

 no participan como parámetros optimizables.

Preservación de la ruta anterior. Cambiar manualmente e
N
	​

 dentro de una prueba sintética y comprobar que las salidas para instrucciones sin ese token permanecen bit-exact. Mantener invariables los IDs internos anteriores al ampliar el vocabulario.

Inferencia sin atajos. El nuevo operador pasa por el lookup y el scorer ordinarios. No hay un if OP_NOOP: return NONE, eliminación de cláusulas, máscara por identidad del token ni entrada active_roles.

Contrato NULL. NOOP atómico tiene cuatro targets NONE; eso no equivale a producir cuatro vectores cero ni cuatro VALUE_0. La canonicalización sigue condicionada a presencia predicha.

No ejecutar optimizer.step() en esta unidad. Los tests pueden construir módulos sintéticos y hacer backward; no se ajustan parámetros de un modelo de campaña.

Una precisión de código que debe quedar registrada

Al consultar GenericNRoleBinder.forward, encontré que la key se calcula con:

local_norm(local_binding(concat(previous, embeddings)))

No aparece una SiLU explícita en ese forward, aunque varias fórmulas históricas la mencionaban.

Esta autorización conserva el código real hasheado: no añadir una activación para hacerlo coincidir con el diagrama. Registrar la discrepancia documental en el preflight, sin modificar los artefactos ni resultados cerrados.

7. Regla científica para la futura prueba

El gate de la etapa NOOP no será simplemente «la loss bajó». Será:

G
r
NOOP→NULL
	​

=b
r
	​

−
z∈U
max
	​

s
r
	​

(z)>0
	​


para los cuatro roles, además de la preservación del comportamiento base.

Si esos certificados pasan, la evaluación emparejada comprobará que insertar NOOP conserva tanto los argumentos presentes como la ausencia de los demás roles.

Si no pasan, la conclusión será limitada:

Entrenar únicamente el embedding NOOP, con este core congelado y este presupuesto, no consiguió el rechazo requerido.

No concluiríamos automáticamente que hace falta recalibrar NULL, que el binder no puede representar NOOP o que debe reentrenarse todo. Tampoco desbloquearíamos más parámetros dentro de esa misma corrida.

Dirección

No hace falta que opencode proponga alternativas antes de avanzar. La candidata queda definida:

core y NULL congelados+embedding NOOP entrenable+supervisi
o
ˊ
n expl
ı
ˊ
cita score-vs-NULL
	​


La siguiente entrega autorizada es el preflight de esa ruta de supervisión, con pruebas sintéticas de gradiente, congelamiento y ausencia de atajos. Sin entrenamiento de campaña todavía, sin tocar T6 y sin delegación ni spawning de agentes.