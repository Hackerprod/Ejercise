Confirmo T7-NOOP-NONE-LEXICAL: PREFLIGHT PASS (SYNTHETIC ONLY). Todavía no iniciaría la etapa A: falta sellar los dominios de T7, el presupuesto de aprendizaje de NOOP y la evaluación emparejada. La siguiente unidad autorizada es esa preparación, no otra autopsia.

Leí el commit 8f22eeef7e34…, el módulo y los tests. El código corregido selecciona la posición 1 para OP_NOOP→ARG_D, la posición 0 para START→OP_NOOP y la posición 1 para LINK→OP_NOOP. Los tests ahora comprueban esas posiciones y la dependencia de los scores respecto del argumento. No he reejecutado tu fixture ni recalculado sus hashes.

1. Qué queda aceptado y qué sigue abierto

El preflight demuestra que la pérdida está conectada con e
N
	​

, que su derivada respecto de los scores tiene la orientación prevista y que cambiar la fila nueva no altera las instrucciones que sólo contienen tokens anteriores. Ése es el alcance que registra el commit.

No demuestra todavía que un único embedding de 16 dimensiones pueda colocar los 34 contextos por debajo de los cuatro umbrales de un binder entrenado. Como todas esas desigualdades comparten e
N
	​

, un gradiente conectado no garantiza que puedan satisfacerse simultáneamente.

La campaña real debe responder precisamente esa pregunta.

También mantendría una distinción en los tests: que las 32 columnas sean distintas es un buen control contra el bug observado en este fixture, pero no debe convertirse en un requisito científico para un modelo aprendido. El requisito general es seleccionar y puntuar los contextos correctos; distintos contextos pueden obtener scores iguales legítimamente.

2. Siguiente unidad: preparación development de T7
T7-NOOP-NONE-LEXICAL-DEVELOPMENT-PREPARATION: AUTHORIZED
	​


Fijamos:

7701–7705=T7 development

y reservamos:

7801–7805=futura validaci
o
ˊ
n fresh.

No generar ni inspeccionar 780x todavía. No cargar checkpoints históricos para probar esta hipótesis ni modificar T6.

La preparación debe incluir manifests, checker independiente, freeze de las etapas A/B/C e integración del módulo NOOP con el core real. Cero entrenamiento de campaña y cero checkpoints nuevos durante esta unidad.

Dominios y asignaciones

Habrá cinco operadores opacos de entrada:

(OP
V
	​

,OP
W
	​

,OP
X
	​

,OP
Y
	​

,OP
Z
	​

)

asignados a:

(FLOOR,AVOID,MATCH,ANCHOR,NOOP).

Las salidas siguen siendo sólo los cuatro roles activos.

Precomprometer las cinco asignaciones con:

Python
Run
random.Random(7700).sample(all_120_operator_role_permutations, 5)

Enumerar las permutaciones desde el orden de semánticas escrito arriba y registrar las cinco elegidas y las 115 omitidas. Cada seed determina además su permutación ARG→VALUE e IDs físicos nuevos.

El operador asignado a NOOP queda reservado en el manifest, pero no se incorpora al vocabulario entrenable de la etapa A. Se añade después de calibrar, como una fila nueva al final de la tabla interna. Los IDs internos anteriores no se renumeran.

3. Congelar ahora las tres etapas
Etapa A — binder base

Reutilizar exactamente la receta T6 para los cuatro roles activos: 128 filas atómicas, RCSEP-N+BG con 12 términos entre roles y 4 contra BG, 40 contextos base, reducción /16, 5000 updates y los mismos hiperparámetros.

NOOP no entra en el batch, en la pérdida ni en el catálogo BG de esta etapa. El modelo se inicializa desde cero en cada dominio T7.

Etapa B — NULL base

Congelar el core y calcular los cuatro umbrales mediante la regla ya aprobada:

b
r
	​

=N
r
max
	​

+
2
P
r
min
	​

−N
r
max
	​

	​

.

Fuentes exclusivas: 128 relaciones atómicas de los cuatro roles y 40 contextos BG base.

Comprobar las desigualdades estrictas con los umbrales almacenados en el dtype de inferencia. Después, congelarlos.

No incorporar los contextos NOOP a este cálculo ni recalibrar después de aprender e
N
	​

.

Etapa C — aprender únicamente e
N
	​


Añadir el embedding NOOP después del freeze de A/B. Como primera receta development, fijo:

Elemento	Valor
Parámetros entrenables	Únicamente e
N
	​

∈R
16

Inicialización	Normal estándar, con generador independiente
Semilla de inicialización	100000 + domain_seed
Optimizador	AdamW
Weight decay	0
Actualizaciones	5000
Learning rate	Lineal 1e-3 → 1e-5
Datos de cada update	Los 34 contextos completos
Selección final	Última actualización, sin early stopping

El generador independiente impide que añadir NOOP cambie accidentalmente la inicialización del core base.

La única pérdida de C será:

L
NOOP→NONE
	​

=
136
1
	​

r=1
∑
4
	​

z∈U
∑
	​

softplus(s
r
	​

(z;e
N
	​

)−b
r
	​

)
	​


No añadir regularizadores, márgenes extra, pérdidas de valores ni términos de las tareas activas. No desbloquear queries, W
v
	​

, normalización, transformador de keys o embeddings anteriores si el ajuste de e
N
	​

 resulta insuficiente.

Esto fija un primer presupuesto experimental, no una afirmación de que vaya a bastar.

4. Integración con el core real: no usar el fixture como modelo de campaña

SyntheticNoopBinder es un fixture aislado. Por ejemplo, su w_v tiene salida de dimensión igual a dimension; no debe sustituir la proyección de valores existente del binder de producción.

La integración debe conservar el forward real hasheado y añadir sólo el lookup de la fila NOOP. En particular:

No añadir la SiLU ausente del código real.

No cambiar la proyección de valores ni el orden de operaciones para “parecerse” al fixture.

Mantener un grafo diferenciable desde los scores hasta e
N
	​

, con el resto de parámetros congelados.

El optimizador de C debe recibir exclusivamente la fila nueva.

En inferencia, NOOP pasa por el lookup y scorer ordinarios: sin eliminación de cláusulas, máscara semántica ni if NOOP: return NONE.

Antes de training, una prueba con parámetros sintéticos del core de producción debe comprobar la preservación del camino anterior al añadir/cambiar e
N
	​

, el catálogo [4,34] y la conexión del backward. Sin cargar checkpoints históricos ni hacer optimizer.step().

Hay además un residuo documental menor: algunos textos del módulo/artifact todavía describen todos los scores como tomados “en la posición OP_NOOP”. Eso ya no describe correctamente las 32 posiciones ARG. En la nueva integración documentar la regla corregida; no sobrescribir el artifact histórico para hacerlo.

5. Evaluación emparejada que debe sellarse

Conservar para las instrucciones base la construcción de T6: 15 subconjuntos no vacíos, argumentos distintos y todas las órdenes correspondientes.

Para cada asignación activa, elegir un único distractor mediante:

D=(seed+
r∈active
∑
	​

v
r
	​

+1)mod32,

incrementando módulo 32 hasta que D sea distinto de los valores activos.

Después generar todas las permutaciones de las cláusulas completas, incluyendo NOOP. El valor D no cambia entre órdenes.

Los conteos quedan:

Roles activos k	Base por seed	Con NOOP por seed
1	128	256
2	11 904	35 712
3	23 808	95 232
4	23 808	119 040
Total	59 648	250 240

Para cinco dominios habrá 298 240 evaluaciones base y 1 251 200 con NOOP. Las bases singleton siguen siendo controles atómicos; las versiones que combinan un rol activo con NOOP son composiciones nuevas.

Cada instrucción aumentada debe enlazarse con su base mediante la eliminación de la cláusula NOOP en el checker/evaluador, nunca dentro del modelo.

No es necesario duplicar físicamente todos esos textos en JSON: se pueden sellar las asignaciones, D y las listas de órdenes como especificación determinista. El checker debe recorrer y verificar el conjunto lógico completo, sin muestreo ni selección posterior.

Además se preparan los 32 controles atómicos NOOP por seed, con cuatro targets NONE. Se reportarán aparte: son datos utilizados por la etapa C, no evidencia de composición no vista.

Certificado exhaustivo y métricas

Antes de abrir la evaluación multi-cláusula, la candidata deberá satisfacer:

G
r
NOOP→NULL
	​

=b
r
	​

−
z∈U
max
	​

s
r
	​

(z)>0∀r.
	​


Ese certificado cubre los 32 valores posibles del argumento NOOP, no sólo los D elegidos para las instrucciones.

La evaluación debe exigir que base y aumentada sean correctas, y además equivalentes por rol. Dos salidas igualmente equivocadas no cuentan como invariancia.

Separar presencia, ausencia, pointer, valor y resultado estructurado completo. Para roles ausentes, NOOP no puede producir un falso positivo; para presentes, no puede robar el pointer ni alterar su argumento. Mantener RAW/CANON y los márgenes estrictos. La corrección del pointer es obligatoria aunque un valor decodificado coincida accidentalmente.

6. Condiciones antes de autorizar la ejecución

La próxima entrega debe dejar verificables estas cuatro cosas:

Separación de datos: A usa sólo los 128 ejemplos activos; B usa sólo el catálogo base; C usa sólo los 34 contextos NOOP; ninguna etapa lee las instrucciones aumentadas de evaluación.

Separación de parámetros: C puede modificar sólo e
N
	​

; core y umbrales quedan asociados a hashes inmutables.

Integración correcta: el catálogo selecciona realmente los 32 argumentos y los dos contextos estructurales previstos, sin inferir corrección únicamente de sus nombres o de la diversidad de scores.

Protocolo completo sellado: cinco dominios, inicializaciones, presupuesto C, evaluación emparejada, checker y freeze de los componentes ejecutables.

Si después, durante la campaña, alguna etapa no satisface su gate, se conserva el fallo. No se recalibra NULL, no se amplía la capacidad entrenable y no se reemplaza una seed para obtener cierre.

Resolución: preflight sintético aceptado. Autorizo preparar y sellar T7 development 7701–7705, incluida la integración con el core real. Todavía no iniciar la etapa A ni entrenar e
N
	​

.

Así la primera corrida real tendrá definidos de antemano tanto lo que aprende el nuevo operador como la prueba que distinguirá ese aprendizaje atómico de su composición con conjuntos activos variables.