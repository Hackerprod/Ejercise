Confirmo T7 STAGE-A FRESH: CLOSED/PASS y autorizo exclusivamente la etapa B fresh: calibración NULL base sobre los cinco cores 7801–7805 ya entrenados. C y la evaluación emparejada permanecen en HOLD.

Revisé el runner y el consolidado del commit 5a7782757d91…: registran los cinco checkpoints, A1/A2 completos, los 640 controles de mapping y las dependencias congeladas. El runner construye la vista del vocabulario siguiendo token_order, no el orden incidental del diccionario de IDs físicos. No he reejecutado los entrenamientos ni recalculado tu auditoría exhaustiva.

T7 STAGE-A FRESH: CLOSED / PASS
	​

T7 STAGE-B FRESH — BASE NULL CALIBRATION: AUTHORIZED
	​

1. Cargar exactamente los cores de A y conservar su mapping

Utilizar los checkpoints registrados en:

campaign/t7_noop_none_lexical_stage_a_fresh/
    training/seed_<seed>/final.pt

Cada checkpoint debe corresponder a su seed y a su manifest fresh sellado. Los cinco archivos y sus hashes están identificados en el consolidado de A.

Reutilizar la misma construcción de la vista base que A —incluido fresh_manifest_view()— y verificar que el mapping real del vocabulario cargado coincide con token_order y con la metadata del checkpoint.

La tabla continúa teniendo:

37×16

componentes organizados en 32 argumentos, cuatro operadores activos y LINK. No se añade todavía NOOP.

Cargar el estado con correspondencia estricta, poner el core en modo evaluación y congelar todos sus parámetros. B no utiliza optimizador, backward ni actualizaciones.

Se permite reutilizar el código aprobado de development. No se permite cargar pesos ni umbrales development 770x como entradas de esta calibración.

2. Calibración: mismo procedimiento, números nuevos por modelo

Reconstruir mediante el forward de cada core congelado:

128 relaciones at
o
ˊ
micas+40 contextos BG base.

No calcular los umbrales a partir de valores redondeados del reporte de A.

Para cada rol r:

P
r
min
	​

=
i=0,…,31
min
	​

s
r
	​

(OP
r
	​

,ARG
i
	​

),
N
r
max
	​

=max
	​

r
′

=r
j=0,…,31
	​

max
	​

s
r
	​

(OP
r
′
	​

,ARG
j
	​

),
z∈S
4
	​

max
	​

s
r
	​

(z)
	​

.

El máximo negativo considera los 96 argumentos de otros roles y los 40 contextos BG. NOOP no participa.

Aplicar el calibrador congelado, conservando su orden aritmético:

b
r
	​

=N
r
max
	​

+
2
P
r
min
	​

−N
r
max
	​

	​

.
	​


Mantener también las posiciones correctas de los scores: el argumento en OP→ARG, LINK en ARG→LINK y el operador en START→OP o LINK→OP. START conserva el prefijo cero del forward, sin introducir BOS.

Criterio de aceptación

Para los 20 intervalos, exigir valores finitos y:

P
r
min
	​

>N
r
max
	​

.

Después de almacenar b
r
	​

 en el dtype real de inferencia:

P
r
min
	​

−b
r
stored
	​

>0,b
r
stored
	​

−N
r
max
	​

>0.
	​


Registrar los extremos y sus contextos, anchura del intervalo, midpoint calculado, umbral almacenado, dtype y ambas holguras.

No imponer un mínimo de holgura derivado de development o de los márgenes de A. El criterio predeclarado sigue siendo estrictamente >0.

Cada modelo tendrá un único vector de cuatro constantes, ordenado por:

FLOOR, AVOID, MATCH, ANCHOR

No calibrar por subconjunto, orden, tamaño activo o batch. No copiar umbrales de otros seeds.

3. Sanity atómico: utilizar desde el inicio la ruta reparada

El decoder/codebook congelado que utilizó A1 queda explícitamente autorizado en B. Verificar su identidad contra A3.decoder_checkpoint.sha256 de los resultados fresh de A. El consolidado también identifica esa dependencia compartida.

La implementación del sanity debe seguir la reparación aprobada de B development: seleccionar el estado mediante el pointer predicho y ejecutar:

Python
Run
raw, canonical = decode_states(executor, codebook, selected)

La función existente decodifica el estado ampliado con ceros mediante register_decoder y después decodifica el prototipo canónico indexado por el resultado RAW. No aplica argmax directamente a W
v
	​

 ni asigna canonical = decoded.

Incluí en la procedencia del runner B fresh la referencia a ese sanity reparado. El antiguo atomic_sanity() que produjo el intento inválido no debe ejecutarse como parte de esta etapa.

Decisiones que se deben comprobar

Sobre las 128 filas atómicas de cada seed, ejecutar siempre las cuatro queries.

La presencia se determina exclusivamente por:

t<length
max
	​

s
r,t
	​

>b
r
stored
	​

.

Si gana NULL, conservar ausencia explícita y no invocar el decoder. Si se predice presencia, decodificar la posición realmente seleccionada, incluso cuando esa predicción resulte incorrecta. El evaluador no puede sustituirla por el target.

Los totales exigidos son:

Comprobación	Por seed	Cinco seeds
Rol presente: pointer + RAW + CANON correctos	128	640
Roles ausentes: NULL correcto	384	1 920
Instrucciones atómicas completas correctas	128	640

Conservar VALUE_0 como valor presente válido, distinto de NULL. La canonicalización sólo utiliza índices decodificados, nunca valores verdaderos del manifest.

Este sanity utiliza el catálogo atómico conocido. Es una comprobación de implementación y calibración, no evidencia nueva de composición multi-cláusula.

4. Inmutabilidad y artefactos

Comprobar antes y después de B, por separado:

SHA256
checkpoint
before
	​

=SHA256
checkpoint
after
	​

,
hash
estado de tensores
before
	​

=hash
estado de tensores
after
	​

.

Son esquemas distintos: no se espera igualdad entre el hash del archivo y el hash de los tensores. Usar también un esquema consistente para comprobar la inmutabilidad del runtime y del codebook.

Guardar las calibraciones y volver a leerlas desde disco. Reconstruir los umbrales por nombre de rol y en el dtype de inferencia; verificar igualdad con los valores almacenados y que continúan estrictamente dentro de sus intervalos.

Utilizar escritura binaria y verificación byte-exacta, distinguiendo el self-hash del SHA-256 del archivo completo. El valor del reporte de chat no sustituye al registrado y verificado en el artefacto.

Una ubicación exclusiva para esta unidad sería:

campaign/t7_noop_none_lexical_stage_b_fresh/

No sobrescribir los checkpoints de A, los manifests sellados ni artefactos 770x.

5. Regla de cierre y límites

Si los cinco modelos cumplen los 20 intervalos, el sanity atómico y la inmutabilidad:

T7 STAGE-B FRESH: CLOSED / PASS
	​

.

Un intervalo inválido o un fallo del sanity con implementación correcta se registra sin ajustar los umbrales para rescatar el resultado. Una discrepancia de carga, mapping, hashes o decoder exige HALT como defecto de implementación, no clasificar automáticamente un fallo científico.

La entrega debe incluir las cinco calibraciones, los 20 intervalos completos, los conteos por seed, los mínimos de holgura y las referencias exactas a checkpoints, manifests, decoder y runner ejecutado.

Autorización concreta: ejecutar ahora B sobre 7801–7805, secuencialmente y sin delegación ni spawning. No inicializar e
N
	​

, no medir contextos NOOP, no abrir evaluación multi-cláusula y no iniciar C automáticamente aunque B pase.