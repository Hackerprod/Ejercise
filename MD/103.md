No congelaría la receta ni abriría 6901–6905 todavía.

Formalmente:

RCSEP-B-GATE: CLOSED / VALID FAIL
	​


Stage A queda exonerado 5/5: el único fallo funcional restante, 6804, viene de que HARDPTR excluye ARG_07 por c<0.5. Y B2 demuestra que no es un teacher malo:

y(ARG)≥0.998,y(structure)≤0.148.

El problema está entre esa señal limpia y el gate lineal aprendido.

Además, OP_X>0.5 en 6801–6803 significa que no lo trataría como un único outlier de 6804. Stage B no está reproduciendo de manera robusta la partición que el teacher ya conoce.

Antes de corregir: una última prueba muy concreta

Autorizo:

T2-NOBYPASS-2-RCSEP-B-FIT-ALG
	​


Cero training del modelo, cero cambios de checkpoints. Este audit debe decidir si falla:

la capacidad del gate lineal;

el optimizador actual de Stage B;

o el objective soft-BCE respecto al criterio duro c>0.5.

Hay una ventaja importante: con embeddings congelados,

c
t
	​

=σ(w
⊤
e
t
	​

+b)

y BCE con targets fijos es un problema de regresión logística convexo en esos 17 parámetros. Podemos calcular prácticamente el óptimo global; no tenemos que especular sobre “quizá faltaban más steps”.

A. Teacher hard-class sanity

En los cinco RCSEP-A cores calculá:

m
t
	​

=numeric-margin(W
v
	​

(e
t
	​

))

con el mismo decoder y:

y
^
	​

t
	​

=1[m
t
	​

>3.0545].

Exigir:

32/32 ARG→1
3/3 structure→0

en los cinco seeds.

Si esto falla en alguno, HALT: el teacher no es universal.

B. ¿Los embeddings son linealmente separables?

Usando sólo los pseudo-labels derivados de numeric-confidence, no is_VALUE externo, resolver hard-margin linear separability sobre los 35 embeddings raw.

Por seed reportar:

ARG
min
	​

(w
⊤
e+b)
struct
max
	​

(w
⊤
e+b)

y el máximo margen.

Especialmente ARG_07/6804.

Si algún seed es inseparable:

el gate lineal de 17 params no puede garantizar B1
	​


y no tiene sentido arreglar Adam/BCE.

C. Óptimo global del objective Stage B actual

Resolver exactamente la misma loss actual:

L
soft
	​

=
35
1
	​

t
∑
	​

BCEWithLogits(z
t
	​

,y
t
	​

),z
t
	​

=w
⊤
e
t
	​

+b

con los mismos soft targets actuales, pero mediante un optimizador determinista de convergencia, por ejemplo LBFGS con tolerancia estricta o scipy.optimize.

Esto es diagnóstico, no el training candidato.

Por seed guardar:

BCE del gate Adam actual;

BCE óptimo;

grad norm en el óptimo;

min c(ARG);

max c(structure);

clasificación 35/35 según 0.5.

Las ramas quedan clarísimas.

C1 — óptimo soft-BCE pasa B1

Si el óptimo global produce:

minc(ARG)>0.5>maxc(structure)

en 5/5, pero Adam-5000 no:

problema de optimizaci
o
ˊ
n
	​


No cambiamos teacher ni arquitectura; cambiamos solamente la receta de optimización de Stage B.

C2 — óptimo soft-BCE también falla B1

Si el espacio es linealmente separable pero el óptimo de los soft targets deja, por ejemplo, ARG_07<0.5 u OP_X>0.5:

objective/calibration mismatch
	​


La representación puede expresar la solución, pero minimizar fidelidad continua al teacher no equivale a garantizar la clasificación dura requerida por HARDPTR.

Ésta me parece actualmente la rama más probable.

D. Direct-teacher oracle funcional

Sin entrenar nada, reemplazar sólo diagnósticamente la eligibility por:

h
t
NC
	​

=1[m
t
	​

>3.0545].

Mantener exactamente los RCSEP-A scores y HARDPTR.

Evaluar los 992 naturales + reverse de los cinco seeds.

Si A pasa y RCSEP-A ya tiene G
F
	​

,G
A
	​

>0, mi predicción es:

992/992
	​


en todo para 5/5.

Esto responderá una pregunta decisiva:

¿Stage B sigue aportando alguna capacidad necesaria, o sólo está destilando imperfectamente una decisión que el numeric teacher ya puede tomar exactamente?

Por qué no ir a fresh todavía

Porque si abrimos 6901 ahora y falla por un ARG con:

teacher=0.999,gate=0.49,

no aprenderíamos nada nuevo. Ya conocemos ese defecto en development.

Una batería fresh debe gastarse sólo cuando:

Stage A invariant cerrado + eligibility invariant cerrado
	​


y ahora sólo tenemos el primero.

Qué corrección espero después del audit

No la ejecutes todavía, pero dejo las ramas predeclaradas.

Si C1: optimization

Mantener:

soft teacher;

gate lineal;

loss actual.

Cambiar únicamente Stage B a un solver/receta que alcance el óptimo convexo de forma reproducible.

Si C2: calibration/objective

Mantener gate lineal y numeric-confidence, pero dejar de destilar la magnitud continua del teacher.

La siguiente candidata sería una eligibility pseudo-binaria derivada exclusivamente de NC:

y
^
	​

t
	​

=1[m
t
	​

>3.0545]

con un loss diseñado para garantizar el signo del logit.

Probablemente además balancearía positivos/negativos, porque hoy tenemos:

32 ARG:3 structure,

y los tres OP_X>0.5 son compatibles con esa asimetría. Pero no cambiaría target + weighting a la vez hasta que FIT-ALG nos diga cuál es necesario.

Si B falla: capacity

Entonces recién rediseñamos el gate.

Si D da 5/5 perfecto

Eso confirma que el teacher mismo es una eligibility funcional robusta y que todo el problema Stage B reside en su destilación.

Sobre el artifact_self_hash

No lo considero bloqueante aquí. El SHA256 real del archivo y los checkpoints están verificados, y cruzaste los campos sustantivos del JSON. Hay que conservar la nota de proceso, pero no cambia el diagnóstico científico.

Resolución

RCSEP-A: CLOSED / PASS_STRONG, causa 1 resuelta.

RCSEP-B-GATE: CLOSED / VALID FAIL, causa 2 aislada.

6901–6905: HOLD.

Siguiente unidad autorizada: RCSEP-B-FIT-ALG, cero training/cambios, con:

teacher hard sanity→linear separability→global optimum soft-BCE→direct-NC HARDPTR oracle.

Con ese resultado podremos modificar Stage B una sola vez y con atribución causal limpia, antes de gastar la batería fresh.