No abriría todavía 780x. El resultado reportado es un PASS sobre los casos ejecutados, pero encontré una discrepancia concreta en su generación que debemos resolver antes de confirmar el cierre protocolario de development. A/B/C permanecen cerrados y no necesitan repetirse.

Consulté el commit 60f8d863d13ea…, los resultados publicados y el código del evaluador. El commit registra los conteos que reportaste y los hashes de los cinco archivos de evidencia. No he accedido a los cases.jsonl locales ni repetido tu auditoría de 1.549.440 filas.

1. La discrepancia: el tercer argumento se genera con +2, no con +1

En la preparación de T7 habíamos fijado conservar la construcción de argumentos base de T6. Esa construcción distingue:

v
2
	​

=(seed+v
0
	​

+v
1
	​

+1)mod32,
v
3
	​

=(seed+v
0
	​

+v
1
	​

+v
2
	​

+2)mod32,

incrementando cada candidato hasta que sea distinto de los anteriores. El generador de T6 implementa precisamente:

Python
Run
offset = 1 if len(values) == 2 else 2
value = (seed + sum(values) + offset) % VALUE_COUNT

En cambio, el evaluador T7 recién ejecutado contiene:

Python
Run
def derived_value(seed: int, values: list[int]) -> int:
    candidate = (seed + sum(values) + 2) % VALUE_COUNT

value_assignments() utiliza esa función tanto para añadir el tercer argumento como para añadir el cuarto. Por tanto, aplica +2 en ambos pasos.

Ejemplo derivado directamente de ambas fórmulas

Para seed=7701, con los primeros valores (0,1):

Campo	Construcción acordada de T6	Evaluador T7 ejecutado
Tercer argumento	23	24
Cuarto argumento	15	16

En este ejemplo no hay colisiones que expliquen la diferencia. Es un cambio de regla.

Esto afecta las asignaciones de los subconjuntos de tamaño k≥3 y puede cambiar también D, porque el distractor depende de la suma de los valores activos. No cambia necesariamente ningún conteo. Se pueden obtener exactamente las mismas cantidades de filas con instrucciones diferentes.

2. Por qué tu auditoría y el checker podían pasar

Tu recálculo exhaustivo comprueba que las salidas son correctas para las filas efectivamente ejecutadas, y que sus parejas coinciden. Eso sigue siendo evidencia útil; no lo estoy cuestionando.

La cuestión adicional es si esas filas corresponden al procedimiento predeclarado.

El logical_checker() del evaluador llama al mismo build_cases_for_subset() que construye los casos. Después comprueba longitudes, cantidades, presencia de NOOP y reconstrucción de la pareja, pero no contrasta la fórmula de los argumentos derivados contra una implementación independiente de la regla acordada. Un cambio común al generador y al checker puede conservar todas esas propiedades.

Además, en el generador de preparación T7, evaluation_spec() registra los conteos y una descripción general, pero no explicita las dos fórmulas de v
2
	​

/v
3
	​

. Mi aceptación de aquel sellado no detectó esa omisión. El contrato conversacional pedía conservar T6, pero la especificación archivada no lo fijó con suficiente precisión.

Por eso no lo trataría como un fallo científico del binder. Lo separaría así:

Componente	Estado
Stage A, B y C	Sus PASS se conservan
Resultados sobre el conjunto realmente ejecutado	PASS observado, según tu auditoría y los resultados publicados
Conformidad de la selección de argumentos con el contrato previo	Discrepancia de implementación/especificación
Cierre completo de T7 development	Pendiente de resolver esa discrepancia
Preparación fresh 780x	HOLD
3. Siguiente unidad autorizada: corregir la evaluación, sin tocar modelos
T7-PAIRED-DEVELOPMENT-CONFORMANCE-REPAIR: AUTHORIZED
	​


No reentrenar A ni C, no recalibrar B, no cambiar seeds ni reemplazar checkpoints. La corrección se limita a reconstruir y evaluar el conjunto acordado.

Primero: hacer explícita la especificación

Crear un artefacto de corrección que referencie el commit actual y documente:

v
2
	​

=(seed+v
0
	​

+v
1
	​

+1)mod32,
v
3
	​

=(seed+v
0
	​

+v
1
	​

+v
2
	​

+2)mod32,
D=(seed+∑v
active
	​

+1)mod32,

con los incrementos módulo 32 ya establecidos para evitar coincidencias.

Conservar el mismo orden canónico de roles, permutaciones ARG→VALUE, asignaciones de operadores, subconjuntos y órdenes de cláusulas.

No sobrescribir los manifests ni los resultados anteriores. Registrar que se está haciendo explícita y aplicando la regla acordada, no seleccionando otra distribución a partir de los fallos o aciertos.

Segundo: checker independiente de la fórmula

El checker debe calcular por su cuenta los valores esperados y compararlos con los casos generados. No debe importar la función derived_value() del evaluador como su propia referencia.

Antes de ejecutar el modelo, verificar:

Las asignaciones exactas, no sólo que sus valores sean distintos.

La conversión mediante la permutación inversa VALUE→ARG.

El distractor D, constante entre órdenes de una misma asignación.

Las cláusulas completas, los identificadores de pareja, la unicidad y los conteos.

Añadir el ejemplo 7701, (0,1) → v2=23, v3=15 como regresión unitaria. El control exhaustivo debe cubrir igualmente todos los casos lógicos.

Tercero: reevaluación completa con el estado congelado

Si ese checker pasa, queda autorizada dentro de esta misma unidad la reevaluación completa:

298240 bases+1251200 aumentadas=1549440.

Utilizar exactamente los mismos cinco checkpoints finales de C, calibraciones B y decoder/codebook aprobados. Mantener sin cambios el forward, la selección NULL, el readout, el batching y los criterios de aceptación.

La salida debe ir a un directorio nuevo, por ejemplo:

campaign/t7_noop_none_lexical_paired_development_conformance/

Comparar los hashes del modelo, e
N
	​

, umbrales, runtime y manifests antes y después. No reutilizar los agregados del intento anterior como resultados de la corrida corregida.

Si vuelve a cerrar todo, entonces sí:

T7-NOOP-NONE-LEXICAL DEVELOPMENT: CLOSED / PASS
	​


y el siguiente paso será congelar la receta completa y preparar 7801–7805.

4. Sobre los archivos locales de evidencia

No es necesario subir los 3,3 GB a Git para completar esta corrección. Pero debe quedar precisa la distinción:

Los hashes identifican los archivos; no sustituyen su contenido para una auditoría independiente. Los resúmenes permiten comprobar lo que se reportó, pero no recalcular por sí solos las firmas semánticas y decisiones de cada caso.

Conservar los JSONL originales y los nuevos en ubicaciones separadas. Versionar el script de tu auditoría independiente y un resultado compacto que registre qué hashes de entrada auditó, cuántas líneas procesó y qué discrepancias encontró. Eso no exige repetir entrenamiento ni cambiar ningún criterio científico.

Una copia comprimida sin pérdida es compatible con esta preservación si, al descomprimirla, recupera los bytes y el SHA-256 del original. No borrar los archivos basándose únicamente en que sus hashes quedaron registrados.

La dirección inmediata no es otra hipótesis de arquitectura ni una batería fresh: es una reparación acotada de conformidad del generador de evaluación.

Los certificados de A/B/C hacen razonable esperar que también cierre el conjunto acordado, pero esa expectativa no reemplaza la corrida. Autorizo la corrección y reevaluación sobre los mismos modelos congelados; 780x permanece en HOLD hasta ese resultado.