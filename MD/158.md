Sí. **Corresponde congelar ya el piloto científico.** No veo ningún gate técnico adicional que justifique otro preflight antes de fijar el experimento.

La razón es explícita en el propio historial: `MD/157.md` estableció que, después de reparar causalidad/datos/AdamW, debía hacerse **una única repetición técnica GPU conforme** y que, una vez validado ese camino, correspondía **“congelar el piloto científico con sus seeds, datos y criterios de NLL, no seguir acumulando preflights de infraestructura.”**

Y `dcf8b040...` satisface precisamente esa condición que estaba pendiente: ejecución CUDA real, las tres variantes, 21/21 updates, calendario causal correcto, 2048 targets en todos los updates, estado procedente únicamente de la ventana 0 inmediatamente anterior, ocho documentos aprobados y configuración efectiva de AdamW/backend comprobada. Por tanto, el estado lógico pasa de:

```text
R1
  ↓
arquitectura validada
  ↓
training path validado
  ↓
carga nominal validada
  ↓
runtime recuperable
  ↓
CUDA real
  ↓
conformidad causal corregida
  ↓
CUDA conforme   ← dcf8b04
```

a:

```text
→ FREEZE DEL PILOTO CIENTÍFICO
→ ejecución
→ evaluación predeclarada
→ decisión sobre la hipótesis R1
```

No corresponde volver a arquitectura, T7/T8, hacer otro CUDA-preflight, cambiar R1 ni probar más infraestructura. Esa secuencia también está alineada con la dirección general de la spec: **núcleo lingüístico → piloto sin banco → memoria externa → cuantización/integración nativa**.

### Qué falta entonces

Falta **una unidad de freeze**, no otro experimento.

Yo abriría exactamente:

**`OMEGA-CORE-LM-0-R1-SCIENTIFIC-PILOT-FREEZE`**

Su trabajo sería puramente predeclarativo: fijar ahora, antes de ver resultados científicos, qué vamos a entrenar, medir y considerar PASS/FAIL.

Debe congelar al menos:

* **Arquitectura:** R1 sin modificaciones.
* **Variantes:** `shared_K1`, `shared_K4`, `untied_K4`.
* **Presupuesto:** 20,000 updates por corrida.
* **Replicación:** 5 seeds por variante, elegidas y registradas **ahora**, no después de observar resultados.
* **Batch efectivo:** 8.
* **Longitud:** 256 targets/update.
* **Microbatch físico:** 2 × 4 acumulaciones.
* **Datos:** splits publicados de WikiText-2 sin repartición propia, política de documentos/deduplicación fijada.
* **Teacher:** revisión concreta y congelada de DistilGPT2.
* **Loss:** `0.5 CE + 0.5 T² KL`, `T=2`.
* **AdamW:** `lr=3e-4`, `betas=(0.9,0.999)`, `eps=1e-8`, `weight_decay=0`.
* **Clipping:** norma 1.0 una vez por update.
* **BPTT/estado:** mismas reglas causales que acaba de demostrar `dcf8b04`.
* **FP32 inicial**, sin AMP, cuantización, `torch.compile`, memoria externa ni otras optimizaciones que mezclen variables.
* **Checkpoint/evaluation schedule:** fijado antes de iniciar.
* **Prohibición de seleccionar checkpoint usando el test.**
* **Stop/failure rules:** NaN, corrupción del ledger, desviación de receta, etc.

La spec ya fija además los tres comparadores como necesarios: K1 controla la ausencia de profundidad recurrente adicional; shared K4 prueba recurrencia con pesos compartidos; untied K4 controla profundidad usando más pesos únicos. La pregunta científica no es simplemente si K4 aprende lenguaje, sino si **las rondas compartidas aportan calidad frente a K1 sin necesitar los parámetros únicos de cuatro bloques independientes**.

### Los gates científicos deben quedar congelados ahora

Hay dos números particularmente importantes que ya estaban predeclarados y **no debemos modificar después de ver las curvas**:

$$
\Delta_{\text{depth}}
=
NLL_{\text{shared,K1}}
-
NLL_{\text{shared,K4}}
$$

con criterio:

$$
\boxed{\Delta_{\text{depth}}\ge0.05\;\text{nats/token}}
$$

y para dependencia del estado recurrente:

$$
\Delta_{\text{ablation}}
=
NLL_{\text{anchor-reset}}
-
NLL_{\text{normal}}
$$

con:

$$
\boxed{\Delta_{\text{ablation}}\ge0.01\;\text{nats/token}}
$$

con orientación positiva = **K4/estado recurrente realmente ayuda**. `state_shuffle` permanece diagnóstico secundario, no sustituto del gate causal.

También estaba fijado que deben existir **5 seeds y los 3 baselines precomprometidos antes de la campaña completa**, precisamente para evitar elegir seeds, cantidad de pasos o comparaciones después de conocer qué variante va ganando.

### Hay una cuestión que sí debemos resolver durante el freeze: el presupuesto

Esto no bloquea el freeze científico, pero sí debe bloquear **el lanzamiento ciego de toda la campaña**.

Los tiempos que acabamos de medir son aproximadamente:

```text
shared_K1     ~3.6 s/update
shared_K4     ~8.9 s/update
untied_K4     ~8.9 s/update
```

Para el contrato completo:

```text
3 variantes × 5 seeds × 20,000 updates
= 300,000 optimizer updates
```

Con esos tiempos de L4, el orden de magnitud sería aproximadamente:

```text
K1:
100,000 × 3.6 s ≈ 100 h

shared K4:
100,000 × 8.9 s ≈ 247 h

untied K4:
100,000 × 8.9 s ≈ 247 h

TOTAL ≈ 594 GPU-h
```

A `$0.49/h` serían aproximadamente **$291 de compute**, antes de evaluación, preparación y almacenamiento, y unas **24–25 jornadas de GPU en ejecución serial**.

Esto no invalida R1 en absoluto. Es justamente una de las cosas que el preflight debía revelar: el camino es técnicamente viable, pero una replicación científica completa con el runner actual **no es un experimento de $5–20**.

Por ello no congelaría simplemente «L4 + $291». En el freeze separaría:

```text
CONTRATO CIENTÍFICO
    inmutable

de

PLATAFORMA DE EJECUCIÓN
    puede elegirse antes de comenzar
    buscando menor costo por update
    sin alterar la receta científica
```

Eso evita cometer otro error metodológico: modificar el modelo para hacerlo más barato antes de saber si la hipótesis funciona.

### Mi decisión formal

Yo cerraría ahora el estado así:

```text
OMEGA-CORE-LM-0-R1-CUDA-PREFLIGHT-CONFORMANT
= CLOSED / PASS

cuda_execution_validated = true
causal_training_conformant = true
nominal_batch_contract_validated = true
runtime_recovery_validated = true

architecture_changes_required = false
additional_cuda_preflight_required = false
additional_infrastructure_research_required = false

scientific_pilot_started = false

NEXT:
OMEGA-CORE-LM-0-R1-SCIENTIFIC-PILOT-FREEZE
```

Y **después del freeze**, no antes, pasamos al entrenamiento científico.

La única corrección operativa que incluiría en ese mismo freeze es generar una **identidad inmutable del entorno que realmente ejecutará la campaña** —commit/código, dependencias, dataset/teacher revisions y preferiblemente una imagen/digest que ya incorpore `a226489`/`dcf8b04`. `MD/157` advertía específicamente que una imagen antigua no incorpora mágicamente las correcciones del runner. Eso es empaquetado del piloto, **no otro gate experimental**.

Así que la respuesta corta es: **sí, ya llegamos al punto que llevábamos varios gates intentando alcanzar. Ahora se congela el experimento científico. No haría otro preflight.**
