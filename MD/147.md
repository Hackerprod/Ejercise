No autorizo todavía el piloto de 20.000 pasos. Las pruebas sintéticas son válidas, pero el diseño necesita dos correcciones arquitectónicas y una corrección del criterio experimental antes de entrenar.

Revisé el commit d151c4ab0609…, la implementación de OmegaCoreLM0, el generador del artefacto y los componentes reutilizados de T1. No he reejecutado tus 18 pruebas ni recalculado sus hashes. Los problemas que señalo se deducen del código y de la especificación publicados; no contradicen que esas pruebas hayan pasado.

La siguiente unidad autorizada será:

OMEGA-CORE-LM-0-R1-ARCHITECTURE-PREFLIGHT
	​


No es volver a T7 ni cambiar de dirección. Es corregir el primer estudiante lingüístico para que realmente pruebe la hipótesis que acabamos de recuperar de Conversacion.md.

1. El workspace es matricial dentro del token, pero su memoria entre tokens vuelve a ser vectorial

El punto más importante está en:

Python
Run
summary = previous_state.mean(dim=1)
anchor = self.prelude_norm(
    self.prelude(torch.cat((token_state, summary), dim=-1))
)

Después, update_step() comienza con state = anchor. No existe otra ruta desde el workspace anterior hacia el siguiente token.

Eso implica:

S
t−1
	​

∈R
8×128
⟶
S
ˉ
t−1
	​

∈R
128
⟶A
t
	​

∈R
8×128
.

Se transmiten 1.024 componentes por la interfaz, pero el siguiente token sólo consume 128 combinaciones de ellos.

No es una sospecha sobre el entrenamiento. Es una invariancia de la arquitectura. Para cualquier perturbación Δ cuya media entre slots sea cero:

i=1
∑
8
	​

Δ
i
	​

=0,

se cumple matemáticamente:

Prelude(x,S+Δ)=Prelude(x,S).

Por ejemplo, añadir un vector u al slot 0 y restarlo del slot 1 no cambia lo que recibe el siguiente token. El espacio de perturbaciones descartadas tiene dimensión:

(8−1)⋅128=896.

Esto no significa que el modelo no pueda aprender lenguaje. Significa que estamos proponiendo una memoria temporal de 128 dimensiones con un workspace expandido durante el cómputo de cada token, no un workspace matricial íntegramente persistente.

El test previous_state_is_consumed sólo demuestra sensibilidad a algún cambio del estado previo. No comprueba que sobrevivan las diferencias entre slots que dejan intacta su media. El código de esa prueba utiliza un estado aleatorio completo, que normalmente también modifica la media.

Corrección mínima que autorizo

Conservar el Prelude actual como propuesta de escritura, pero añadir un camino residual para el estado completo:

U
t
	​

=W
p
	​

[E[x
t
	​

];mean(S
t−1
	​

)]+b
p
	​

,
A
t
	​

=reshape(RMSNorm
md
	​

[vec(S
t−1
	​

)+U
t
	​

]).
	​


Después:

S
t
(0)
	​

=A
t
	​

,

y el bloque recurrente continúa como está definido.

En código, la diferencia esencial sería:

Python
Run
write = self.prelude(torch.cat((token_state, summary), dim=-1))
anchor = self.prelude_norm(previous_state.flatten(start_dim=1) + write)

Esto conserva las dimensiones y no añade parámetros. La media sigue siendo una señal auxiliar para decidir la escritura, pero deja de ser el único canal temporal.

No garantiza buena memoria a largo plazo: la normalización y las actualizaciones todavía pueden degradarla. Lo que elimina es la pérdida obligatoria, por construcción, de toda diferencia entre slots con media cero.

2. La cabeza de salida está desproporcionada respecto al experimento

La implementación utiliza:

Python
Run
self.head = nn.Linear(slots * dimension, vocab_size)

Con md=1024 y V=50257, la cabeza contiene:

P
head
	​

=1024⋅50257+50257=
51513425
	​

.

El bloque recurrente tiene 197.888 parámetros. Por tanto, a partir de los conteos publicados:

Componente de shared_K4	Parámetros	Fracción del total
Bloque recurrente	197.888	0,34 %
Cabeza de salida	51.513.425	88,19 %
Modelo completo	58.410.449	100 %

La cabeza sola ocupa aproximadamente 196,51 MiB en FP32. Son cálculos sobre las dimensiones del código, no mediciones de tráfico DRAM.

Esto no invalida matemáticamente la comparación K1/K4. Sin embargo, introduce una gran capacidad común de salida y un coste que puede dominar el sistema que pretendemos estudiar.

Mi instrucción de conservar el vocabulario completo no obligaba a proyectar directamente los 1.024 componentes del workspace hacia las 50.257 palabras. Tampoco exige implementar ahora vocabulario selectivo.

Corrección propuesta: readout compacto y pesos de entrada/salida compartidos

Mantener la normalización del workspace aplanado, añadir una proyección:

u
t
	​

=W
o
	​

RMSNorm
md
	​

(vecS
t
(K)
	​

),W
o
	​

∈R
d×md
,

y producir los logits usando la misma matriz de embeddings del estudiante:

z
t
	​

=
d
	​

Eu
t
	​

	​

,E∈R
V×d
.
	​


Para esta primera revisión, fijaría W_o sin bias y sin bias adicional de vocabulario. La escala d
−1/2
 queda explícita y es común a las tres variantes.

Se evalúan todos los tokens del vocabulario: no hay retrieval, selección aproximada ni shortlist.

La nueva proyección añade:

md
2
=131072

parámetros y elimina la matriz de salida independiente de 51,5 millones. Manteniendo intacto el resto de la implementación, los totales calculados serían:

Variante	Parámetros esperados
shared_K1	7.027.328
shared_K4	7.028.096
untied_K4	7.621.760

shared_K4 quedaría en 28.112.384 bytes de parámetros FP32, aproximadamente 26,81 MiB.

Estos números deben verificarse mediante la nueva clase real. No copiarlos como si ya fueran resultados ejecutados.

Esta modificación reduce la capacidad de la cabeza y no es funcionalmente equivalente a la anterior. Por eso se registra como R1 y se aplica a los tres baselines. Es una decisión arquitectónica previa al entrenamiento, no una optimización gratuita ni un arreglo posterior para mejorar resultados.

El shell seguirá siendo mayor que el core. Eso se reportará; no vamos a llamar al sistema completo “modelo de 197.888 parámetros”.

3. El criterio de ablación tiene el signo invertido

El artefacto contiene:

normal minus anchor_reset test NLL >= 0.01

Eso exigiría que la ejecución normal tenga mayor pérdida que la ablación. Es lo contrario de la hipótesis que se quiere comprobar. La frase está tanto en el artefacto como en su generador.

La definición correcta será:

Δ
ablation
	​

=NLL
anchor_reset
	​

−NLL
normal
	​

,
Δ
ablation
	​

≥0.01 nats/token
	​

.

Y para profundidad:

Δ
depth
	​

=NLL
shared,K=1
	​

−NLL
shared,K=4
	​

,
Δ
depth
	​

≥0.05 nats/token
	​

.

Las diferencias positivas significan que la variante normal o más profunda mejora. Los intervalos de confianza se calculan con esa misma orientación.

Conservaría state_shuffle como diagnóstico secundario: corromper el estado con el de otra secuencia puede empeorar el resultado, pero eso no demuestra por sí solo que cuatro rondas sean mejores que una.

También debe fijarse la comparación de longitudes sobre los mismos tokens objetivo, variando el contexto disponible. Comparar la NLL de los primeros 32 tokens con la de otros 512 mezcla dificultad de los ejemplos con capacidad de memoria.

4. Maestro y corpus son candidatos razonables, pero su contrato aún no está cerrado

Mantendría provisionalmente DistilGPT2 y WikiText-2 raw-v1 para el piloto pequeño. No cambiaría otra vez el objetivo hacia tareas simbólicas.

Verifiqué las fuentes públicas para resolver tres puntos de la propuesta:

DistilGPT2. La distribución oficial distilbert/distilgpt2 declara Apache-2.0, no MIT. Su configuración especifica vocabulario de 50.257 tokens y contexto posicional de 1.024. El freeze debe utilizar una revisión concreta, no simplemente main. 
Hugging Face
+1

WikiText-2. Ya dispone de splits train, validation y test. Para este piloto elegiría conservar esos splits, reconstruyendo artículos dentro de cada uno cuando haga falta, en vez de mezclar todo y volver a repartir 80/10/10. Las filas del dataset no deben tratarse automáticamente como documentos independientes. 
Hugging Face

Licencia del dataset. La fuente consultada contiene una discrepancia: sus metadatos enumeran CC BY-SA 3.0 y GFDL, mientras que el texto de la ficha menciona CC BY-SA 4.0. El registro debe conservar la metadata de la revisión utilizada y señalar esa discrepancia, no dar por verificado un único valor. 
Hugging Face

La deduplicación debe tener una política determinista y documentada. Normalizar espacios para calcular un hash no implica modificar silenciosamente el texto que se entrenará o evaluará.

Además, “test no visto” se referirá al entrenamiento de este estudiante. No afirmaremos sin evidencia que esos documentos nunca aparecieron en los datos de preentrenamiento del maestro.

Estado persistente y BPTT

El protocolo debe fijar expresamente:

El estado se reinicia al comenzar un documento o cambiar la secuencia asignada a una posición del batch.

Entre ventanas contiguas del mismo documento se conserva su valor, cortando el grafo en la frontera de BPTT.

No se arrastra estado de un documento a otro al barajar ejemplos.

Padding y posiciones sin target válido no contribuyen a la pérdida ni actualizan inadvertidamente el estado de una secuencia finalizada.

El maestro tiene un contexto máximo de 1.024 posiciones; por tanto, debe definirse cómo se obtienen sus logits causales en documentos más largos. “Mismo prefijo” no puede significar un prefijo ilimitado para ese modelo. 
Hugging Face

La pérdida propuesta puede mantenerse:

L=0.5CE+0.5T
2
KL(p
T
	​

∥p
S
	​

),T=2.

Su implementación debe sumar KL sobre el vocabulario y promediar sobre tokens válidos, con el desplazamiento correcto entre entrada x
t
	​

 y target x
t+1
	​

. El maestro permanece congelado y no se usa en inferencia.

5. El presupuesto de entrenamiento aún necesita una comprobación técnica

Los 233 MB de parámetros del diseño original no demostraban que todo el entrenamiento cupiera en 16 GiB. Tampoco los aproximadamente 28 MB de R1 demostrarán eso.

Sólo un tensor de logits FP32 con la propuesta B=8,L=256,V=50257 ocupa:

8⋅256⋅50257⋅4=411705344 bytes≈392.63 MiB.

Durante entrenamiento se suman otros tensores del estudiante, del maestro, de la pérdida, del backward y del optimizador. No afirmo que excederá el límite; afirmo que el conteo de parámetros no lo resuelve.

También:

20000⋅8⋅256=40960000

posiciones por variante, antes de descontar padding. Eso es un presupuesto lingüístico sustantivo, no el equivalente de las pequeñas campañas atómicas de T7.

La receta aún debe fijar optimizador, LR, calendario, weight decay, clipping, inicialización, seeds y dispositivo efectivo. cpu_threads=8 no responde dónde se ejecutará el entrenamiento ni cuánto costará cada actualización. El artefacto actual no especifica todos esos elementos.

No autorizaría 20.000 pasos sobre la laptop por omisión del campo de dispositivo. La dirección sigue siendo entrenamiento lingüístico en GPU cuando esté disponible y validación CPU separada; cualquier ejecución CPU del piloto debe tener primero su coste medido.

Las cinco semillas y los tres baselines deben quedar precomprometidos antes de la campaña completa. El presupuesto final se decidirá antes de observar el test, no aumentando pasos cuando una variante quede por detrás.

6. Unidad concreta autorizada
OMEGA-CORE-LM-0-R1-ARCHITECTURE-PREFLIGHT: AUTHORIZED
	​


No autoriza todavía entrenamiento real, descarga de pesos/corpus ni generación de trazas del maestro. Autoriza una revisión delimitada del prototipo y sus pruebas sintéticas.

Cambios de código autorizados

Implementar el camino residual del workspace completo en Prelude y el readout compacto con la matriz de embeddings compartida. Mantener d=128,m=8, el WorkspaceUpdateBlock existente, los gates por profundidad y las tres variantes K1/K4/untied.

No añadir banco externo, halting, cuantización, nuevas tareas o dependencias de T7. No reentrenar ni cargar checkpoints históricos.

Guardar R1 y sus resultados en rutas nuevas, conservando el prototipo anterior como versión de diseño.

Pruebas que debe añadir R1

Persistencia entre tokens. Construir pares de estados con la misma media pero diferencias entre slots —por ejemplo, perturbaciones opuestas en dos filas— y comprobar que el nuevo Prelude puede distinguirlos. Los fixtures deben verificar primero que realmente mantienen la media. Esta prueba no pretende demostrar que toda pareja de estados sea distinguible.

Compartición de entrada/salida. Demostrar que el readout usa la matriz embedding.weight real, no una copia independiente. Verificar conteos únicos, forma de logits y backward por las dos rutas.

Causalidad y continuidad. Conservar las pruebas de tokens futuros, recurrencia entre rondas, identidad shared y bloques untied. Añadir equivalencia entre procesar una secuencia de una vez y hacerlo en ventanas contiguas conservando el estado, sin actualizaciones de pesos entre ventanas.

Reset, padding y BPTT. Verificar con datos sintéticos que no se transmite estado entre documentos, que el padding no altera estados válidos y que cortar el grafo en una frontera no reinicia el valor del estado.

Signo de las métricas. Incluir un ejemplo numérico donde NLL_normal=3.0 y NLL_anchor_reset=3.1 produzca una mejora de +0.1, no −0.1.

Se permite backward sobre fixtures sintéticos para comprobar conexiones. No ejecutar optimizer.step() en esta unidad.

Una corrección pequeña del validador

El generador del artefacto hace:

Python
Run
tests.get("checks_passed") != tests.get("checks_total")

pero esos dos contadores no se guardan en el JSON producido por el script sintético; se imprimen en stdout. En ese caso la comparación puede ser None != None, que no detecta campos ausentes. Eso no desmiente tus 18 pruebas ejecutadas, pero el guard no las está validando como pretende.

R1 debe validar el diccionario real checks, exigir que no esté vacío y que todos los resultados requeridos sean True. Añadir una prueba negativa: un check falso con status="passed" debe ser rechazado.

7. Entrega y decisión siguiente

La entrega será una única revisión integrada con el código R1, conteos medidos desde los objetos reales, pruebas sintéticas, criterio NLL corregido y contrato de datos/estado actualizado.

El resultado actual se conserva como prototipo causal con pruebas sintéticas aprobadas. No lo reclasifico como fallo de aprendizaje: todavía no se ha entrenado. Lo que no ratifico es la conclusión de que el diseño ya está listo para lanzar el piloto completo.

Después de R1, el paso será un preflight técnico del entrenamiento —carga del maestro, pérdida real, backward y memoria/tiempo por actualización— antes de comprometer la campaña de 20.000 pasos. No otra ampliación de tareas de binding.

La dirección sigue siendo construir el núcleo lingüístico de OMEGA. Pero debe conservar su workspace entre tokens, utilizar una cabeza proporcionada al piloto y evaluar la recurrencia con el signo correcto. Autorizo esa revisión R1; el entrenamiento real permanece en HOLD.