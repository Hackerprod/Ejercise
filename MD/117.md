Confirmo el cierre documental. El commit 8525a2973ce7... contiene el artifact final con self-hash c82c2630... y, crucialmente, ya referencia el run RCSEP3-BG correcto de development (campaign/t4_nobypass2_rcsep3_bg/...) y sus checkpoints 710x, no el RCSEP-3 puro fallido.

T4-NOBYPASS: CLOSED / PASS_STRONG / HALT
	​


710x queda development/diagnostic permanente y 720x queda evidencia fresh sellada. Ninguno debe usarse para diseñar T5; para equivalencias de implementación usaremos únicamente 710x.

También autorizo:

T5-NOBYPASS-NROLE-DESIGN-AUDIT: AUTHORIZED
	​


Cero training. Cero manifests nuevos. Cero checkpoints nuevos.

Objetivo de T5

La pregunta cambia de “¿funcionan tres roles?” a:

¿Puede expresarse la solución de T4 como una arquitectura y un objective realmente parametrizados por N, de los cuales T4 sea exactamente el caso N=3?

La forma que quiero auditar es:

Q∈R
N×d

en vez de q_F/q_A/q_M hardcodeados.

Para cada role r:

s
r,t
	​

=Q
r
⊤
	​

k
t
	​

/4
j
r
	​

=arg
t
max
	​

s
r,t
	​

r
r
	​

=W
v
	​

(e
j
r
	​

	​

).

W_v sigue único y compartido.

RCSEP-N + background

Generalizar exactamente:

L
r←r
′
	​

=mean
i,j
	​

softplus[s
r
	​

(OP
r
′
	​

,ARG
j
	​

)−s
r
	​

(OP
r
	​

,ARG
i
	​

)]

para todos:

r
′

=r.

Y:

L
r←BG
	​

=mean
i,z∈S
N
	​

	​

softplus[s
r
	​

(z)−s
r
	​

(OP
r
	​

,ARG
i
	​

)].

La loss genérica debe ser:

L
sep
N+BG
	​

=
N
2
1
	​

	​

r
∑
	​

r
′

=r
∑
	​

L
r←r
′
	​

+
r
∑
	​

L
r←BG
	​

	​

	​


porque:

N(N−1)+N=N
2
.

Para N=3:

N
2
=9

y debe reducirse exactamente a RCSEP3-BG.

Background genérico

Con la gramática actual de cláusulas separadas por LINK:

∣S
N
	​

∣=N+32+N=
32+2N
	​

.

Es decir:

START→OP_r: N;

ARG_i→LINK: 32;

LINK→OP_r: N.

Para N=3:

32+6=38,

exactamente T4.

Para un futuro N=4:

40
	​


contextos BG.

Esto debe salir de N, no de constantes 3 o 38.

Gate principal del design audit: N=3 equivalence

Implementá una versión genérica sin entrenarla y cargá únicamente un checkpoint development RCSEP3-BG 710x.

Mapeá:

Q
0
	​

=q
F
	​

,Q
1
	​

=q
A
	​

,Q
2
	​

=q
M
	​

.

Con exactamente el mismo orden de términos que T4, exigí equivalencia entre la implementación específica y la genérica para:

todos los k_t;

score tensors de los tres roles;

pointer indices;

r
F
	​

,r
A
	​

,r
M
	​

;

decoded indices;

los 6 términos role→role individuales;

los 3 términos role→BG individuales;

L_sep;

background context list;

HARDPTR outputs.

Quiero bit-exact donde se mantenga el mismo orden de operaciones. Si algún agregado flotante no puede ser bit-exact únicamente por orden de reducción, documentar el motivo y exigir una tolerancia numérica explícita mínima; no usar una tolerancia amplia por defecto.

No usar 720x para esto.

Audit de hardcoding

La implementación genérica debe demostrar que no contiene ramas como:

if role == FLOOR
if role == AVOID
if role == MATCH

en:

query scoring;

RCSEP;

BG generation;

HARDPTR;

reference decoding.

La semántica particular puede vivir en metadata del role, pero el binder debe operar sobre índices 0..N-1.

Quiero una estructura conceptual tipo:

roles = [...]
operators = [...]
queries = Q[N,d]

no tres caminos copiados.

Escalamiento computacional

El audit debe calcular el número de comparaciones contrastivas por update.

Role-vs-role:

32
2
N(N−1).

Role-vs-BG:

32N(32+2N).

Total:

1024N(N−1)+32N(32+2N)

que simplifica a:

1088N
2
	​

.

Por tanto:

N=3⇒9792

comparaciones,

N=4⇒17408.

Quiero esto reportado porque T5 debe seguir siendo compatible con la filosofía CPU-native; para N=4 el coste continúa siendo pequeño.

Cuarto rol para el futuro N=4

No extendería C1.

Para T5 la variable científica es binding N-role, no semántica del executor.

Propondría provisionalmente un cuarto role activo ANCHOR/REFERENCE, cuya condición primaria sea simplemente:

decode(r
H
	​

)=H.

Es un role de referencia activo y distinto en addressing, pero no se presenta como nuevo primitive C1.

El design audit debe dejar explícito que el eventual claim N=4 sería sobre:

four simultaneous operator→argument bindings,

no sobre cuatro operaciones nativas del executor.

Tampoco implementes todavía los manifests N=4; sólo valida que el role registry pueda representar algo como:

{FLOOR,AVOID,MATCH,ANCHOR}

sin introducir código específico para el cuarto role.

Output esperado

Un artifact T5-NROLE-DESIGN-AUDIT debería contener, como mínimo:

fórmula genérica de query bank;

fórmula L
sep
N+BG
	​

;

prueba de N
2
 términos;

prueba de 32+2N BG contexts;

equivalencia N=3 contra 710x;

audit anti-hardcoding;

parameter count en función de N;

coste contrastivo 1088N
2
;

diseño provisional N=4;

claim/non-claim de esa futura prueba.

Regla

Si N=3 no reproduce T4, HALT. No diseñar N=4 alrededor de una generalización que ni siquiera conserva el caso conocido.

Si reproduce T4:

T5-NROLE-DESIGN-AUDIT: PASS
	​


y la siguiente unidad será diseñar/sellar development N=4 con seeds completamente nuevos —probablemente 7301–7305— antes de cualquier training.

Autorización explícita: iniciar T5-NOBYPASS-NROLE-DESIGN-AUDIT ahora, usando sólo T4 development/710x para la equivalencia y sin tocar 720x.