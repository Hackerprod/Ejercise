--- mrdl/language.py
+++ mrdl/language.py
@@ -413,4 +413,5 @@
     clean_empty_fraction: float
     controller_updates: int
     promotion_store_connected: bool
+    target_missing_controller_skips: int = 0
     fallback_counts: tuple[tuple[str, int], ...] = ()
@@ -475,3 +475,4 @@
         self._causal_rejected = 0
         self._causal_probe_count = 0
+        self.target_missing_controller_skips = 0
         self._training_stories: tuple[tuple[str, ...], ...] = ()
@@ -1000,3 +1000,7 @@
                     records_by_id = {record.record_id: record for record in self.edge_memory.lane_records()}
                 prediction = self.predict(context)
+                if target not in prediction.candidates:
+                    self.target_missing_controller_skips += 1
+                    context.append(target)
+                    continue
                 active_records = tuple(records_by_id[record_id] for record_id in prediction.active_record_ids if record_id in records_by_id and records_by_id[record_id].level is MemoryLevel.M2)
@@ -1093,4 +1093,5 @@
             clean_empty_fraction=empty / max(1, len(health)),
             controller_updates=self.controller_updates,
             promotion_store_connected=True,
+            target_missing_controller_skips=self.target_missing_controller_skips,
             fallback_counts=tuple(sorted(fallback_counts.items())),
--- tests/test_language_pipeline.py
+++ tests/test_language_pipeline.py
@@ -1,6 +1,6 @@
 import unittest
 from pathlib import Path
 from unittest.mock import patch
-from mrdl.language import EmbeddingTable, MRDLLanguagePipeline, PropagationConfig, SparseController, load_tinystories, TrigramBaseline
+from mrdl.language import EmbeddingTable, MRDLLanguagePipeline, Prediction, PropagationConfig, SparseController, load_tinystories, TrigramBaseline
 from mrdl.evidence import MemoryLevel
 from mrdl.lanes import FrozenOperator, LaneRecord
@@ -19,2 +19,13 @@
     def test_m1_cannot_update_controller(self):
         stories=load_tinystories('/root/mrdl/data/TinyStories-valid.txt',2,300); tokens={t for s in stories for t in s}; pipe=MRDLLanguagePipeline(EmbeddingTable.random_frozen(tokens,8,1),8); pipe.observe_training(stories); records=pipe.edge_memory.lane_records(); self.assertEqual(0,pipe.controller.sparse_backprop(records,1.0)); self.assertEqual(0,pipe.controller_updates)
+
+    def test_missing_target_skips_controller_update_and_counts_skip(self):
+        stories = [["<bos>", "target"]]
+        tokens = {token for story in stories for token in story}
+        pipe = MRDLLanguagePipeline(EmbeddingTable.random_frozen(tokens, 8, 41), 4)
+        prediction = Prediction("other", {"other": 1.0}, ("other",), 1.0, False)
+        with patch.object(pipe, "predict", return_value=prediction):
+            updates = pipe.train_controller(stories, max_steps=1)
+        self.assertEqual(0, updates)
+        self.assertEqual(0, pipe.controller_updates)
+        self.assertEqual(1, pipe.target_missing_controller_skips)
