Confirmado.

T2-NOBYPASS-1-STAGED-HARDPTR: DEVELOPMENT CLOSED / PASS_STRONG
	​


Los tres pasos pasaron sobre el mismo ensamblado e6408c20..., sin tuning ni cambios de pesos:

HARDPTR-NBF0: PASS_STRONG
HARDPTR-NB3-CANON: PASS_STRONG
HARDPTR-NB4: PASS_STRONG

Esto ya autoriza congelar formalmente la versión HARDPTR y desellar NB5.

Qué queda congelado

Desde este punto no se cambia, mirando NB5:

arquitectura RELKEY [previous,current];

arquitectura KV y dimensiones;

HARDPTR;

gate lexical lineal de 17 parámetros;

Stage-B NUMERIC-CONFIDENCE teacher;

alpha=2, tau=3.0545;

threshold HARDPTR c>0.5;

tie policy first/min-position;

optimizer, losses, schedules, batches y número de updates de Stage A/B;

decoder/ISA;

canonicalization;

gamma=2.5 permanece en config aunque sea funcionalmente irrelevante para argmax.

Si NB5 falla, NB5 queda fallado. No se ajusta nada usando sus resultados y se vuelve a llamar “fresh”.

NB5: fresh lexical cipher

La prueba que autorizaría es deliberadamente distinta de los 992 development actuales.

El ISA numérico de 32 valores permanece igual, pero todo lo que identifica lexicalmente los argumentos y operadores cambia.

Para cada dominio fresco d:

ARG
0
(d)
	​

,…,ARG
31
(d)
	​


dos operadores opacos:

OP
X
(d)
	​

,OP
Y
(d)
	​


y un linker nuevo:

LINK
(d)
.

Generar una permutación aleatoria sellada:

π
d
	​

:S
32
	​


tal que:

ARG
i
(d)
	​

↦VALUE
π
d
	​

(i)
	​

.

Así ARG_07, por ejemplo, no significa VALUE_7.

También randomizar con el mismo manifest cuál operador representa FLOOR:

OP
X
	​

↔FLOOR

o

OP
Y
	​

↔FLOOR.

Eso elimina otro posible shortcut basado en identidad del operador.

Yo además randomizaría los token IDs físicos de los 35 tokens relevantes dentro de cada dominio. El modelo sólo ve IDs/embeddings; no debe existir una correlación accidental entre “los ARG están en estos IDs” y su semántica.

Muy importante: pesos nuevos

NB5 no reutiliza los pesos e6408c20....

Se reutilizan:

arquitectura + receta + hiperpar
a
ˊ
metros
	​


pero se inicializa un modelo fresco.

Si copiáramos los embeddings/pesos antiguos a los nuevos tokens, ya no sería una prueba limpia de que el procedimiento aprende la primitive composicional.

El frozen numeric ISA/codebook sí se conserva porque NB5 mide:

¿puede la arquitectura volver a aprender operator→argument binding y luego componerlo?

No mide generalización a una ISA numérica nueva. Eso sería otra prueba posterior.

Training permitido

Stage A debe ser exactamente la receta ya cerrada de R1.3, sustituyendo únicamente vocabulario/mapeo del manifest.

Sólo:

atomic + NOOP

Nunca una secuencia que contenga simultáneamente los dos roles.

Por ejemplo, conceptualmente:

OP
X
	​

 ARG
i
	​


o

OP
Y
	​

 ARG
j
	​

.

La supervisión puede conocer:

π(i)

para construir el target de entrenamiento; el model forward no.

Stage B se ejecuta exactamente como quedó cerrado:

congelar Stage A;

calcular numeric-confidence mediante el frozen decoder;

teacher fijo

y
t
	​

=σ(2(m
t
	​

−3.0545));

entrenar únicamente los 17 parámetros del gate;

cero is_VALUE labels;

cero ejemplos joint.

Y después HARDPTR:

h
t
	​

=1[c
t
	​

>0.5]
j
r
	​

=arg
h
t
	​

=1
max
	​

s
r,t
	​

r
r
	​

=W
v
	​

(e
j
r
	​

	​

).

Nada se recalibra con el dominio fresco.

Especialmente NO recalcular:

τ

a partir de los márgenes del nuevo dominio.

Si 3.0545 no transfiere, eso es un resultado de NB5.

Test realmente fresh

Después de terminar Stage A y B, recién abrir el joint test.

Para cada L

=F:

32×31=
992
	​


combinaciones.

Natural:

OP
F
	​

 ARG
L
	​

 LINK OP
A
	​

 ARG
F
	​


y, crucialmente, también reverse exhaustivo, no sólo 139:

OP
A
	​

 ARG
F
	​

 LINK OP
F
	​

 ARG
L
	​

.

Por tanto cada fresh domain evalúa:

992+992=1984

joint examples que nunca aparecieron durante training.

No usaría un solo fresh cipher

Aquí aplicaría replicación real.

Predeclararía cinco dominios completos antes de correr el primero:

seeds=6801,6802,6803,6804,6805
	​


Cada seed determina de manera reproducible:

permutación π
d
	​

;

swap de operadores;

asignación de token IDs;

init del modelo;

manifest train/test.

Los cinco manifests y hashes deben escribirse antes de entrenar 6801. Así no podemos escoger después ciphers “bonitos”.

Podemos ejecutarlos secuencialmente:

NB5-P=6801

y, si pasa:

NB5-R=6802…6805.

Pero los cinco quedan precomprometidos desde el comienzo.

Controles de contaminación

Antes de cada training, el artifact debe demostrar:

joint_train_examples=0

y que el forward sigue siendo solamente:

(token_ids,lengths).

La permutación π
d
	​

, los valores objetivo y los labels FLOOR/AVOID pueden existir en data-loader/evaluator, pero nunca en forward.

Stage B debe además confirmar:

teacher_uses_numeric_confidence=true
teacher_uses_isVALUE=false

y en inferencia:

teacher_invoked=false.

El test manifest joint no debe ser leído por ninguna etapa de entrenamiento.

Contrato NB5

Exigiría estas métricas por cada fresh domain:

Gate	Natural	Reverse
gate classifies 32 ARG eligible + 3 structural ineligible	35/35	mismo vocab
strict FLOOR pointer margin >0	992/992	992/992
strict AVOID pointer margin >0	992/992	992/992
pointer FLOOR correcto	992/992	992/992
pointer AVOID correcto	992/992	992/992
decode FLOOR correcto	992/992	992/992
decode AVOID correcto	992/992	992/992
RAW execution	992/992	992/992
CANON execution	992/992	992/992
RAW ≡ CANON	992/992	992/992

Los strict margins son importantes. No quiero que un PASS dependa del tie-break first-position.

También repetiría los guardrails:

canonicalization_uses_decoded_indices_only=true
ground_truth_absent=true.
Criterio de aceptación

Para el NB5 primario:

seed 6801 debe dar exactitud completa en los 1984 joint
	​


sin modificar absolutamente nada.

Si falla una fila:

NB5-P: VALID FAIL
	​


Se sellan los resultados. No se tunea y se vuelve a ejecutar 6801.

Si 6801 pasa, correr 6802–6805 exactamente igual.

Yo reservaría la etiqueta:

T2-NOBYPASS: PASS_STRONG
	​


para:

5/5 fresh domains
	​


cada uno con:

992/992 natural

y:

992/992 reverse

en pointers, decode y ejecución.

Eso supone:

5×1984=
9920
	​


joint cases frescos, ninguno visto durante training como composición.

Qué demostraría un PASS

Sería bastante más fuerte que NB4.

No sería simplemente:

“memoriza todas las parejas”.

Cada modelo sólo habría aprendido atomicamente:

OP→role
ARG
i
	​

→VALUE
π(i)
	​


y eligibility numérica.

Después tendría que producir correctamente las 992 combinaciones de dos argumentos y ambos órdenes, pese a:

nueva asignación lexical;

nueva permutación valor↔token;

nuevos operadores;

nuevos token IDs;

fresh initialization;

cero joint training.

Entonces sí podríamos decir, con bastante fundamento:

la primitive operator→argument aprendida por la arquitectura compone fuera del dominio usado para dise
n
˜
arla
	​


No equivaldría todavía a “razonamiento general”. Sería evidencia fuerte y controlada de composición/binding sistemático en un dominio fresco.

Resolución

HARDPTR-NBF0: CLOSED / PASS_STRONG
HARDPTR-NB3-CANON: CLOSED / PASS_STRONG
HARDPTR-NB4: CLOSED / PASS_STRONG

HARDPTR DEVELOPMENT CLOSURE CONFIRMED
	​

NB5: UNSEALED / AUTHORIZED
	​


Autorizaría ahora la preparación de los cinco manifests sellados 6801–6805 antes de entrenar ninguno, y después comenzar por NB5-P / seed6801 con el contrato anterior.