Sí, pero haría un cambio importante respecto a tu propuesta: la nueva unidad no debe ser otro LRU de 4 GiB ni necesita 4000 updates. Step3 ya demostró por qué ese diseño fracasa. Prioridad 1 debe ser un cache persistente, completo, sellado y compartido entre corridas.

Además, una precisión del manifest: los 1000 pares son el schedule congelado de una corrida, no el período fundamental del patrón. Con 602 documentos y desplazamiento de 8:

602/gcd(602,8)=301

pares para repetir exactamente la posición inicial del batch. Y un documento individual reaparece mucho antes, alrededor de 75/76 pares. Por eso el caché completo puede quedar poblado aproximadamente en los primeros 76 pares = 152 updates, no después de 2000 ni 4000.

1. Granularidad: (documento, ventana), sin K ni seed

Tu sospecha es correcta.

El teacher recibe únicamente:

tokens/contexto
teacher weights

No consume:

K
student weights
student seed
student state

Por tanto:

cache key=(documento, ventana, identidad del teacher/contexto)
	​


y explícitamente:

K      NOT IN KEY
seed   NOT IN KEY
student architecture NOT IN KEY

El mismo target puede utilizarse para:

K1;

K4;

seed13;

seed14;

incluso ER32/ER64 u otra variante futura,

siempre que permanezcan iguales teacher, tokenizer, tokens y política de contexto.

Cachearía logits FP32 crudos

No teacher_probs, no FP16, no targets LSE en esta primera versión.

Ventajas:

preserva exactamente la loss original;

es independiente de T=2;

cambiar temperatura después no exige reconstruir el caché;

torch.save/load o mmap no debería cambiar ni un bit del tensor;

no introduce el cambio F→L que ya complicó la línea anterior.

Key mínimo:

teacher model id
teacher revision
teacher parameter hash
tokenizer revision/hash
dataset revision
retained_513_token_sha256
window = 0 | 1
teacher_context_range
representation = raw_fp32_logits
dtype = float32

No incluiría temperatura precisamente porque son logits crudos.

Y mantendría las dos formas actuales de forward:

window0:
    teacher context = tokens[0:256]

window1:
    teacher context = tokens[0:512]
    retain logits[256:512]

No fusionaría ambos en un único forward de 512 todavía. Causalmente deberían coincidir para las primeras 256 posiciones, pero ya habíamos señalado que diferentes shapes pueden producir pequeñas diferencias numéricas. Eso sería otra optimización.

2. Correctness: sí, pero más fuerte que “primeros N”

Exigiría bit-exactitud.

Para teacher logits:

torch.equal(direct, cached)
	​


y SHA-256 de los bytes exactamente iguales.

No 1e-5: aquí no estamos cambiando operaciones matemáticas ni precisión. Si almacenamos FP32 crudo, el caché debe devolver exactamente los mismos bits.

Hay una sutileza importante

El caché es por documento, pero el teacher actual corre B=8.

Debemos demostrar que el logit del documento sigue siendo bit-idéntico cuando reaparece:

en otra posición del batch;

acompañado de otros documentos.

Usaría posiciones predeclaradas del schedule:

pair 0
pair 75   # wraparound, reaparecen docs iniciales en otra posición
pair 76
pair 301  # batch-start cycle exacto

Ambas ventanas.

Esto comprueba algo mucho más fuerte que comparar un único B=1 contra B=1.

Si la salida cambia por batch position/shape, HALT. No relajamos tolerancia ni añadimos K a la key para esconder el problema.

Después, equivalencia de entrenamiento

Con dos copias bit-idénticas del mismo F:

DIRECT teacher
vs
CACHED teacher

por varios pares completos:

comparar exactamente:

CE;

KL;

total loss;

student gradients;

clip norm;

parámetros post-AdamW;

exp_avg;

exp_avg_sq;

estado recurrente detached;

checkpoint/model hash.

Idealmente torch.equal donde corresponda.

Si el cache target es bit-exacto y el resto de la ruta es idéntica, quiero checkpoint final bit-exacto. Si no lo es, se diagnostica antes de benchmarkear.

3. No usaría 4000 updates

Sería un gasto innecesario.

La pregunta de cache es de implementación, no de aprendizaje. Si los logits son bit-idénticos, no necesitamos volver a demostrar NLL después de miles de updates.

Abriría:

OMEGA-TEACHER-LOGIT-CACHE

como unidad aislada con cuatro fases.

A. Feasibility / footprint

Hay:

602×2=1204

entradas únicas.

Cada entrada raw FP32 contiene:

256×50257×4=51,463,168 bytes.

Cache completo:

1204×51,463,168=61,961,654,272 bytes

aproximadamente:

57.7 GiB
	​


Eso debe quedar explícito antes de escribir un byte.

Y una corrida de 2000 updates leería aproximadamente:

16000×51.46MB≈824GB

de logits lógicos desde el cache.

Así que ésta no es una optimización “gratis”: cambia CPU por mucho I/O. Precisamente por eso hay que medir end-to-end.

Antes de construir:

disk inventory
expected cache bytes
free-space reserve
filesystem
drive identity

Si no cabe con margen:

INCONCLUSIVE_STORAGE

No borrar nada ni cambiar a FP16 silenciosamente.

B. Formato: no repetiría los 1204 .pt de Step3

Step3 ya mostró que torch.load por entrada añade bastante overhead.

Preferiría un único backing store FP32 memory-mapped:

teacher_logits.fp32
shape conceptually:
[2, 602, 256, 50257]

orden:

[window, document, token, vocab]

Eso tiene una ventaja importante: los batches normales son ocho documentos consecutivos, así que:

cache[window, d:d+8]

es prácticamente un bloque contiguo de ~392 MiB.

En wraparound se hacen dos slices.

Acompañado por:

cache_manifest.json

con:

offsets/shapes;

hashes;

source token hashes;

teacher identity;

revisions;

file SHA;

build provenance.

El archivo grande no va a Git.

Build

Lo construiría una sola vez usando los mismos B=8 forwards del teacher.

Pairs 0..75 bastan para tocar los 602 documentos.

Cuando aparezcan documentos repetidos en el último batch, no reescribir sus entradas.

Después:

CACHE_SEALED / READ_ONLY

Los procesos de training nunca escriben.

C. Correctness gate

Antes de timing:

RAW_LOGITS_EQ
    PASS exact

CROSS_BATCH_POSITION_EQ
    PASS exact

WINDOW0/WINDOW1_CONTEXT
    PASS

TRAINING_UPDATE_EQ
    PASS exact

NEGATIVE_KEY_CONTROL
    PASS

Además:

cached training process:
    teacher_loaded = false
    teacher_forward_calls = 0

Si cualquiera falla:

CACHE_CORRECTNESS_FAIL

y no hay benchmark de performance.

D. Benchmark real

Aquí usaría K1 y K4, seed13 técnico, porque el beneficio porcentual end-to-end puede diferir aunque el cache sea común.

Cuatro rutas:

DIRECT K1
CACHE  K1
DIRECT K4
CACHE  K4

Fresh child cada una.

Mismos:

student init;

AdamW;

corpus schedule;

windows;

4/1 threads;

FP32;

CE+KL;

F implementation.

152 updates por ruta, no 4000

Usaría:

76 pares×2=152 updates.

Eso recorre al menos una vez los 602 documentos.

Para cache, el backing store ya está preconstruido, por tanto son 152 hits reales.

Además 57.7 GiB > RAM de la laptop, así que un sweep completo evita que el resultado sea simplemente “todo estaba caliente en page cache”.

Yo reportaría:

updates 0..39
    warmup / page-cache churn

updates 40..151
    primary measured region
    112 updates

40 updates ya implican tocar del orden de 16 GiB de targets, suficiente para superar aproximadamente la RAM física útil.

No necesitamos limpiar el page cache de Windows ni privilegios raros.

Primary timing

No teacher_forward_seconds.

La métrica principal debe ser:

R
cache,K
	​

=
T
DIRECT,K
	​

T
CACHE,K
	​

	​


sobre total_seconds/update de la región medida.

Porque mmap puede trasladar el costo de I/O a softmax/loss mediante page faults; medir sólo una función cache_read() podría mentir.

Registrar también:

direct teacher forward;

cache materialization/copy;

vocab/loss;

backward;

RSS;

available memory;

disk bytes logically consumed.

Gate que propondría

Dado que el precio operacional es ~58 GiB persistentes y ~0.8 TB de lecturas lógicas por corrida, exigiría más que el antiguo 10%.

Por K:

R_cache,K <= 0.90

al menos 10% de ahorro y ninguna regresión >5%.

Y global:

R
joint
	​

=
T
direct,K1
	​

+T
direct,K4
	​

T
cache,K1
	​

+T
cache,K4
	​

	​


con:

R
joint
	​

≤0.80
	​


es decir, al menos 20% de reducción conjunta.

Pero además calcularía el costo de construcción.

Break-even real

Sea:

B=T
build-cache
	​

D=T
direct,2000 projected
	​

C=T
cache,2000 projected
	​

.

Número de corridas para amortizar:

N
break-even
	​

=
D−C
B
	​

.

Dado que cada documento se reutiliza 13–14 veces dentro de una sola corrida, esperaría que:

N
break-even
	​

≤1
	​


o, equivalentemente, que incluso incluyendo el build inicial:

B+C<D.

Si ni siquiera se amortiza dentro de la primera corrida de 2000, 58 GiB de cache son difíciles de justificar.

Después de la primera, K1/K4/seeds posteriores reciben el cache gratis.

Respuestas directas
1. ¿Por (documento, ventana,K)?

No.

(documento, ventana)

más identidad/provenance del teacher.

Compartido entre K1/K4 y también entre seeds.

No sólo “mismo seed”.

2. ¿Validar primeros N con/sin cache?

Sí, pero exigir bit-exactitud y probar también reutilización en otra posición del batch.

Teacher logits exactos + entrenamiento exacto antes de medir rendimiento.

3. ¿Nueva unidad aislada y 4000 updates?

Nueva unidad aislada: sí. 4000 updates: no.

El contrato que recomiendo es:

OMEGA-TEACHER-LOGIT-CACHE

A — feasibility
    1204 unique doc-window entries
    ~57.7 GiB raw FP32
    disk safety check

B — build once
    canonical B=8 teacher forwards
    pairs 0..75
    raw FP32 mmap
    immutable/sealed cache

C — correctness
    exact raw logits
    cross-batch-position
    exact training-update equivalence
    no teacher loaded on hit route

D — cost benchmark
    DIRECT/CACHE × K1/K4
    seed13 technical
    152 updates/route
    first40 warmup
    last112 measured

Gate:
    each K no meaningful regression
    joint >=20% end-to-end saving
    cache build amortizes <=1 full 2000-update run

Esto responde exactamente la pregunta de Prioridad 1 sin gastar otras 4,000–8,000 actualizaciones científicas.

Y hay una ventaja adicional importante: si funciona, el cache se construye una sola vez para toda la campaña futura con ese teacher/corpus, no una vez por K ni por seed. Esa reutilización entre experimentos es donde esta optimización puede terminar siendo mucho más valiosa que lo que sugería el viejo Step3 de 4 GiB.