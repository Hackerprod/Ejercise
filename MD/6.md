Sí. Confirmo el cierre de C1-lossnorm-seed101 como CLOSED / PASS_STRONG, y autorizo las semillas 202, 303, 404 y 505 antes de avanzar.

El siguiente orden sigue siendo: replicación de C1 → programas mixtos con routing oráculo → router aprendido. No introduciría programas mixtos y control aprendido simultáneamente.

Revisé el commit 7100832, la normalización de la pérdida, el runner y el JSON final. Esta confirmación se basa en el código y los resultados publicados; no he vuelto a ejecutar los checkpoints.

## 1. Seed101 cumple el criterio fijado antes de la corrida

La conservación de RAW ya no depende de escoger un checkpoint intermedio favorable:

| H | Baseline U0-A | Límite fijado | Checkpoint final | Resultado |
|---|---|---|---|---|
| 2 | 0.000252616 | 0.000262616 | 0.000114499 | Pasa |
| 4 | 0.000424738 | 0.000434738 | 0.000125491 | Pasa |
| 6 | 0.001438047 | 0.001448047 | 0.000477668 | Pasa |

El JSON confirma esos valores y que best_step=12000. El checkpoint final satisface por sí mismo las restricciones; no necesitamos apoyarnos en la etiqueta "best". Las matrices discretas publicadas también conservan sus resultados.

La tarea transformada registra: max e_final=0.0003020368, max e_delta=0.0003417411, min cos=0.9999997616, con top-1 correcto en todas las rondas activas y error de payload cero. Pasa tanto el criterio final como el de deltas.

"Reader real y oráculo coinciden" debe entenderse como equivalencia numérica en esta evaluación: los máximos coinciden y algunas medias por ronda difieren en los últimos decimales. No hace falta exigir identidad bit a bit de todas las reducciones para cerrar este gate.

No queda pendiente otra auditoría de BLEND para seed101.

## 2. La intervención ya tiene una conclusión causal útil

El código aplica ahora L_Δ = Σ m_br(Δ̂_brd-Δ*_brd)² / (D·Σm_br), y conserva L_transformada = L_Δ + 0.25·L_final. La división adicional por D aparece exactamente en delta_loss_per_coordinate; el runner mantiene el resto de esa combinación.

Con la inicialización y el protocolo controlados, la conclusión es:

> Eliminar la ponderación dimensional accidental fue suficiente para resolver la regresión RAW observada en seed101, conservando el éxito de SELECT y de las demás tareas.

No necesitamos demostrar que desapareció todo coseno negativo entre gradientes. El objetivo no era hacer idénticas las preferencias de las tareas, sino encontrar una receta conjunta que las satisfaga.

Tampoco concluiría todavía que este ajuste resolverá cualquier interferencia futura. Esa generalización no está probada. Pero sí queda descartada, para esta corrida, la necesidad de separar readers, aumentar D o introducir nuevos adapters para corregir aquella deriva.

## 3. Las otras cuatro semillas quedan autorizadas, con una definición explícita de replicación

Para evitar otro desvío de interpretación, esta campaña debe repetir la fase C1 desde el mismo checkpoint compuesto inicial:

- Base: U0-A seed101 aprobado.
- Corrector: C0-oracle seed101 network_trainable aprobado.
- Semilla de optimización de C1: 101, 202, 303, 404, 505.

La seed101 ya está terminada. Ejecutar únicamente las cuatro restantes.

Mantener los mismos datasets y splits. Cambiar la semilla que controla el orden de entrenamiento y la aleatoriedad de C1; no cambiar simultáneamente las semillas generadoras de los datasets ni los checkpoints de preentrenamiento.

Esto responde una pregunta bien delimitada:

> ¿La receta C1 corregida conserva las siete tareas con distintos órdenes de optimización, partiendo de la misma base validada?

No sería correcto describir después esas cinco corridas como cinco entrenamientos independientes de toda la arquitectura desde cero. Serían cinco réplicas de fine-tuning conjunto condicionadas a una inicialización preentrenada común. La robustez frente a diferentes checkpoints de origen es otro experimento, que no hace falta añadir ahora.

### Ajuste necesario del runner

El source actual todavía declara `SEED = 101` y no expone `--seed` en el parser. Los generadores de los loaders se construyen a partir de esa constante. Cambiar únicamente el nombre del directorio no produciría otra semilla.

Parametrizar `--seed` y propagarlo a la aleatoriedad de entrenamiento y a los generadores de los loaders. Conservar por separado las semillas fijas de los splits.

Este es un ajuste de ejecución, no una nueva variante arquitectónica.

## 4. Receta y criterios congelados

Para las cuatro réplicas:

| Elemento | Mantener |
|---|---|
| Modelo | Misma arquitectura y mismos pesos iniciales |
| Presupuesto | 12,000 supersteps |
| Optimizador | Nuevo en cada corrida; misma configuración |
| Pérdida | Normalización por coordenada corregida |
| Replay ALU | Mismo calendario 5:1 |
| Lectura | BLEND histórico; SELECT con STE en la ruta declarada |
| Datos | Mismos splits y targets externos |
| Evaluación principal | Checkpoint final del step 12000 |
| Checkpoint de mejor validación | Se informa aparte; no sustituye al final |

Cada réplica debe satisfacer los criterios existentes, no reproducir exactamente los números de seed101:

- Las tareas discretas conservan su gate y sus controles de profundidad.
- La tarea transformada conserva coseno, error final y error de delta dentro de los límites establecidos.
- RAW satisface, para cada H: e_RAW,final(H) ≤ e_RAW,step0(H) + 1e-5.

Como las réplicas parten del mismo estado inicial y usan la misma evaluación, sus baselines deben coincidir. El preflight de igualdad de pesos y resultados de step 0 debe mantenerse.

No añadir PCGrad, schedules nuevos, otra temperatura, más steps o cambios de pérdida por semilla. Si alguna falla, se registra como fallo de esa receta; no se repite silenciosamente hasta conseguir cinco éxitos.

Tampoco es necesario que las cinco mejoren RAW tanto como seed101. Conservarlo dentro de la tolerancia fijada ya satisface el gate.

## 5. El timeout no invalida este resultado

El intento interrumpido y el posterior reinicio completo deben contarse por separado:

- Intento interrumpido: coste operativo, no resultado de replicación.
- Corrida completa: 12,000 supersteps desde la inicialización correcta.

Dado el reinicio limpio que reportas, no hay que sumar los pasos del intento abortado a la trayectoria del checkpoint final.

Sí hay un detalle operativo que conviene resolver para no volver a desperdiciar cómputo: el `save_checkpoint()` actual guarda modelo, optimizador y RNG globales, pero el runner utiliza generadores propios de cada DataLoader y no guarda su posición de iteración. Por ello, el `--resume` publicado no asegura reconstruir exactamente la misma secuencia de minibatches que una corrida ininterrumpida.

Esto no reabre seed101, porque terminó desde cero. Para las siguientes corridas, usar directorios exclusivos, impedir dos procesos sobre la misma salida y no presentar una reanudación como bit-equivalente sin conservar también el estado del muestreo. Los intentos abortados deben permanecer identificados, no mezclados con el log final.

## 6. Después de la replicación: composición primero, router después

Si las cinco semillas pasan, C1 podrá cerrarse como receta reproducida de coexistencia conjunta.

El siguiente gate debe mantener el routing oráculo y comprobar programas donde una operación consuma realmente el resultado de otra. No basta con ejecutar varias tareas independientes dentro del mismo ejemplo.

Por ejemplo, el criterio conceptual será: resultado de lectura → operando de transformación → estado utilizado por una operación posterior.

Los controles deberán mostrar que alterar el estado intermedio cambia o destruye el resultado final. Eso separa composición real de la mera coexistencia de rutas.

El router aprendido va después. Si se introducen al mismo tiempo programas nuevos y un controlador aprendido, un fallo volvería a mezclar dos preguntas: si las primitivas componen y si el controlador sabe elegirlas.

## Ledger

```
C1-lossnorm-seed101: CLOSED / PASS_STRONG.
Replicación C1 con seeds 202/303/404/505: AUTORIZADA.
C1 multi-seed: PENDIENTE de esas cuatro corridas.
Programas mixtos con routing oráculo: SIGUIENTE tras la replicación.
Router aprendido: DESPUÉS de validar composición.
```

Seed101 ya no necesita más pruebas ni ajustes. El resultado cumple el criterio fijado antes de entrenar, en el checkpoint final, sin sacrificar RAW para mejorar SELECT. Ahora corresponde comprobar la robustez de esa misma receta, no seguir modificándola.
