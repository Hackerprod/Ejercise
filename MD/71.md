Sí: verificaría primero la geometría. No autorizo todavía Q(r)=[r,0] como teacher de R1.

El bloqueo que encontraron es real y cambia una suposición de mi diseño anterior: yo propuse register_decoder(r_F, C) como si r_F viviera en el mismo espacio de 64 dims del codebook. No es así.

Qué implica exactamente el zero-pad

Sea cada embedding VALUE congelado:

c
i
	​

=[a
i
	​

,b
i
	​

]∈R
64
,a
i
	​

,b
i
	​

∈R
32
.

Y el bridge:

Q(r)=[r,0].

Entonces el decoder calcula:

ℓ
i
	​

(r)=20
∥r∥∥c
i
	​

∥
r
⊤
a
i
	​

	​

.

Por tanto el prototipo efectivo que ve R1 sería:

p
i
	​

=
∥c
i
	​

∥
a
i
	​

	​

∈R
32
.

Tu cota:

ρ
i
	​

=
∥c
i
	​

∥
∥a
i
	​

∥
	​


es correcta: ése es el máximo coseno efectivo que puede alcanzar la clase i.

Pero ρ
i
	​

 solo no basta. Incluso con ρ=.5, los 32 VALUE podrían estar perfectamente separables; o con ρ=.9, varias clases podrían ser casi colineales en esos primeros 32 dims.

T2-NOBYPASS-REF-GEOM-ALG: AUTHORIZED

Cero entrenamiento.
R0 congelado.
Executor/codebook congelado.
Ningún gate nuevo.

Calcularía cuatro cosas.

1. Distribución de energía por mitad

Para los 32 VALUE:

ρ
i
	​

=
∥c
i
	​

∥
∥c
i
	​

[:32]∥
	​

.

Reportar:

min

median

mean

max

los 32 valores individuales.

También:

ρ
i
tail
	​

=
∥c
i
	​

∥
∥c
i
	​

[32:]∥
	​

.

Esto responde si el zero-pad está descartando gran parte de la geometría.

2. Geometría efectiva del decoder

Construir:

P=
	​

p
0
⊤
	​

⋮
p
31
⊤
	​

	​

	​

∈R
32×32
,p
i
	​

=
∥c
i
	​

∥
c
i
	​

[:32]
	​

.

Reportar:

rank numérico;

los 32 singular values;

σ
min
	​

;

σ
max
	​

;

condition number.

Esto es mucho más decisivo que sólo la norma.

3. Self-query directo

Para cada VALUE i, usar:

r
i
	​

=c
i
	​

[:32].

Pasarlo por el decoder exactamente mediante zero-pad:

register_decoder([r
i
	​

,0],C).

Reportar:

top-1 i==prediction;

32/32 total;

margen top1-top2 para cada VALUE;

mínimo/mediana de margen.

Si ya obtenemos:

32/32

con esa lectura trivial, tenemos evidencia bastante fuerte de que la primera mitad preserva la identidad numérica.

4. Prueba constructiva más fuerte

Ésta es la que realmente decide viabilidad.

Si P tiene rango 32, entonces existe para cada VALUE una dirección 32-D que el decoder zero-pad clasifica de forma única.

Resolver:

PU=I.

La columna u
i
	​

 satisface:

Pu
i
	​

=e
i
	​

.

Después normalizar:

u
^
i
	​

=
∥u
i
	​

∥
u
i
	​

	​

.

Entonces, algebraicamente:

P
u
^
i
	​

=
∥u
i
	​

∥
e
i
	​

	​

.

Es decir, antes del factor 20:

clase correcta: 1/∥u
i
	​

∥

las otras 31: exactamente 0.

Pasar también [û_i,0] por el register_decoder real y comprobar:

32/32
	​


más los 32 márgenes efectivos:

m
i
	​

=
∥u
i
	​

∥
20
	​

.

Esta prueba es importante porque self-query puede fallar y aun así la geometría 32-D ser perfectamente capaz de representar las 32 clases.

Ramas predeclaradas
A — P full-rank + 32/32 constructivo con márgenes razonables

Entonces:

Q(r)=concat(r,0): AUTHORIZED para R1.

Y R1 sigue siendo exactamente la intervención causal que queríamos:

R0+L
ref
	​


sin arquitectura nueva.

La loss puede escribirse literalmente:

CE(register_decoder(Q(r
F
	​

),C),L)

y análogo para AVOID.

No cambiar temperatura ni Wv.

B — no full-rank, pero las 32 clases siguen siendo separables

El full-rank es suficiente, no matemáticamente necesario.

Si algún singular value es cero, pero una prueba de optimización por clase demuestra 32/32 regiones top-1 con buenos márgenes, zero-pad podría seguir siendo válido.

En ese caso revisaría los resultados antes de autorizarlo.

C — alguna VALUE no puede ser top-1 bajo el subespacio zero-pad

Entonces:

zero-pad queda RECHAZADO como teacher
	​


porque NB2 contendría un imposible geométrico.

No entrenaría R1 y luego culparía al encoder.

Si falla el bridge, mi alternativa preferida

No usaría entropy loss ni temperatura.

Haría que la referencia realmente viva en el espacio de la ISA:

W
v
	​

:16→64

en vez de:

16→32.

Entonces:

r
F
	​

,r
A
	​

∈R
64

y tanto el scorer como register_decoder consumirían el objeto directamente, sin bridge artificial.

Eso añadiría sólo:

(16⋅64+64)−(16⋅32+32)=1088−544=
544
	​


parámetros.

Total del encoder:

2656+544=
3200
	​

.

Pero sería un cambio arquitectónico explícito, así que no lo haría mientras exista la posibilidad de que el zero-pad ya sea geométricamente suficiente.

Una precisión metodológica

Si el diagnóstico demuestra que zero-pad es viable, no deberíamos decir después que R1 “aprendió embeddings VALUE completos de 64 dims”.

Habrá aprendido una referencia de 32 dims en un subespacio fijo del codebook, capaz de ser decodificada por la ISA congelada.

Eso basta para NB2 y es una afirmación limpia.

Orden

R0: permanece CLOSED / VALID FAIL @ NB2.

R1: HOLD antes de entrenamiento.

T2-NOBYPASS-REF-GEOM-ALG: AUTHORIZED.

Haz exactamente el análisis congelado de ρ
i
	​

, matriz efectiva P, singular values/rank, self-query 32/32 y la construcción PU=I. Con eso podemos decidir sin contaminar R1 si el bridge zero-pad es una interfaz válida o si hay que llevar r
F
	​

/r
A
	​

 a 64 dims.