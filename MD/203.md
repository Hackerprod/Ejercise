No mantendría κ≥32 como requisito para R1. Tampoco lo bajaría a 15 o 8: eso sería recalibrarlo mirando este update.

Cambiaría el fallback de backward —para R0 y R1 por igual— por una cota basada en el número real de sumandos FP32.

La política primaria queda intacta:

∣g
cpp
	​

−g
ref
	​

∣≤10
−5
+10
−4
∣g
ref
	​

∣.

Sólo si falla, calcular:

A=
i
∑
	​

∣c
i
	​

∣,η=
A
∣g
cpp
	​

−g
ref
	​

∣
	​


y, con n igual al número real de contribuciones acumuladas para ese elemento,

γ
n
	​

=
1−nϵ
nϵ
	​

,ϵ=1.1920928955078125×10
−7
.

Como estamos comparando dos reducciones FP32 distintas, aceptaría por fallback si:

η≤2γ
n
	​

	​


El factor 2 viene de que cada ruta puede desviarse del sumatorio exacto por su propio orden de reducción; por desigualdad triangular, la diferencia entre ambas puede alcanzar aproximadamente la suma de ambas cotas.

κ se sigue registrando, pero pasa a ser diagnóstico, no gate.

Por qué esto es mejor

Tus dos fallos actuales tienen:

eta ≈ 9.30e-6
eta ≈ 9.91e-6

Eso no parece grande para una reducción de cientos o miles de contribuciones.

Por ejemplo, si depth_embedding acumula realmente 8×256=2048 términos por elemento:

2γ
2048
	​

≈4.88×10
−4
.

Los errores observados serían unas 49× menores que esa cota.

Incluso con solamente 128 contribuciones:

2γ
128
	​

≈3.05×10
−5
,

seguirían pasando.

Eso es mucho más defendible que decir “κ=15 ahora también cuenta como cancelación suficiente”.

No declararía R1 PASS todavía

Antes de adoptarlo, haría un diagnóstico congelado y pequeño, porque este cambio aparece después del primer update vivo.

Ejecutaría:

K1: updates 0..7
K4: updates 0..7

16 comparaciones reales en total.

Muy importante: el PyTorch reference avanza pesos/optimizer/state. El kernel nativo se evalúa como shadow oracle en cada update; sus gradientes no deben determinar el siguiente estado durante este diagnóstico.

Así cada punto prueba una distribución real distinta sin dejar que una discrepancia previa contamine las siguientes.

Registrar para cada parámetro/elemento que falle el gate primario:

abs_error
relative_error
n_contributions
sum_abs_contributions
kappa
eta
gamma_n
eta / (2*gamma_n)

y clasificación:

PASS_PRIMARY
PASS_ACCUMULATION_BOUND
FAIL
Regla congelada antes de correrlos
BACKWARD PASS iff every element:

    primary allclose
    OR
    eta <= 2*gamma_n

Sin:

excepciones por nombre de parámetro;

umbral especial para depth_embedding;

cambio de constantes después de ver los 16 updates.

Si los 16 updates K1/K4 pasan, adopto esta política como la numérica canónica de P2-R y R1 puede continuar.

Si aparece algún elemento con:

η>2γ
n
	​

,

eso vuelve a ser debug obligatorio, no otra ampliación de tolerancia.

Qué haría con Addendum 236

Lo supersedería sólo en su fallback:

OLD:
    kappa >= 32
    eta <= 32*eps

NEW:
    n-aware accumulation bound
    eta <= 2*gamma_n

El primary atol=1e-5, rtol=1e-4 permanece exactamente igual.

Y volvería a pasar los 4 goldens R0 con la nueva regla, sin reentrenar ni regenerarlos, sólo para demostrar que el nuevo criterio también cubre todo el conjunto histórico.

En resumen: no generalizaría κ=32 a datos vivos; reemplazaría esa heurística por una cota de acumulación FP32 dependiente de n, y la validaría en 8 updates K1 + 8 K4 antes de autorizar R1 como PASS.