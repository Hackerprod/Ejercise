Sí. Confirmo el cierre de C para SCOPE-A y autorizo integrar F, comprobar la integración y relanzar las cuatro corridas reales. No hace falta otra autorización intermedia si esas comprobaciones pasan.

Revisé d6d765cf4f66173f8b7b051f06ef9c891fe299a0, el simulador, el reporte de la ejecución completa y el runner actual de SCOPE-A. También reproduje por separado el recorrido modular y el LRU: obtuve 0 aciertos, 16,000 fallos y 15,917 expulsiones. No he reejecutado el entrenamiento en tu laptop.

1. C queda cerrado: el experimento respondió la pregunta

Con esta política de caché y este recorrido, no se recupera el coste de guardar los targets. El reporte registra 5.774 s/update para F y 11.132 s/update para el fallo de caché C+L: aproximadamente 1.93 veces el coste de F cuando no hay aciertos. Los aciertos rápidos existen, pero la simulación no los obtiene bajo el recorrido y la capacidad fijados.

Por tanto, la clasificación es:

C+L / FP32 / LRU de 4 GiB / recorrido actual
    CLOSED — NOT_SELECTED

F / modelo fusionado / pérdida original / profesor directo
    SELECTED_FOR_SCOPE_A

No ampliaría el caché, no cambiaría su política y no reduciría el corpus para intentar salvarlo ahora. Cerramos esta línea de optimización y continuamos con F.

El alcance debe quedar bien escrito: esto descarta esta configuración de caché para esta campaña, no demuestra que todo precálculo del profesor sea inútil. Tampoco es un fallo científico de OMEGA: es una decisión de implementación respaldada por el resultado.

Una corrección documental que no cambia el cierre

301 pares es el período de repetición del lote completo ordenado, no el tiempo hasta que vuelve a aparecer cualquier documento.

Con posiciones elegibles 0…601, el lote del par p comienza en (8p) mod 602. En el par 75, contando desde cero, aparecen:

600, 601, 0, 1, 2, 3, 4, 5

Ya hay documentos repetidos. El lote inicial completo vuelve en:

602/gcd(602,8)=301 pares.

El código calcula precisamente la primera repetición del lote, y el LRU simula los accesos individuales. Por eso corregir esa frase no cambia los cero aciertos. Regístralo como aclaración del cierre, sin modificar las mediciones ni repetir la prueba.

2. La integración de F debe cubrir el runner completo

La autorización incluye los siguientes ajustes de integración, no otra campaña de optimización.

Inicialización y construcción del optimizador. Para cada combinación de seed y variante, inicializar la referencia con la convención existente y convertirla mediante OmegaCoreLMFast.from_reference(). Después, liberar la referencia y construir AdamW sobre los parámetros de F. No sustituir eso por inicializar directamente la clase fusionada con la misma seed: el orden y las formas de los parámetros han cambiado. La conversión explícita ya existe en el candidato.

Entrenamiento y validación. El candidato devuelve (estado, logits, trace), mientras que _batch_loss() y evaluate_validation() del runner actual esperan dos resultados. Adaptar ambos caminos —o usar un adaptador mínimo común—, descartando las trazas que no se necesitan. No basta cambiar el constructor del modelo. La validación debe seguir calculando NLL sobre los targets, no la pérdida combinada de destilación.

Checkpoint y reanudación. Guardar y restaurar la clase F, su optimizador y su identidad de implementación. No cargar checkpoints fusionados en la clase original ni reutilizar estados del intento abortado. El runner actual construye la clase original también en su rama de reanudación; esa rama forma parte del cambio.

Persistencia de resultados. Hay un detalle que conviene resolver en esta misma integración: actualmente validation_curve.json se escribe al finalizar la corrida. Debe persistirse cada evaluación al producirse, junto con su checkpoint, para que una interrupción no pierda la curva ya calculada. No cambia el calendario ni el criterio científico; cumple el contrato de recuperación.

F queda exactamente como se seleccionó: modelo fusionado, pérdida original y profesor directo. Sin introducir simultáneamente LSE, caché, otra ruta de contextos del profesor o nuevas fusiones.

3. Comprobación breve sobre el camino que realmente se ejecutará

Antes del relanzamiento, ejecutar los tests afectados y un smoke de integración que invoque el mismo _batch_loss, constructor, evaluador y mecanismo de checkpoint que utilizará la campaña.

Debe comprobar:

Batch real [8,256], 2,048 targets por update, una llamada batched del estudiante y del profesor; un backward, un clipping y un AdamW step.

Dos parejas 0→1, cambiando de documentos entre parejas: reset en window 0 y continuidad detached exclusivamente desde su window 0 en window 1.

Correspondencia numérica dentro del gate vigente, evaluación NLL y recuperación desde un checkpoint F en un proceso nuevo.

Los fixtures de recuperación pueden ser pequeños. Para el smoke con dimensiones y profesor reales, bastan cuatro updates por variante, descartables y separados de los 2,000 científicos. No hay que repetir R/F/L/C ni volver a medir el caché.

Si pasa, se versiona la integración y se inicia SCOPE-A bajo esta autorización. Si falla, se detiene y se corrige el defecto localizado; no se ignora para lanzar la campaña.

4. Mantener la protección de memoria durante la campaña

La ejecución completa publicada registra mínimos observados superiores a la reserva: aproximadamente 1.27 GB para F y 2.35 GB para C+L. Eso cierra la comprobación corta exigida, pero no convierte el margen de F en una garantía para todas las horas posteriores.

Para SCOPE-A, conservar:

Un solo entrenador activo, preferentemente un proceso nuevo por corrida; cuatro hilos intraoperación y uno interoperación, como en la prueba aceptada; controles de memoria antes de asignaciones importantes y entre fases; parada ante disponibilidad inferior a 1 GiB, pérdida o gradientes no finitos, o desviación de identidad.

Los controles de pérdida y gradientes deben actuar antes de optimizer.step(), no limitarse a registrar retrospectivamente que una actualización fue inválida. No se reduce batch, se cambia backend o se eliminan procesos ajenos para continuar silenciosamente.

5. Autorización de SCOPE-A con F
Elemento	Contrato
Variantes	shared_K1 y shared_K4
Seeds pareadas	20260913 y 20260914
Presupuesto	2,000 updates por combinación; 4 corridas y 8,000 updates científicos
Ejecución	Esta laptop, CPU, FP32 eager, batch físico y efectivo 8
Implementación	F, pérdida original, DistilGPT2 congelado y directo
Datos	Recorrido completo de train y manifiestos fijados; no limitarse a los ocho documentos técnicos
Evaluación y checkpoints	0, 500, 1000, 1500, 2000; mismo conjunto de validation para todas las corridas
Test y ampliaciones	Test prohibido; SCOPE-B y campaña confirmatoria no autorizados por extensión

Las cuatro corridas empiezan desde cero, con directorios e identidades nuevos. Los 99 updates del intento defectuoso y todos los updates técnicos se conservan como evidencia histórica, pero no cuentan como progreso de esta campaña.

El nuevo registro debe identificar el commit integrado, el candidato F, sus dependencias y los manifiestos. No debe afirmar que conserva los hashes del freeze confirmatorio original.

La interpretación científica tampoco cambia: se reportan las curvas y los deltas pareados a igual número de updates. PROMISING, MIXED o NO_SIGNAL siguen siendo clasificaciones de scoping; no equivalen a confirmar el gate de Δ_depth ≥ 0.05, ni autorizan automáticamente más entrenamiento.

CPU-FASTPATH-VALIDATION
    CLOSED / DECISION_COMPLETE

C+L
    CLOSED / NOT_SELECTED_FOR_SCOPE_A

F
    SELECTED

INTEGRACIÓN + TESTS + SMOKE
    AUTHORIZED

SCOPE-A REAL CON F
    AUTHORIZED AFTER INTEGRATION CHECKS PASS

SCOPE-B / CAMPAÑA CONFIRMATORIA / NUEVAS OPTIMIZACIONES
    HOLD

Cerramos la optimización previa. El siguiente resultado que debe buscar el proyecto es la curva lingüística de SCOPE-A con F, no otra variante de infraestructura.