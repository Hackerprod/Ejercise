No lo clasificaría como una simple seed desafortunada. Hay una razón estructural clara para que el gate multiplicativo interfiera con el binding durante entrenamiento, y los resultados encajan con ella.

Estado formal

T2-NOBYPASS-1-R1.4-S seed6706: CLOSED / VALID FAIL @ NB2-RB-PAIR

La regresión es grande:

R1.3:139/139, 139/139

frente a R1.4-S:

FLOOR=112/139,AVOID=66/139.

66/139 = 47.5%, así que lo llamaría aproximadamente chance-level, no evidencia sólida de rendimiento por debajo del azar.

Y el hecho de que normal y reverse den exactamente 112/66 es interesante: la invariancia relacional que R1.3 consiguió parece seguir estructuralmente presente; lo que se degradó es la geometría de las queries.

Por qué el gate puede causar esto

R1.3 tenía:

r=
i
∑
	​

a
i
	​

v
i
	​

.

Por tanto el gradiente hacia un score de atención s
j
	​

 contiene aproximadamente:

∂s
j
	​

∂r
	​

=a
j
	​

(v
j
	​

−r).

En R1.4 cambiamos a:

r=
i
∑
	​

a
i
	​

c
i
	​

v
i
	​

.

Entonces:

∂s
j
	​

∂r
	​

=a
j
	​

(c
j
	​

v
j
	​

−r).

Eso no es la misma dinámica con un filtro añadido downstream. El propio c
j
	​

 cambia la señal que reciben q
F
	​

,q
A
	​

 y las keys.

Y además apareció una segunda ruta de optimización:

L
ref
	​

→{
cambiar a
t
	​

cambiar c
t
	​

	​

(binding)
(eligibility)
	​


En atomic training sólo hay un argumento semánticamente relevante. Por tanto esas dos rutas son parcialmente sustituibles.

R1.3 obligaba al sistema a resolver bastante bien:

¿dónde está el argumento?

R1.4 puede resolver parte del mismo loss mediante:

¿qué tipos de token dejo contribuir?

sin necesitar que q
F
	​

/q
A
	​

 desarrollen la misma separación.

Y eso es exactamente lo que vemos:

c(VALUE)=0.419>c(structure)=0.265

—el gate aprendió algo—

pero al mismo tiempo:

RB-PAIR

se degradó brutalmente.

Así que tu explicación de gradient/credit-assignment interference me parece la principal.

No autorizaría otra seed end-to-end todavía

Una segunda seed con exactamente la misma receta respondería:

¿pasa siempre?

pero no:

¿por qué pasó?

Y si seed6707 casualmente diese 139/139, quedaríamos sin saber si el diseño es robusto o simplemente multimodal.

Primero podemos hacer una prueba causal mucho más limpia.

T2-NOBYPASS-1-R1.4-S-GATEONLY: AUTHORIZED

Usar como base el checkpoint R1.3 seed6705 que ya demostró:

RB-PAIR=139/139

en ambos roles y ambos órdenes.

Congelar absolutamente todo:

embeddings;

RELKEY W
k
	​

;

q
F
	​

,q
A
	​

;

W
v
	​

;

mode/control head;

resto del encoder/runtime.

Añadir únicamente los 17 parámetros:

c
t
	​

=σ(w
c
⊤
	​

e
t
	​

+b
c
	​

).

Y entrenar sólo esos 17 parámetros sobre exactamente el mismo dominio atomic/NOOP.

Sin joint examples.

Por qué este experimento es especialmente limpio

Como q,k están congelados:

RB-PAIR debe permanecer 139/139 bit-for-bit
	​


No puede degradarse.

Por tanto la pregunta se reduce a:

dado un binding correcto ya existente, ¿puede el gate lexical aprender a eliminar contaminación estructural?

Ésa era la hipótesis original de R1.4, aislada ahora del confound de coadaptación durante entrenamiento.

Training del gate-only

Mantendría:

L=L
behavior
	​

+L
ref
	​


pero gradientes únicamente para w
c
	​

,b
c
	​

.

Misma data.

Usaría los mismos 5000 updates y wd=0 para no abrir otro sweep.

No hace falta heredar el optimizer de R1.3.

Para inicialización prefiero:

w
c
	​

=0,b
c
	​

=0.

Entonces:

c
t
	​

=0.5

para todos los tokens inicialmente.

Eso multiplica todo el readout por el mismo escalar positivo. Para el decoder coseno-normalizado la dirección inicial es exactamente la misma que R1.3, así que partimos de una gate neutral en geometría.

Gates del diagnóstico
G0 — frozen-core integrity

Antes y después del entrenamiento:

hash(core)=hash(R1.3 core)

y los logits/scores de addressing deben ser idénticos.

G1 — RB-PAIR preservation

Normal y reverse:

139/139

en las cuatro métricas.

Si cambia una sola: HALT, el freeze no fue real.

G2 — gate separation

Diagnóstico, no gate:

c
ˉ
VALUE
	​


vs

c
ˉ
structure
	​

.
G3 — DEC

Normal/reverse FLOOR/AVOID/JOINT.

Aquí tengo una predicción bastante concreta.

Qué espero

Recordemos el oracle ZERO-STRUCT de R1.3:

FLOOR 139/139;

AVOID 138/139.

Así que si el learned gate consigue aproximar bien:

c(VALUE)≈1,c(structure)≈0,

deberíamos movernos hacia ese mismo régimen.

No espero necesariamente 139/139 sin temperatura, porque ya sabemos que existe el residual independiente 29 vs 27.

Así que un resultado como:

FLOOR=139,AVOID=138

sería en realidad éxito completo del gate, no fallo arquitectónico.

Después aplicaríamos el sweep congelado γ=2. Dado lo ya demostrado, debería cerrar.

Interpretaciones predeclaradas
A — gate-only recupera ~ZERO-STRUCT

Por ejemplo 139/138 o 139/139.

Entonces queda demostrado:

el gate funciona; el fallo de seed6706 fue coadaptaci
o
ˊ
n durante joint optimization
	​


No era una mala idea de inferencia.

Era una mala factorización del aprendizaje.

En ese caso yo no repetiría R1.4 end-to-end sin cambios.

Diseñaría el entrenamiento definitivo de forma staged:

Phase A: train RELKEY binding
Phase B: freeze binding, train lexical gate

y sólo si hace falta:

Phase C: learn/fix mild temperature.

La arquitectura de inferencia permanece microscópica; sólo cambia el curriculum de optimización.

B — gate-only apenas mejora DEC

Entonces el gate sigmoidal lineal:

σ(w
⊤
e+b)

no tiene capacidad/identificabilidad suficiente para aprender VALUE-vs-structure desde el objective disponible.

Ahí revisaríamos el gate mismo, no q/k.

C — gate-only empeora pese a core congelado

Entonces la formulación multiplicativa del content gate es el problema y hay que descartarla.

No espero esta rama.

Después, si A pasa, hay una alternativa elegante para fresh end-to-end

No la ejecutaría todavía, pero la dirección sería evitar que el gate robe la señal de binding mediante una loss auxiliar ungated durante entrenamiento:

r
r
bind
	​

=
t
∑
	​

a
r,t
	​

W
v
	​

(e
t
	​

)

para mantener la presión original de R1.3 sobre q/k,

mientras el path funcional usa:

r
r
gated
	​

=
t
∑
	​

a
r,t
	​

c
t
	​

W
v
	​

(e
t
	​

).

Así:

el ungated reference objective preserva el aprendizaje de binding;

el gated objective aprende content eligibility.

En inference sólo necesitaríamos el path gated.

Pero introducir eso antes de comprobar gate-only sería prematuro.

Sobre una segunda seed

Después del gate-only, sí podría tener sentido una repetición como seed-stability study.

Ahora mismo:

otra seed R1.4-S end-to-end: NOT AUTHORIZED.

Porque tenemos una hipótesis causal mejor que “mala suerte”.

Resolución

R1.4-S seed6706: CLOSED / VALID FAIL.

Content gate: aprendió señal real pero débil.

Binding regression: probablemente credit-assignment/coadaptation causada por el nuevo multiplicative path, no evidencia de que RELKEY haya dejado de ser válido.

Otra seed idéntica: HOLD.

T2-NOBYPASS-1-R1.4-S-GATEONLY: AUTHORIZED usando el checkpoint R1.3/6705 como core congelado y entrenando únicamente los 17 parámetros del content gate.

Ese experimento separará de manera mucho más limpia “el gate es una mala idea” de “el gate es correcto, pero no debe aprender simultáneamente con el binding”.