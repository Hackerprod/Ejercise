Confirmo formalmente:

T5-N4-FRESH: CLOSED / PASS_STRONG
	​


y cerraría también la campaña científica actual de T5 como:

T5-NROLE: CLOSED / PASS_STRONG / HALT
	​


con un alcance muy preciso: la parametrización genérica reproduce T4 exactamente en N=3 y fue validada fresh en N=4. Eso no significa todavía “funciona para cualquier N”.

El incidente de schema no afecta la clasificación: el intento fue rechazado antes de construir/evaluar ningún seed y no produjo checkpoints ni evidencia parcial. El commit final confirma la corrida limpia posterior.

Qué ya sabemos

La evidencia ahora forma una cadena bastante fuerte:

N=3:generic RCSEP-N+BG≡T4 RCSEP3-BG

bit-exact, y después:

N=4:5/5 fresh

con:

119040/119040

composiciones correctas y todas las 4!=24 órdenes.

Por tanto ya tenemos evidencia de que el mecanismo no dependía de tres queries hardcodeadas.

Pero siguen existiendo límites importantes. Todos los tests T4/T5 tienen exactamente una instancia de cada rol y, en T5-N4, siempre están presentes los cuatro roles.

Ese supuesto es ahora más interesante que simplemente pasar de N=4 a N=5.

Primero: T5-FINAL-CLOSURE

Haría una unidad documental, cero training:

T5-FINAL-CLOSURE
	​


Consolidaría:

N=3 equivalence bit-exact;

fórmula genérica

L
sep
N+BG
	​

;

N
2
 términos;

32+2N backgrounds;

coste 1088N
2
;

730x = development;

740x = fresh;

hashes de manifests, freezes, checkpoints y results;

16 mínimos globales fresh;

incidente pre-training de schema;

claims/non-claims;

HALT explícito sobre 740x.

Después, 7401–7405 no se vuelven a utilizar para diseñar nada.

Siguiente fase científica: no recomiendo N=5 todavía

Hacer N=5 con exactamente un FLOOR, un AVOID, un MATCH, un ANCHOR y otro REFERENCE probablemente pase y añadiría evidencia de escala, pero conceptualmente sería más de la misma prueba.

El límite estructural más importante ahora es éste:

el binder supone un conjunto fijo de roles presentes
	​


En una instrucción real podrías tener:

sólo FLOOR;

FLOOR + MATCH;

AVOID + ANCHOR + FLOOR;

los cuatro;

etc.

El sistema actual produce una query para cada rol aunque ese rol no exista en la instrucción.

Por eso propondría:

T6-NROLE-ACTIVESET
	​

Pregunta científica de T6

¿Puede un único binder N-role determinar qué roles están presentes y enlazar correctamente sólo sus argumentos, componiendo subconjuntos de roles jamás vistos conjuntamente?

Eso elimina una dependencia bastante más seria que escalar de 4 a 5.

Punto crítico

No quiero que el evaluator reciba como side-channel:

active_roles = [...]

y simplemente ignore las queries ausentes.

Eso probaría binding condicionado a información externa, no parsing autónomo.

El binder debería producir por cada role:

ARG
i
	​

oNONE.
T6-DESIGN-AUDIT

La siguiente unidad científica que autorizaría después del cierre documental sería sólo:

T6-NROLE-ACTIVESET-DESIGN-AUDIT
	​


Cero training inicialmente.

Una opción limpia es añadir un candidato NULL/NONE por query:

j
r
	​

=argmax{s
r,1
	​

,…,s
r,T
	​

,s
r,∅
	​

}.

Entonces:

role presente → pointer al ARG correcto;

role ausente → pointer a NONE.

Lo interesante es que el dataset atomic ya contiene supervisión de ausencia.

En una fila atomic FLOOR:

F=present

pero:

A=M=H=absent.

Así pueden aprenderse presencia y binding sin ningún multi-clause training.

Esto preservaría:

multi_clause_train=0
	​

Test futuro de T6

Con N=4, probar todos los tamaños de active set:

k=1,2,3,4.

Número de subconjuntos no vacíos:

(
1
4
	​

)+(
2
4
	​

)+(
3
4
	​

)+(
4
4
	​

)=15.

Para cada subset de tamaño k, probar sus:

k!

órdenes.

Por ejemplo:

{F};

{F,M};

{A,H};

{F,A,M};

{F,M,H};

{F,A,M,H};

etc.

Y exigir simultáneamente:

present role⇒correct ARG
absent role⇒NONE.

Eso sería una propiedad cualitativamente nueva.

Por qué esto vale más que N=5

T5 ya muestra:

3→4

sin rediseñar el principio.

T6 respondería si el mecanismo puede pasar de:

fixed complete role set

a:

variable compositional active set
	​


que es mucho más cercano a un parser/binder real.

Después de eso, veo dos fronteras naturales:

T7: m
u
ˊ
ltiples instancias del mismo rol

por ejemplo dos MATCH diferentes en la misma instrucción —algo que el actual “una query por role” no puede representar directamente— y, más adelante, una batería de scaling N=5,6,….

Dirección
1. T5-FINAL-CLOSURE
	​


Luego:

2. T6-NROLE-ACTIVESET-DESIGN-AUDIT
	​


No usaría 740x para diseñar T6. T5-N4 ya está cerrado.