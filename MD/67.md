d4cbf33567986188b6cb131aca41fef34ad03d69 cuadra con tu reporte. La conclusión ya no es ambigua.

Estado formal

T2-NOBYPASS-A-B: CLOSED / DIAGNOSTIC PASS_STRONG

Auditoría A establece causalmente el bypass: en los 28 casos discriminativos, 28/28 siguen los lower/forbidden falsos inyectados al harness y 0/28 siguen el condition correspondiente a la instrucción real. Esto ya no es inferencia por lectura de código.

Auditoría B es todavía más destructiva para la interpretación anterior:

E
0
	​

=51/139
E
1
	​

=
139/139
	​

E
0
	​

+E
1
	​

=128/139.

Para el checkpoint 6301 y este consumidor, dos slots factorizados queda refutado. Añadir E0 a E1 degrada FLOOR y SUM.

Precisión: llamaría a E0 «ruido funcional para este downstream», no necesariamente información matemáticamente inútil. Pero definitivamente no es «la otra mitad de una representación FLOOR/AVOID factorizada».

Y sí: esto degrada otra vez COMP-0. En G3-calibration:

E
1
	​

solo=139/139

antes de necesitar Z.

Por tanto, en esos 139 casos Z no estaba resolviendo una composición que E1 fuera incapaz de expresar. Estaba reparando, al menos en buena parte, la decisión arquitectónica de obligar al consumidor a recibir E0+E1.

No diría todavía que todo el efecto de Z en los 853 se reduce a cancelar E0, porque todavía no hemos medido E1 sola allí.

Retiro VIEW-0 para el checkpoint 6301

Ya no HOLD.

T2-I3-VIEW-0 sobre Writer6301: CANCELLED / NOT JUSTIFIED.

VIEW-0 se diseñó para recuperar dos evidencias desde dos slots. Ahora sabemos que el objeto causal que estábamos intentando reparar no existe como creíamos: E1 por sí solo ya resuelve toda la calibration bajo el consumidor contaminado.

Construir un readout sofisticado sobre E0/E1 sería seguir optimizando una representación cuya interpretación quedó refutada.

Un último audit viejo sí vale la pena

No bloquea NOBYPASS, pero lo autorizaría porque cuesta prácticamente nada:

T2-NOBYPASS-B-ALL: AUTHORIZED / NON-BLOCKING

Cero training. Los 992 ya son development.

Medir en calibration+853:

Condition	Qué medir
E0	matching
E1	matching
B=E0+E1	matching
B+.25Z	matching

Especialmente quiero:

E1
853
	​

vs831/853 de COMP.

Si E1 sola da ≥831, la historia de COMP-0 queda prácticamente reducida a compensación del mal pooling de slots.

Si E1 da bastante menos de 831, entonces Z sí aprendió una transformación que aporta algo sobre distribución más amplia, aunque no sea «composición de dos evidencias».

No usar este audit para diseñar NOBYPASS.

Nueva campaña: T2-NOBYPASS

No reutilizaría Writer6301 como arquitectura principal. Su mecanismo interno ya está demasiado condicionado por el benchmark contaminado.

La campaña debe reconstruir primero una cadena limpia:

instruction→mode/reference representations→comparisons→CTRL
	​


con la regla absoluta:

L y F nunca entran al forward por fuera de la instrucci
o
ˊ
n
	​


El evaluador puede conocerlos para saber cuál es la respuesta correcta. El modelo, no.

T2-NOBYPASS-P0 — plumbing oracle-from-text

AUTHORIZED.

Cero training.

Antes de volver a aprender nada, construir run_learned_nobypass() cuya ruta de modelo no tenga argumentos:

lower
forbidden
constraints

Su entrada semántica será únicamente la instrucción tokenizada.

Para este P0 solamente, parse_instruction_i1() puede extraer determinísticamente operador y VALUE desde el propio texto. El parser actual ya devuelve constraints, lower y forbidden desde los tokens.

Después:

L
text
	​

→C
L
	​

F
text
	​

→C
F
	​


usando el codebook congelado, y:

cmp
F
	​

=scorer(Q(R),C
L
	​

)
cmp
A
	​

=scorer(Q(R),C
F
	​

).

Esto reproduce deliberadamente la maquinaria numérica de CTRL-7, pero ahora el único origen de las referencias es la instrucción.

La canonicalización existente ya define precisamente el codebook de 32 VALUE y cómo un vector se convierte en una entrada canónica.

P0 no demuestra aprendizaje. Demuestra que podemos eliminar el side channel sin romper el runtime.

Gate P0

Debe dar exactamente el comportamiento oracle previo.

Y exigiría dos controles causales inversos:

Metadata perturbation: mantener el texto fijo y cambiar lower/forbidden almacenados sólo en el evaluador. La trayectoria del modelo debe ser bit/acción-idéntica.

Instruction perturbation: mantener metadata externa fija y cambiar VALUE_12→VALUE_19 en el texto. El modelo debe seguir el 19 proveniente de la instrucción.

Si cualquiera falla:

HALT — todavía existe una fuga.

Si pasa, NOBYPASS-1 queda automáticamente autorizado.

T2-NOBYPASS-1 — learned argument binding

Aquí sí pondría la primera arquitectura entrenada.

No dos «semantic slots» genéricos.

Primero aislemos la capacidad que nunca probamos: binding operador↔argumento numérico.

Encoder mínimo

Entrada:

tokens=t
1
	​

,…,t
n
	​


Embedding:

e
t
	​

∈R
16

Estado local:

h
t
	​

=LN(SiLU(W[e
t−1
	​

,e
t
	​

,e
t+1
	​

]))

igual filosofía local de R3: nada de self-attention global.

Dos queries de rol:

a
t
F
	​

=softmax
t
	​

(q
F
⊤
	​

h
t
	​

)
a
t
A
	​

=softmax
t
	​

(q
A
⊤
	​

h
t
	​

)

y:

u
F
	​

=
t
∑
	​

a
t
F
	​

h
t
	​

,u
A
	​

=
t
∑
	​

a
t
A
	​

h
t
	​

.

Una misma proyección de valor para ambos roles:

r
F
	​

=W
v
	​

u
F
	​

r
A
	​

=W
v
	​

u
A
	​

.

Compartir W
v
	​

 es importante: obliga a separar:

cuál token pertenece a FLOOR/AVOID

de:

qué número representa ese token.

No quiero dos decoders independientes que puedan aprender códigos incompatibles.

Aparte, una pequeña salida de objetivo:

c=W
g
	​

pool(h)∈R
32

que reemplaza el viejo condition.

Entonces:

instruction→(c,r
F
	​

,r
A
	​

).

Y el feature builder queda:

cmp
F
	​

=scorer(Q(R),r
F
	​

)
cmp
A
	​

=scorer(Q(R),r
A
	​

).

Ningún L/F externo.

No pondría targets de valor en R0

Primero haría la versión científicamente más limpia:

T2-NOBYPASS-1-R0

Training puramente conductual.

El CTRL/scorer/executor permanecen congelados y el gradiente puede atravesar el scorer hacia r
F
	​

,r
A
	​

. No usaría canonical_value_view() dentro del training porque contiene argmax.

Loss:

L=L
action
	​


sobre las tareas atómicas/NOOP conocidas.

Nada de FLOOR+AVOID conjunto en R0.

Semilla nueva, por ejemplo 6701, 5000 updates, AdamW wd=0, LR lineal 1e-3→1e-5, batch 128. No sweep.

Gate crítico que evita un nuevo shortcut

Aunque entrenemos sólo conductualmente, en evaluación exigiría que las referencias aprendidas realmente decodifiquen el argumento usando el decoder congelado:

decode(r
F
	​

)=L

para todos los FLOOR presentes, y:

decode(r
A
	​

)=F

para todos los AVOID presentes.

No basta con end-task PASS.

Porque de lo contrario podríamos volver a descubrir otro código adversarial que sólo manipule el scorer.

También ejecutaría una segunda versión del end-task reemplazando:

r
F
	​

→Q(r
F
	​

),r
A
	​

→Q(r
A
	​

).

Si el modelo entendió el argumento, canonicalizar esas referencias no debe romper la tarea.

Ese sería el verdadero argument-binding gate.

Gates de NOBYPASS-1

Los fijaría así:

Gate	Criterio
NB0	static + behavioral bypass isolation
NB1	atomic/NOOP end-task exact
NB2	argument decode exact: FLOOR L y AVOID F
NB3	canonicalized-reference execution exact
NB4	joint FLOOR+AVOID development sobre los 992
NB5	fresh-domain held-out

Los 992 actuales en NB4 son development. Un 992/992 será útil para cerrar implementación, pero no se llamará generalización nueva.

Y aquí no usaría nuestro viejo matching identity/swap. Ya no tiene sentido.

Ahora tenemos roles explícitos funcionales:

r
F
	​

,r
A
	​

.

Podemos evaluar directamente si cada argumento está donde debe estar.

Si R0 pasa end-task pero falla decode

Eso sería otro hallazgo de no-identificabilidad.

No cambiaría arquitectura inmediatamente.

Pasaría a:

T2-NOBYPASS-1-R1

misma arquitectura, única modificación:

L=L
action
	​

+λL
ref
	​

,λ=1.

donde:

L
ref
	​

=CE(register_decoder(r
F
	​

,C),L)+CE(register_decoder(r
A
	​

,C),F)

sólo cuando el rol correspondiente está presente.

El register_decoder ya existe y compara contra el mismo codebook congelado.

Esto sería supervisión explícita de argumento, sí, pero sólo como training target. L/F seguirían completamente ausentes del forward en inference.

Si R1 pasa, podremos afirmar:

dado entrenamiento para reconocer valores, el sistema puede transportar esos argumentos desde la instrucción al comparador sin side channel.

No más que eso.

El verdadero nuevo held-out no puede ser G5 viejo

Aquí coincido con la corrección metodológica anterior.

Una vez que NOBYPASS funcione en los 992, crearía un benchmark nuevo después de congelar la arquitectura.

Mi propuesta es especialmente fuerte y barata:

T2-NOBYPASS-X — lexical cipher

Crear nuevos tokens:

ARG_00 ... ARG_31
OP_A
OP_B

y un mapping aleatorio fijo, por ejemplo:

ARG
i
	​

→VALUE
π(i)
	​


con una permutación π generada por una seed predeclarada y guardada en un manifest antes del entrenamiento.

OP_A y OP_B no tienen significado lingüístico previo.

Training nuevo:

OP_A ARG_i
OP_B ARG_i
NOOP ...

para que aprenda atómicamente qué operador y número representa cada símbolo.

Nunca:

OP_A ARG_i AND OP_B ARG_j

durante train/val.

Held-out:

todos los pares conjuntos válidos.

Ahora, si pasa:

texto nuevo→dos argumentos→comparaciones reales→composici
o
ˊ
n

sin que ninguna decisión de diseño haya sido adaptada a esos pares concretos.

Ése sí sería el primer resultado T2 que yo volvería a llamar generalización composicional limpia de argumentos.

Qué pasa con COMP/THINK después

No portaría ninguno todavía.

Primero observar:

NOBYPASS Writer/Binder→joint raw.

Sólo si falla, volver a hacer residual decomposition.

Si aparecen nuevamente fallos JOINT_ONLY, entonces un compositor separado vuelve a estar justificado, pero esta vez operando sobre representaciones que demostrablemente contienen L/F.

Si aparecen fallos de binding, se arregla binding.

Si pasa raw, COMP ni siquiera hace falta.

Así evitamos repetir el error de construir THINK/COMP/VIEW encima de una semántica interna que nunca habíamos establecido.

Resolución

T2-NOBYPASS-A-B: CLOSED / DIAGNOSTIC PASS_STRONG.

Bypass: PROVEN CAUSALLY.

Writer6301 two-slot factorization: REFUTED for the tested downstream.

VIEW-0/6301: CANCELLED.

COMP-0: historical result retained, interpretation downgraded; full-domain E1 ablation authorized non-blocking.

T2-NOBYPASS-P0: AUTHORIZED.

Si P0 elimina la fuga y reproduce el runtime:

T2-NOBYPASS-1-R0: AUTOMATICALLY AUTHORIZED bajo la arquitectura y gates anteriores.

Y no abriría ninguna otra rama — ni COMP, ni THINK, ni nuevas seeds de R3 — hasta saber una cosa que ahora sí es fundamental:

¿puede el modelo leer L y F de la instrucci
o
ˊ
n y usarlos sin que el harness se los entregue?
	​


Ése es el gate que faltó realmente desde que comenzó T2.