Confirmo T7 STAGE-B DEVELOPMENT: CLOSED/PASS y autorizo iniciar la etapa C sobre los cinco modelos 7701–7705. La autorización comprende aprender únicamente el embedding NOOP y verificar sus certificados locales; la evaluación multi-cláusula emparejada sigue en HOLD.

El commit fea9ea43f255… registra el sanity corregido mediante decode_states, la reutilización de las calibraciones sin cambios y los resultados 640/640 presentes, 1920/1920 ausentes y 640/640 instrucciones completas. Revisé el código versionado de la reparación; no he reejecutado tus comprobaciones ni recalculado los hashes.

T7 STAGE-B DEVELOPMENT: CLOSED / PASS
	​

T7-NOOP-NONE-LEXICAL / STAGE-C DEVELOPMENT: AUTHORIZED
	​

1. Punto de partida: los cores de A y los umbrales validados por B

Para cada seed, cargar su propio checkpoint final de Stage A y su propia calibración, validada por la reparación de B. Verificar la asociación mediante hashes de checkpoint, manifest y calibración.

No volver a entrenar el binder ni recalcular los midpoints. Los cuatro umbrales se leen por nombre de rol y se reconstruyen en el orden de las queries, utilizando exactamente los valores b_r_stored.

La nueva intervención es exclusivamente:

e
N
	​

∈R
16
.

Permanecen congelados los 37 embeddings anteriores, las cuatro queries, el transformador de keys, la normalización, W
v
	​

, el decoder/codebook y los cuatro b
r
	​

.

No cargar directamente el constructor sintético como modelo de campaña. El módulo de integración consultado todavía construye internamente un GenericNRoleBinder con un manifest sintético fijo. Para C, el adaptador debe recibir el core entrenado cargado desde A y los umbrales de B; puede reutilizarse el mecanismo AppendedNoopEmbedding, pero no reemplazar el modelo entrenado por esa inicialización sintética.

2. Incorporación de NOOP sin alterar el vocabulario anterior

Añadir una fila al final de la tabla:

37 filas base→38 filas.

El nombre del operador NOOP se obtiene del manifest de cada seed. No asumir que siempre es OP_Z.

Conservar todos los índices internos anteriores y registrar el mapping ampliado. La nueva fila tiene índice interno 37; eso no convierte al entero 37 en un valor semántico ni en un ID físico del corpus.

Inicializar únicamente e
N
	​

 con una normal estándar y un generador independiente:

seed
e
N
	​

	​

=100000+seed
dominio
	​

.

No inicializar desde el promedio de otros operadores, no probar varias inicializaciones y no reutilizar un embedding NOOP entre seeds. Esta inicialización independiente está precomprometida en el manifest.

La inferencia continúa usando el lookup y scorer ordinarios. No añadir detección especial de NOOP, eliminación de cláusulas, máscaras semánticas ni una quinta query.

3. Objective y presupuesto: exactamente lo sellado

Usar los 34 contextos completos en cada actualización:

Contexto	Cantidad	Posición supervisada
OP_NOOP→ARG_D	32	1: el argumento
START→OP_NOOP	1	0: el operador inicial
LINK→OP_NOOP	1	1: el operador

START conserva el prefijo cero del forward real. Para LINK→OP_NOOP se utiliza solamente el score de la posición del operador; no se añade START→LINK como negativo.

La única pérdida es:

L
C
	​

=
136
1
	​

r∈{F,A,M,H}
∑
	​

z∈U
∑
	​

softplus(s
r
	​

(z;e
N
	​

)−b
r
	​

).
	​


La matriz de diferencias debe tener forma [4,34]. No aplicar argmax dentro de la pérdida, no usar una pérdida numérica para D y no incorporar ejemplos de evaluación.

Elemento	Configuración
Parámetros optimizados	Sólo los 16 componentes de e
N
	​


Optimizador	AdamW
Weight decay	0
Actualizaciones	5000
Learning rate	Lineal 1e-3 → 1e-5
Datos por actualización	Los 34 contextos completos
Selección final	Última actualización; sin early stopping
Términos adicionales	Ninguno

Estas condiciones coinciden con la etapa C sellada: no hay pérdida de roles activos, pérdida de valores ni reajuste de umbrales.

Congelar el core no significa envolver el forward de entrenamiento en no_grad(). Debe conservarse el grafo desde los scores hasta e
N
	​

. El optimizador recibe exclusivamente ese parámetro; no basta con confiar en que los demás “normalmente no tengan gradiente”.

4. Controles antes y después del entrenamiento
C0 — carga, congelamiento y línea base

Antes del primer update, verificar la identidad del core, la calibración, el mapping y el único parámetro entrenable. Guardar el embedding inicial y medir la tabla completa:

S
r,z
initial
	​

=s
r
	​

(z;e
N
initial
	​

),

junto con la pérdida y los cuatro certificados iniciales:

G
r
initial
	​

=b
r
	​

−
z∈U
max
	​

S
r,z
initial
	​

.

Aunque la inicialización ya pase algún certificado, ejecutar igualmente las 5000 actualizaciones. No volver a sortear. Ese dato inicial permitirá distinguir lo que ya cumplía el embedding aleatorio de lo que cambia durante el aprendizaje.

Registrar el gradiente real de e
N
	​

 al inicio y al final. Si el parámetro está desconectado del grafo, o aparecen parámetros base dentro del optimizador, es un defecto de implementación y corresponde detenerse.

C1 — certificado local final

Al terminar, guardar la tabla final [4,34] y calcular:

G
r
NOOP→NULL
	​

=b
r
	​

−
z∈U
max
	​

s
r
	​

(z;e
N
final
	​

)>0.
	​


Deben pasar los cuatro roles en cada seed: 680 comparaciones finales por debajo de NULL entre los cinco modelos.

Registrar el contexto de máximo score, su posición, el score, el umbral y la holgura. No basta que baje la pérdida promedio: un único candidato por encima de NULL hace fallar ese certificado.

Si una familia falla, desglosar si corresponde a NOOP→ARG, START→NOOP o LINK→NOOP. No corregirla dentro de esta corrida ampliando capacidad, presupuesto o catálogo.

C2 — preservación del sistema base

Comparar antes y después de C los scores y values de los 128 ejemplos atómicos activos y los 40 contextos BG base, sin NOOP. Deben permanecer bit-exact bajo el mismo camino de cómputo.

Verificar también los controles atómicos estructurados ya aprobados: argumento correcto para el rol presente y NULL para los tres ausentes. Esta comprobación es read-only y no aporta ninguna pérdida a C.

El decoder/codebook aprobado de A/B queda explícitamente autorizado para esos controles. Utilizar decode_states como en la reparación de B; nunca argmax sobre los componentes de W
v
	​

.

C3 — controles atómicos NOOP

Evaluar las 32 instrucciones:

[OP
NOOP
	​

,ARG
D
	​

]

por seed. Las cuatro queries deben devolver NONE:

32/32 instrucciones por seed,
160/160 instrucciones y 640/640 decisiones NULL en total.

Las salidas se obtienen por la competencia ordinaria entre scores y umbrales. No imponer NULL porque el evaluador conoce la etiqueta NOOP.

Estos controles fueron utilizados para aprender el operador: no se presentan como generalización multi-cláusula.

5. Inmutabilidad: comparar correctamente lo que debe permanecer fijo

Los archivos originales de Stage A y las calibraciones de B no se sobrescriben.

Durante C deben permanecer idénticos los tensores base, las filas antiguas de embeddings y los umbrales. El archivo nuevo que contiene e
N
	​

 naturalmente tendrá otro hash.

No exigir que el estado ampliado de 38 filas tenga el mismo hash global que el core original de 37. Comparar la proyección del estado ampliado sobre los componentes base, con nombres normalizados si el wrapper los cambia, y hashear e
N
	​

 por separado.

Guardar la procedencia completa: core de A, calibración aprobada de B, manifest, inicialización NOOP, runner y estado final del embedding. No recalibrar ni reconstruir b
r
	​

 desde números redondeados del reporte.

6. Regla de decisión y límites

Ejecutar los cinco entrenamientos de C secuencialmente y sin cambios entre seeds. Un fallo científico no autoriza sustituir una seed ni modificar la receta; un error de integridad o implementación exige HALT operativo y preservar el intento.

Si los cinco cumplen los certificados locales, los controles atómicos y la preservación del sistema base:

T7 STAGE-C DEVELOPMENT: CLOSED / PASS
	​

.

Si alguno no consigue G
r
NOOP→NULL
	​

>0 bajo la receta fijada:

T7 STAGE-C DEVELOPMENT: VALID FAIL
	​

.

La conclusión quedaría limitada a este aprendizaje de 16 parámetros con este presupuesto, no a una imposibilidad general de representar NOOP.

Ni siquiera un PASS de C autoriza automáticamente la evaluación emparejada. Las instrucciones multi-cláusula base y aumentadas siguen selladas para esta unidad, y 780x permanece sin generar.

Guardar los resultados en un directorio exclusivo, por ejemplo:

campaign/t7_noop_none_lexical_stage_c_development/

La próxima entrega debe incluir hashes, las tablas inicial/final de scores, los cuatro certificados por seed, pérdidas y gradientes, controles atómicos y evidencia de inmutabilidad. Mantener ejecución sin delegación ni spawning.

Autorización explícita: iniciar ahora el aprendizaje de e
N
	​

 en 7701–7705, reutilizando sin cambios los cores de A y los umbrales aprobados de B. Únicamente etapa C y sus controles locales; ninguna recalibración ni evaluación multi-cláusula.