Confirmo T7 STAGE-B FRESH: CLOSED/PASS y autorizo iniciar únicamente la etapa C fresh sobre 7801–7805: aprendizaje del embedding NOOP con los cores y umbrales ya congelados. La evaluación emparejada continúa en HOLD.

Revisé el commit 5a4da7527b5f…, el runner y el registro de calibraciones. El runner importa directamente el sanity reparado, reconstruye el vocabulario mediante fresh_manifest_view() y verifica la asociación entre checkpoint, manifest y resultados de A. Mi revisión es del contenido versionado; no he reejecutado tu auditoría ni recalculado sus hashes.

T7 STAGE-B FRESH: CLOSED / PASS
	​

T7-NOOP-NONE-LEXICAL / STAGE-C FRESH: AUTHORIZED
	​

1. C utiliza los modelos existentes; no inicializa otro binder

Para cada seed, cargar su checkpoint final de A fresh y su calibración de B fresh, verificando los hashes y el mapping de ambos contra el manifest sellado.

Los umbrales se leen desde b_r_stored, por nombre de rol, y se ordenan como:

FLOOR, AVOID, MATCH, ANCHOR

No recalcularlos a partir de P
min
	​

 y N
max
	​

, ni reconstruirlos desde valores redondeados del chat. El registro de B conserva tanto los valores almacenados como su reconstrucción y las holguras posteriores a la lectura.

C no comienza con una nueva inicialización del core. Reutiliza exactamente el modelo de A correspondiente y modifica únicamente:

e
N
	​

∈R
16
.

Permanecen congelados los embeddings anteriores, las cuatro queries, el transformador de keys, la normalización, W
v
	​

, el decoder/codebook y los cuatro umbrales.

Se permite reutilizar código aprobado de development, pero no cargar embeddings NOOP, cores ni calibraciones de 770x.

2. Añadir NOOP conservando el vocabulario base

La tabla pasa de 37 a 38 filas, añadiendo NOOP al final. Utilizar los campos sellados de cada manifest:

noop_operator
stage_c_noop_internal_id
stage_c_noop_external_id
stage_c_token_order

El índice interno añadido es 37; todos los índices anteriores deben permanecer idénticos. No derivarlos del orden incidental del diccionario de IDs físicos.

Como comprobación concreta, el manifest 7801 reserva OP_W al final del vocabulario ampliado, con índice interno 37 e ID externo 52500. No asumir que NOOP siempre corresponde al mismo nombre de operador.

Inicializar sólo la fila nueva mediante un generador independiente:

e
N
initial
	​

∼N(0,I),seed
e
N
	​

	​

=100000+seed.

Corresponden las semillas 107801–107805. No probar varias inicializaciones, copiar un operador existente ni seleccionar la que mejor rechace argumentos.

La adaptación fresh del runner puede resolver rutas, esquema y carga del estado aprobado. No puede sustituir el core cargado por el constructor sintético de integración ni alterar su forward.

3. Pérdida y presupuesto: exactamente los precomprometidos

En cada actualización, usar los 34 contextos completos:

Familia	Cantidad	Posición cuyo score se supervisa
OP_NOOP→ARG_D	32	1: el argumento
START→OP_NOOP	1	0: el operador inicial
LINK→OP_NOOP	1	1: el operador

START sigue siendo el prefijo cero del forward real. Para LINK→OP_NOOP, no supervisar accidentalmente la posición LINK ni añadir START→LINK.

La única pérdida será:

L
C
	​

=
136
1
	​

r∈{F,A,M,H}
∑
	​

z∈U
∑
	​

softplus(s
r
	​

(z;e
N
	​

)−b
r
	​

).
	​


La matriz tiene forma [4,34]. No añadir pérdidas numéricas para el argumento D, términos de las tareas activas, regularizadores ni márgenes auxiliares.

Elemento	Configuración
Parámetros optimizados	Sólo los 16 componentes de e
N
	​


Optimizador	AdamW
Weight decay	0
Actualizaciones	5000
Learning rate	Lineal 1e-3 → 1e-5
Datos por actualización	Catálogo completo de 34 contextos
Selección final	Última actualización, sin early stopping

Esta receta, incluida la inicialización independiente, está registrada en los manifests fresh.

Congelar los parámetros del core no significa desactivar el grafo del forward de entrenamiento. Debe conservarse la conexión desde la pérdida hasta e
N
	​

; el optimizador recibe exclusivamente ese parámetro.

El forward de inferencia sigue recibiendo sólo token_ids,lengths. No introducir eliminación de cláusulas, una máscara por identidad NOOP, una quinta query ni un atajo que devuelva directamente NONE.

4. Controles iniciales y gates finales
C0 — identidad y línea base

Antes del primer update, guardar el embedding inicial, verificar el conjunto exacto de parámetros optimizables y registrar la tabla inicial [4,34], la pérdida y los cuatro certificados:

G
r
initial
	​

=b
r
	​

−
z∈U
max
	​

s
r
	​

(z;e
N
initial
	​

).

No exigir que la inicialización falle. Si todos los certificados ya son positivos en alguna seed, registrarlo y ejecutar igualmente las 5000 actualizaciones. No repetir el sorteo para obtener un caso “más demostrativo”.

Registrar el backward real y la conexión con e
N
	​

. Un parámetro desconectado o parámetros base incluidos en el optimizador son defectos de implementación. No convertir el número de componentes de gradiente no nulos observado en development en un nuevo gate fresh.

C1 — rechazo local final

Después de la última actualización:

G
r
NOOP→NULL
	​

=b
r
	​

−
z∈U
max
	​

s
r
	​

(z;e
N
final
	​

)>0
	​


para los cuatro roles y las cinco seeds.

Eso supone 20 certificados finales, construidos sobre 680 comparaciones score-vs-NULL. Guardar la tabla completa inicial/final y, por certificado, el score máximo, contexto, posición, umbral y holgura.

El gate no es que disminuya la pérdida media. Un solo candidato que alcance o supere NULL incumple el certificado correspondiente.

No exigir reproducir las holguras de 770x: el criterio permanece estrictamente >0.

C2 — preservación del sistema base

Comparar antes y después de C los scores y values de las 128 filas atómicas activas y los 40 contextos BG base. Bajo el mismo camino de ejecución, deben permanecer bit-exact.

Repetir además los controles atómicos estructurados aprobados:

640/640 presentes correctos,
1920/1920 ausentes correctamente NULL,
640/640 instrucciones completas.

Estas comprobaciones son read-only; no aportan pérdida a C.

El decoder/codebook congelado utilizado en A/B queda explícitamente autorizado. Usar decode_states(executor, codebook, selected), nunca argmax sobre los componentes de W
v
	​

. Canonicalizar sólo una presencia predicha y con el índice decodificado, no con el target del evaluador.

C3 — controles atómicos NOOP

Evaluar las 32 instrucciones [OP_NOOP, ARG_D] por seed. Las cuatro queries deben elegir NULL mediante sus scores y umbrales ordinarios:

160/160 instrucciones completas,640/640 decisiones NULL.

El evaluador no debe imponer ausencia porque conoce la etiqueta NOOP. Estos son controles sobre datos utilizados para aprender el operador, no evidencia de composición multi-cláusula no vista.

5. Inmutabilidad y resultados

Conservar intactos los archivos de A, las calibraciones B y los manifests.

Al comparar estados, separar:

componentes base congelados

de:

fila nueva e
N
	​

.

El estado ampliado no tendrá el mismo hash global que el core original, porque contiene un parámetro adicional. Verificar la igualdad del estado base proyectado —normalizando nombres del wrapper si hace falta— y hashear e
N
	​

 por separado.

Los umbrales deben permanecer idénticos antes y después, tanto en sus archivos como en los valores utilizados. No recalibrar B al terminar C.

Guardar todo en un directorio fresh exclusivo, por ejemplo:

campaign/t7_noop_none_lexical_stage_c_fresh/

La entrega debe incluir las identidades de las entradas A/B, el hash del runner ejecutado, embeddings iniciales/finales, tablas de scores, certificados, pérdidas y gradientes, controles atómicos y comprobaciones de inmutabilidad. Mantener escritura binaria y verificación de los bytes guardados.

6. Decisión y alcance

Ejecutar los cinco entrenamientos secuencialmente, sin cambios entre seeds y sin delegación ni spawning.

Si una seed falla científicamente su certificado, completar las demás bajo la misma receta y reportar el fallo. No cambiar el presupuesto, desbloquear parámetros, mover NULL ni sustituir seeds.

Ante una discrepancia de carga, mapping, integridad o implementación, detener la ejecución y preservar el intento como INVALID / HARNESS BUG.

Si los cinco pasan todos los gates:

T7 STAGE-C FRESH: CLOSED / PASS
	​

.

Esto todavía no cierra T7 fresh como PASS_STRONG. Ese veredicto requiere después la evaluación emparejada sobre las instrucciones completas.

Autorización concreta: iniciar ahora C en 7801–7805, aprendiendo únicamente e
N
	​

 sobre los cores de A y los umbrales de B ya congelados. No abrir evaluación multi-cláusula ni continuar automáticamente a ella aunque C pase.