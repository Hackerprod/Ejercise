T0-M t0m_int8_probe.cpp A/B R=1 conditional audit
scope=timed A/B path; source not edited

BEFORE TIMER / SETUP
L142-L145 parse_mode branches on --mode; input parsing only, before workload and timer.
L148-L152 parse_variant branches on --variant; input parsing only, before workload and timer.
L163-L180 option-dispatch branches consume D/S/R/mode/variant/S_tile; before workload and timer.
L199-L205 validate S and S_tile domains; before workload and timer.
L268 variant == B selects block_count = R for B, otherwise 1; before timer. At R=1 both values are 1, but predicate still executes and B/A workload construction is selected by runtime variant.
L476-L477 run_shard depth loop is reached by A/B warmup execute_repetition at L609-L610 only; R-dependent warmup, before timer. Timed A/B path calls run_shard_depth directly instead.
L608-L618 warmup loop and its C/non-C branch; before timer. A/B use L609 false and L610 execute_repetition.
L620-L621 non-C A/B selection and start barrier occur before first timed phase; same outcome for A and B.

PER-KERNEL HOT / INSIDE TIMED A/B KERNEL WORK
L360-L361 run_fused slot_base loop and min(slot_tile, slots - slot_base); S and S_tile dependent, repeated in kernel execution. At S=1, S_tile=8, one slot chunk and slot_count=1.
L376-L376 run_fused slot_offset loop; S/S_tile dependent, hot kernel loop. Same count for A/B at requested S/S_tile.
L387-L389 run_fused output slot loop; S dependent, hot kernel loop. Same count for A/B at requested S.
L401-L401 run_repeat_gemv slot loop; S dependent, hot kernel loop. Not selected by requested mode=fused.
L460-L460 run_shard_depth variant == B selects blocks[depth_index] for B or blocks[0] for A; per-kernel hot and inside timed A/B path. At R=1 depth_index=0, both select logical block index 0, but condition is runtime variant-dependent and can produce different selection instructions/control flow.
L461-L465 run_shard_depth mode branch selects run_fused or run_repeat_gemv; per-kernel hot and inside timed A/B path. Requested mode=fused gives same branch outcome for A/B.
L622-L631 timed worker repetition/iteration/depth loops, phase_ready, shard loop, run_shard_depth, phase_done; per-depth timed. R controls L624 depth count; requested R=1 gives one synchronized phase per repetition.

PER-DEPTH TIMED TIMER LOOP
L663-L671 main timer's non-C branch, timed_units = timed_repetitions * iterations * R, and phase loop around phase_ready/phase_done; per-depth timed for A/B. At R=1, 8 timed units.
L691-L696 MAC total/rate uses R and repetitions after timing; accounting, not a branch or kernel instruction path.

C-ONLY CONDITIONS / PATHS
L586-L588 cold_variant = (variant == C) and eviction allocation; C-only branch/setup.
L609-L617 warmup C branch and cold passes; C-only arm is L612-L616.
L620-L655 non-C A/B timed worker arm versus C cold worker arm; L636-L655 C-only, including eviction touch/flush and cold barriers.
L663-L684 non-C A/B timer versus C timer; L674-L684 C-only cold timing path.
L699 variant == C sets eviction_bytes after timing; output metadata only.

POST-TIMER / NON-TIMED VARIANT CONDITIONS
L546 bytes_for_worker variant == B ternary; reporting after timed execution.
L729 mode_name mode ternary; CSV formatting after timing.
L730-L731 variant_name variant ternary; CSV formatting after timing.
L447 run_reference variant == B selection; self-test/reference path, not timed A/B path.

R=1 A/B INSTRUCTION CONCLUSION
For requested D=512, S=1, R=1, mode=fused, S_tile=8, iterations=1, A and B take same non-C orchestration, same R/S loop counts, and same fused kernel call. Workload block_count is also 1 for both and depth selection resolves to block index 0 for both. However, L460 remains a runtime variant conditional inside every timed shard-depth call; source does not prove identical instruction execution for A versus B. A/B can execute different branch/selection instructions there, even though logical block index and seeded block contents match at R=1. No source-level condition makes B execute a different fused/repeat kernel at this configuration.
