Confirmo T7 STAGE-C FRESH: CLOSED/PASS y autorizo la evaluación emparejada fresh sobre los cinco modelos 7801–7805 ya congelados. Ése es el siguiente paso para determinar PASS_STRONG; no hace falta otra intervención de entrenamiento.

Revisé el commit 2ae31217becf… y el runner de C. El código carga las referencias A/B, optimiza exclusivamente embedding.noop_embedding y comprueba la preservación del estado base y los controles atómicos mediante el sanity reparado. La recalculación completa de resultados y hashes es la que reportaste; no he reejecutado esa auditoría.

T7 STAGE-C FRESH: CLOSED / PASS
	​

T7-NOOP-NONE-LEXICAL / PAIRED FRESH EVALUATION: AUTHORIZED
	​


Esta unidad permite exclusivamente cargar, verificar y evaluar los modelos finales. Cero entrenamiento, cero recalibración y cero cambios de arquitectura.

1. Evaluar el estado final de C, sin reconstruir otra intervención

Para cada seed, utilizar su checkpoint final en:

campaign/t7_noop_none_lexical_stage_c_fresh/
    training/seed_<seed>/final.pt

Verificar sus referencias al checkpoint de A, a la calibración de B y al manifest fresh sellado. C guarda expresamente esos hashes, el vocabulario ampliado, el mapping de NOOP y el vector de umbrales utilizado.

Cargar el e
N
	​

 aprendido, no volver a sortearlo ni ejecutar la inicialización de C. Los cuatro umbrales se recuperan de b_r_stored, por nombre de rol y en el orden:

FLOOR, AVOID, MATCH, ANCHOR

Deben coincidir exactamente con los registrados en el checkpoint de C.

Mantener el vocabulario ampliado de 38 filas: los 37 índices anteriores sin cambios y NOOP en el índice interno 37. Resolver su nombre e ID externo desde el manifest y la metadata guardada, no suponiendo un operador fijo.

El decoder/codebook aprobado de A/B/C queda explícitamente autorizado. RAW y CANON siguen utilizando decode_states(executor, codebook, selected). No reaparece argmax sobre los componentes de W
v
	​

, ni canonical = decoded.

El adaptador fresh puede cambiar rutas y manejar el esquema correspondiente. No puede cambiar el forward, las fórmulas de generación, el readout ni los criterios de aceptación. Registrar el hash del runner efectivo antes del primer forward de evaluación.

2. Ejecutar exactamente el conjunto sellado

Usar el evaluador corregido y la referencia de conformidad, no la variante histórica de +2 fijo.

Las asignaciones permanecen:

v
2
	​

=(seed+v
0
	​

+v
1
	​

+1)mod32,
v
3
	​

=(seed+v
0
	​

+v
1
	​

+v
2
	​

+2)mod32,
D=(seed+∑v
active
	​

+1)mod32,

incrementando módulo 32 hasta evitar las coincidencias previstas en el contrato.

Mantener el orden canónico del registry para asignar argumentos, la conversión mediante la permutación inversa VALUE→ARG y la permutación posterior de cláusulas completas. D permanece fijo para todas las órdenes de una misma asignación.

Antes de evaluar el modelo, el checker independiente debe verificar el conjunto lógico completo que consumirá el runner: asignaciones exactas, mapping, distractor, unicidad, órdenes y parejas. No basta comparar los totales declarados, ni usar la misma función generadora como referencia independiente de su propia fórmula.

Conteos obligatorios
Roles activos k	Base por seed	Con NOOP por seed
1	128	256
2	11 904	35 712
3	23 808	95 232
4	23 808	119 040
Total	59 648	250 240

En los cinco dominios:

298240 evaluaciones base
	​

1251200 evaluaciones aumentadas
	​

1549440 evaluaciones totales
	​

.

Las 1 251 200 comparaciones de parejas reutilizan las salidas base correspondientes; no son forwards adicionales.

Separar los 640 controles base singleton de las 297 600 bases multi-cláusula. Los controles atómicos NOOP ya ejecutados en C tampoco se suman nuevamente como evidencia composicional.

3. Tres condiciones distintas deben cumplirse por pareja

Para cada instrucción aumentada X
N
	​

, identificar la base X que conserva el orden relativo de las cláusulas activas y exigir:

Output(X)=Target(X),
Output(X
N
	​

)=Target(X),
Output(X
N
	​

)≡Output(X).

Dos salidas igualmente equivocadas no constituyen invariancia correcta.

La eliminación de NOOP para identificar la pareja ocurre solamente en el checker/evaluador. El modelo recibe el texto aumentado completo, incluida la cláusula distractora.

Se permite evaluar cada base una sola vez y reutilizar su resultado para sus parejas. Todas las instrucciones aumentadas deben ejecutarse realmente, no deducirse de los certificados locales.

Comparar las firmas semánticas por rol: presencia, argumento seleccionado y valores RAW/CANON. Los índices posicionales absolutos del pointer pueden cambiar legítimamente al insertar o reordenar cláusulas; cada pointer se comprueba contra la posición correcta de su propia instrucción.

4. Métricas y márgenes

Las cuatro queries se ejecutan siempre. El forward recibe únicamente token_ids,lengths, sin etiquetas de conjunto activo, máscaras semánticas o supresión de NOOP.

Roles presentes

Exigir presencia, pointer al argumento de ese rol, decode correcto y RAW/CANON correctos.

Los márgenes deben satisfacer:

M
r
present→NULL
	​

=s
r
	​

(target)−b
r
	​

>0,
M
r
present→real
	​

=s
r
	​

(target)−
t

=target
t<length
	​

max
	​

s
r,t
	​

>0.

El segundo máximo incluye todos los competidores reales: otros argumentos, operadores, conectivos y candidatos NOOP.

Roles ausentes

Exigir NONE explícito y:

M
r
absent
	​

=b
r
	​

−
t<length
max
	​

s
r,t
	​

>0.

No decodificar un vector cero para representar ausencia ni utilizar codebook[-1]. Si el modelo predice presencia incorrectamente, contabilizar el falso positivo; el evaluador no puede corregirlo usando la etiqueta verdadera.

Rechazo de NOOP e invariancia de orden

Registrar también el margen de los candidatos NOOP reales frente a cada umbral, con el contexto y la posición que producen el mínimo. Conservar como referencia los certificados locales finales de C, sin optimizarlos ni recalibrarlos.

Reportar resultados por seed, rol, subconjunto, tamaño activo y orden. Para k=4, las métricas condicionadas a ausencia son not_applicable.

La métrica principal es la instrucción estructurada completa: las cuatro decisiones correctas simultáneamente. Comprobar además equivalencia semántica entre todas las órdenes de una misma asignación.

No introducir un mínimo de margen aprendido de development. Todos los márgenes requeridos deben ser estrictamente positivos; no necesitan reproducir su magnitud anterior.

5. Inmutabilidad y evidencia

Ejecutar en modo evaluación, sin backward ni optimizador. Conservar el manejo aprobado de longitudes variables y padding.

Verificar antes y después los hashes de los checkpoints A/C, calibraciones B, manifests, estado base, e
N
	​

, umbrales, runtime y codebook. No sobrescribir ningún artefacto previo.

Usar un directorio exclusivo, por ejemplo:

campaign/t7_noop_none_lexical_paired_fresh/

Guardar evidencia por caso que permita recalcular los resultados: identificador de caso y pareja, tipo base/aumentada, salidas y expectativas por rol, firma semántica, corrección completa y márgenes. Conservar todos los fallos si aparecen, no sólo una muestra.

Los JSONL pueden permanecer fuera de Git si su tamaño lo requiere. Registrar sus bytes, SHA-256 y conteos de filas, junto con el script de auditoría independiente y su resultado. Los hashes identifican los archivos; no sustituyen su contenido para recalcular la evidencia.

Mantener escritura binaria y comprobación del self-hash sobre los bytes guardados, separándolo del SHA-256 del archivo completo.

6. Veredicto final predeclarado

Ejecutar los cinco dominios secuencialmente, sin delegación ni spawning. Un fallo científico no autoriza cambiar nada entre seeds; completar la evaluación prevista y conservarlo. Ante un defecto de implementación o integridad, detenerse y preservar el intento como INVALID / HARNESS BUG.

Sólo si los cinco dominios cumplen simultáneamente:

298240/298240 bases correctas,
1251200/1251200 aumentadas correctas,
1251200/1251200 parejas correctas y equivalentes,

con cero falsos positivos/negativos, RAW/CANON correctos, márgenes estrictos e inmutabilidad completa:

T7-NOOP-NONE-LEXICAL-FRESH: PASS_STRONG
	​

.

Ese eventual resultado validará la receta completa en cinco dominios fresh: un binder entrenado atómicamente, NULL calibrado por catálogo y un operador NOOP aprendido explícitamente mediante 16 parámetros adicionales. No sería evidencia de rechazo espontáneo de un operador desconocido ni de independencia estadística entre todas las filas enumeradas.

Autorización concreta: abrir y ejecutar ahora la evaluación emparejada fresh sellada sobre los cinco checkpoints finales de C. Sin entrenamiento, sin recalibración, sin nuevas seeds y sin modificar la receta.