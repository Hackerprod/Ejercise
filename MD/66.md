Sí. Esto cambia materialmente el alcance de T2-I2/T2-I3. La respuesta corta es:

En T1 era intencional. En T2 heredamos correctamente esa interfaz a nivel de código, pero empezamos a atribuirle a los experimentos una capacidad —extraer/bindear los argumentos numéricos desde la instrucción— que el consumidor nunca obligó al workspace a demostrar.

No lo llamaría un “bug de código”. Lo llamaría un scope leak experimental.

T1: sí, era deliberado

En CTRL-5 se escribió explícitamente que la referencia numérica b permanecía disponible en ambos modos y que lo que debía aprender el nuevo descriptor era cuándo utilizarla. Incluso se diseñó el control de variar b bajo FETCH para demostrar que el supervisor podía ignorar una referencia presente.

CTRL-7 mantiene ese contrato. supervisor_features_ctrl7() recibe lower y forbidden como enteros, construye directamente sus embeddings canónicos y calcula:

floor_comparison=scorer(Q(R),Q(lower))
avoid_comparison=scorer(Q(R),Q(forbidden)).

Esos dos comparadores ya vienen hechos dentro de observation.

Después, LatentConditionedSupervisor hace esencialmente:

y=W
out
	​

SiLU(W
obs
	​

o+c)

donde c es la condición de la instrucción.

Así que T1 nunca pretendió demostrar que b se extrajera de lenguaje o del workspace. T1 preguntaba si un controlador podía aprender a usar una referencia numérica conocida según un objetivo aprendido. Eso sigue siendo válido.

Donde cambia la historia: T2

El documento que abrió T2 dice explícitamente que ahora había que separar:

semántica del operador, argumento numérico y orden superficial.

Y lo formula como pasar de semántica ya estructurada a construir esa estructura desde una instrucción.

Pero la implementación sólo retiró una parte de la semántica estructurada: los bits FLOOR/AVOID.

No retiró:

L,F

del camino auxiliar.

Eso se ve clarísimo en el matching de I2: Adapter sustituye los bits de constraints por el condition real del Writer, sí; pero después cada llamada a run_learned() sigue recibiendo explícitamente lower y/o forbidden.

Incluso Baseline A de T2 revela retrospectivamente el contrato que realmente implementamos: la red lee textos que contienen VALUE_n, pero sus únicos heads predicen dos bits, FLOOR y AVOID. Nunca tiene un head para recuperar el argumento numérico.

Por tanto, hay una discrepancia real entre:

lo que dijimos que T2 probar
ı
ˊ
a

y

lo que los gates obligan al modelo a transmitir.
Qué están midiendo realmente G1/G2/G3/G4

Opus llama al condition “canal de 2 bits”. Lo matizaría un poco.

El condition son 32 floats y puede contener información correlacionada con L/F. No hemos demostrado que literalmente sólo contenga 2 bits.

Pero sí podemos afirmar algo más importante:

para resolver el benchmark, no necesita transmitir L ni F
	​


porque las comparaciones contra L y F ya están presentes en observation.

Una estadística suficiente desde la instrucción puede ser aproximadamente:

{NONE,FLOOR,AVOID,BOTH}

o sea, funcionalmente cuatro modos.

Esto cambia muchísimo la interpretación de los gates.

G1 demuestra que una condición derivada de la instrucción puede activar correctamente una semántica atómica cuando el comparador correspondiente ya conoce el target. G2 demuestra propiedades del reparto/neutralidad de ese código condicionante, no que los slots contengan argumentos numéricos recuperables. G3/G4 demuestran composición/generalización del código de control de los modos sobre distintas instrucciones (L,F), pero no que el sistema haya leído L y F del texto para efectuar las comparaciones.

Y hay una consecuencia aún más fuerte para nuestro viejo “matching de slots”.

Una misma condición genérica podría pasar como FLOOR y como AVOID porque el harness cambia el observation:

test FLOOR  -> comparador FLOOR contiene el target real
test AVOID  -> comparador AVOID contiene el target real

El slot no necesariamente tiene que decir:

“yo represento FLOOR-17”.

Puede estar diciendo algo mucho más pobre:

“usa la comparación activa”.

Eso explica mucho mejor anomalías como un mismo slot que parece aprobar ambas semánticas.

Así que sí: nuestra interpretación de factorization fue demasiado fuerte.

Esto también cambia cómo leo a Opus

Su punto 1, la separabilidad exacta de R3, sí puedo verificarlo independientemente sin confiar en su NumPy.

R3 calcula cada estado local con ventana [-1,0,+1], hace routing por token y al final suma escrituras. Ningún estado local puede ver simultáneamente la identidad de VALUE_L y VALUE_F; por construcción, cada salida tiene la forma:

E
s
	​

(L,F)=f
s
	​

(L)+a
s
	​

(F).

No puede existir un término cruzado dentro del Writer.

Así que ese hallazgo de Opus es correcto estructuralmente.

Sus números de rank-1 —cos=-.9914, singular values, 93/7% de varianza, etc.— no los considero todavía verificados independientemente. Pero si son correctos, encajan de manera inquietantemente buena con lo que ya observábamos: gran asimetría identity/swap y un slot que sirve para múltiples tests.

Y haría una precisión a otra afirmación suya: COMP es la primera pieza que introduce interacción L,F dentro del encoder de instrucción. No es la primera interacción del sistema completo, porque el supervisor posterior es no lineal y combina condition con observations.

COMP-0: no lo descartaría, pero sí lo renombraría conceptualmente

Para mí éste es el punto más importante.

Los resultados de COMP-0 siguen siendo reales:

783/853→831/853

con +48 correcciones y 0 regresiones en una ventana moderada de α.

Pero ya no diría:

“COMP aprendió a componer dos evidencias numéricas almacenadas”.

Diría:

COMP aprendió una corrección de interacción sobre el código latente de la instrucción que ayuda al consumidor a seleccionar correctamente la combinación FLOOR+AVOID.

Eso es más estrecho, pero sigue siendo un hallazgo válido.

Además, la separabilidad exacta de R3 hace que el resultado de COMP sea incluso causalmente limpio:

f(L)+a(F)⟶f(L)+a(F)+Z(L,F).

El residual Z realmente añade algo que el Writer aditivo no puede expresar.

COMP-0 permanece respaldado como mecanismo de composición de control.

No como demostración de composición de valores/memoria.

VIEW-0: aquí sí cambio mi decisión

Retiro la autorización inmediata de VIEW-0.

Lo pondría:

T2-I3-VIEW-0: HOLD / NOT AUTHORIZED

Porque después de descubrir el bypass, frases que usamos como:

“B recupera la evidencia AVOID”

ya no están justificadas.

Lo que realmente vimos en 21/22 fue:

B produce una condición que hace que CTRL-7 utilice correctamente el comparador AVOID que ya conoce F=10.

Eso no demuestra que B contenga 10, ni siquiera que pueda reconstruirlo.

Y el caso bcb26b5b tampoco prueba pérdida del argumento numérico. Sólo prueba que E0, E1 y B no generan el selector adecuado para utilizar correctamente el comparador oracle en ese estado.

Por tanto, toda nuestra narrativa reciente de:

evidence→VIEW

estaba basada en una semántica más fuerte de los slots que los gates no establecieron.

VIEW-0 podría seguir siendo útil como readout del código de modo, pero eso ya no es suficientemente importante para justificar otra campaña antes de corregir el benchmark.

F=10 también baja mucho de categoría

Aquí Opus me hace reconsiderar.

Nuestro F10-BIND ya demostró que:

no hay routing colapsado;

VALUE_10 funciona atómicamente;

F=10 sólo es un outlier moderado.

Ahora, con el bypass conocido, el hecho de que los 22 se concentren en F=10 no demuestra una pérdida especial de la evidencia F=10.

La hipótesis de Opus —un checkpoint donde una variación pequeña de la condición asociada a cierto F cae del lado incorrecto de una frontera— es perfectamente plausible.

No la considero demostrada hasta verificar sus estadísticas rank/variance o nuevas seeds.

Pero sí cambio el estado a:

“F=10-specific semantic defect”: NOT ESTABLISHED.

Además, 6302/6303 nunca llegaron a entrenarse: el protocolo cerró R3 después del FAIL de 6301 y explícitamente no las ejecutó. Entrenarlas ahora sería un buen estudio de estabilidad entre seeds, pero no un nuevo test independiente; estaría motivado por haber visto el fenómeno F=10.

G5 tampoco arregla este problema

Opus es algo demasiado categórico cuando dice que G5 no aporta nada independiente, pero el núcleo de su crítica es correcto.

La especificación de G5 reserva cosas que G4 normal no mide: interacción L=F, aliases conjuntos, orden invertido, real battery y crossing.

Así que G5 sí puede medir transferencia a superficies/orden/contextos de ejecución.

Pero no puede demostrar extracción de argumentos numéricos mientras siga haciendo:

lower,forbidden→oracle comparisons→observation.

Y varias de esas dimensiones —aliases, orden, real/interacción/crossing— ya fueron usadas repetidamente durante el desarrollo de I2. Por ejemplo, el matching anterior ya ejecutaba aliases, ambos órdenes y categorías real/interacción/crossing.

Así que:

G5 ya no debe tratarse como el gran test independiente de la hipótesis de workspace.

Yo lo mantendría sellado.

Qué haría ahora

No seguiría optimizando los 22.

Antes de VIEW-0 haría un cambio de nivel experimental.

Una sola campaña nueva debería eliminar el bypass:

instruction→workspace→
L
^
,
F
^
,
mode
^
→comparadores→CTRL.

lower y forbidden pueden seguir existiendo en el evaluador/oráculo para calcular la respuesta correcta, obviamente, pero deben desaparecer completamente del forward del modelo.

Podemos reutilizar casi toda la maquinaria ya validada. El ordinal scorer ya sabe comparar:

Q(R)vsQ(target).

La diferencia sería que Q(target) tendría que proceder de una vista/reader del workspace, no de:

Python
Run
token_embedding(VALUE_BASE + lower)

generado desde ground truth.

Eso produciría una pregunta mucho más limpia:

¿Puede la instrucción depositar un argumento numérico en el workspace de forma que el controlador pueda compararlo con R sin volver a recibir ese argumento por otro canal?

Ahí sí estamos testeando lo que creíamos que T2 ya estaba testeando.

Antes incluso de entrenarlo, haría un audit congelado barato: mantener exactamente el mismo condition generado por una instrucción y cambiar sólo los lower/forbidden que recibe el harness. Si las decisiones siguen los valores falsos del harness, queda documentado conductualmente —no sólo por inspección de código— cuánto depende el modelo del bypass.

También correría el control que propuso Opus:

condition=E
0
	​

,E
1
	​

,E
0
	​

+E
1
	​


sobre development. Si E1 solo reproduce casi todo, cerramos definitivamente la interpretación de “dos slots factorizados” del checkpoint 6301.

Mi revisión formal después de este reporte

T1-CONTROL: permanece VALID/PASS_STRONG dentro de su alcance original. La referencia numérica externa era deliberada.

T2-I2/T2-I3: SCOPE CORRECTION REQUIRED. No demostraron argument binding/value memory; demostraron instruction-conditioned control con comparadores oracle.

COMP-0: permanece VALID como hallazgo microarquitectónico, pero reinterpretado como composición de un código de control, no de evidencia numérica.

VIEW-0: HOLD / autorización retirada por ahora.

Residual F=10: reinterpretado como geometría/readout de este Writer hasta demostrar lo contrario, no “pérdida de VALUE_10”.

G5: SEALED, porque ejecutarlo ahora no responde la pregunta importante.

Siguiente gate que yo priorizaría: T2-NOBYPASS, donde ningún argumento numérico ground-truth entra al modelo salvo como etiqueta del evaluador.

Sobre la crítica más amplia de Opus a OMEGA, coincido en lo esencial: T2-I2/I3, tal como están implementados, no son evidencia de que un núcleo pequeño + memoria sustituya capacidad, porque el canal numérico difícil ya estaba resuelto fuera del workspace. Sí dejan dos hallazgos útiles: escritura aditiva y composición separada son mejores inductive biases que pooling normalizado y reescritura destructiva.

Una cosa de Opus sí está desfasada: el T0 hardware/cache sí se ejecutó y cerró en esta campaña; lo que falta ahora no es demostrar que la residencia/cache puede comprar rendimiento, sino conectar ese hallazgo con una tarea donde la información difícil realmente tenga que atravesar memoria/workspace en vez de entrar por un side channel.