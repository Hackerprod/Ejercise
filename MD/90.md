T2-NOBYPASS-1-STAGED-NBF0: CLOSED / PASS_STRONG según el artifact que verificaste.

Y sí:

NB3-CANON: AUTHORIZED
	​


No cambiaría absolutamente nada del pipeline antes de ejecutarlo.

Hay una condición crítica de implementación: la referencia canónica debe derivarse exclusivamente del propio output aprendido, no de los labels del evaluator. Es decir:

L
^
=decode(r
F
	​

),
F
^
=decode(r
A
	​

)

y después:

r
F
canon
	​

=C
L
^
	​

,r
A
canon
	​

=C
F
^
	​

.

No se permite hacer:

C
L
	​

, C
F
	​


usando lower/forbidden ground-truth aunque sólo sea “para canonicalizar”, porque eso reabriría exactamente el bypass que acabamos de eliminar.

Contrato exacto de NB3

Sobre los mismos 139 joint development, en normal y reverse, ejecutar dos rutas desde la misma inferencia de texto:

RAW

r
F
	​

,r
A
	​

→scorer/runtime

CANON

r
F
	​

,r
A
	​

→
L
^
,
F
^
→C
L
^
	​

,C
F
^
	​

→scorer/runtime.

Exigiría en cada orden:

decode(r
F
	​

)=L: 139/139
	​

decode(r
A
	​

)=F: 139/139
	​

RAW execution: 139/139
	​

CANON execution: 139/139
	​


Y además, como sanity fuerte, reportaría si RAW y CANON producen la misma trayectoria de acciones/final value. No hace falta que la igualdad bit-a-bit de estados internos sea un gate si el reemplazo de referencia cambia legítimamente valores continuos, pero action trace y resultado final deberían coincidir.

Por qué NB3 importa

Si pasa, podemos descartar el siguiente shortcut posible:

“el decoder dice correctamente VALUE_L/F, pero el runtime necesita información analógica adicional escondida dentro de r
F
	​

/r
A
	​

.”

Al reemplazar la referencia aprendida por el prototipo canónico:

r→C
v
^
	​

,

destruimos cualquier residuo analógico extra.

Si la conducta permanece exacta:

la identidad discreta del argumento es suficiente para el downstream
	​


Ése es un resultado mucho más fuerte que simplemente decode=139/139.

HALT

Si falla cualquier decode o CANON execution:

HALT. NB4 no se ejecuta.

No cambiar:

γ=2.5;

Stage A;

Stage B;

gate;

scorer;

decoder;

arquitectura;

training.

Primero se clasifica el fallo.

Si NB3 pasa completo:

NB4 autom
a
ˊ
ticamente autorizado bajo el contrato ya congelado
	​


sobre los 992 development, sin volver a tocar ningún hiperparámetro.

Así que sí: procedan con NB3-CANON exactamente ahora.