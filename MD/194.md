Sí. La dirección está invertida otra vez, y tu corrección es exactamente la que corresponde.

Para evitar que vuelva a ocurrir, desde aquí fijaría una convención única para todos los benchmarks de costo:

R
cost
	​

=
T
BASELINE
	​

T
CANDIDATO
	​

	​

	​


De modo que siempre:

R < 1   candidato más rápido
R = 1   mismo costo
R > 1   candidato más lento
Fase D — fórmulas canónicas

Para cada K:

R
cache,K
	​

=
T
DIRECT,K
	​

T
CACHE,K
	​

	​

	​


Entonces:

R_cache = 0.90 → CACHE tarda 90% del baseline → 10% menos tiempo
R_cache = 0.80 → 20% menos tiempo
R_cache = 1.00 → sin cambio
R_cache = 1.10 → 10% más lento

Por tanto el gate queda correctamente:

R
cache,K
	​

≤0.90
	​


si queremos exigir ≥10% de reducción de wall-clock por K.

Joint

Mismo criterio:

R
joint
	​

=
T
DIRECT,K1
	​

+T
DIRECT,K4
	​

T
CACHE,K1
	​

+T
CACHE,K4
	​

	​

	​


y:

R
joint
	​

≤0.80
	​


significa literalmente ≥20% de reducción conjunta.

No debe aparecer DIRECT/CACHE en ninguna de esas dos fórmulas.

Break-even

Ésta sí estaba orientada correctamente:

N
break-even
	​

=
D−C
B
	​


con:

B = tiempo de construir el cache;

D = costo projected de una corrida DIRECT;

C = costo projected de una corrida CACHE.

Y para amortización dentro de la primera corrida:

B+C<D
	​


Eso no necesita corregirse.

Pondría canaries obligatorios

Antes de Fase D real:

DIRECT = 100
CACHE  = 90

R_cache = 0.90
→ PASS individual
DIRECT = 100
CACHE  = 110

R_cache = 1.10
→ FAIL

Y joint:

DIRECT_K1 + DIRECT_K4 = 200
CACHE_K1  + CACHE_K4  = 160

R_joint = 0.80
→ PASS exacto

Más uno de regresión:

CACHE total = 180
R_joint = 0.90
→ FAIL joint

Así el código no puede invertir accidentalmente la semántica aunque la variable se llame mal.

Estado
Phases A/B/C:
    GO / no afectadas

Phase D:
    HOLD hasta corregir fórmula + tests

Canonical cost convention:
    candidate / baseline

R_cache,K:
    T_CACHE,K / T_DIRECT,K

R_joint:
    ΣT_CACHE / ΣT_DIRECT

Buen catch. Si se hubiese dejado DIRECT/CACHE, habría convertido nuevamente una optimización real en una aparente regresión.