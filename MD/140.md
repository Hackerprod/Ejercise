Sí. Autorizo iniciar únicamente la etapa A fresh sobre 7801–7805: entrenamiento de los cinco binders base con la receta congelada. B, C y la evaluación emparejada permanecen en HOLD.

Revisé el commit f33f525003e9…, el generador, el checker y el registro de fuentes. El generador explicita las permutaciones, el ID reservado para NOOP, las fórmulas corregidas y la separación A/B/C; el checker contrasta las asignaciones con una implementación independiente. No he reejecutado tu checker ni recalculado los hashes de los archivos locales.

T7-NOOP-NONE-LEXICAL / STAGE-A FRESH: AUTHORIZED
	​

1. Una omisión en la referencia de B que no bloquea A

El freeze incluye execute_t7_noop_none_stage_b_development.py, pero no identifica el runner de reparación que cerró correctamente el sanity de B. El archivo registrado todavía contiene:

Python
Run
values_logits = details["values"]
decoded = int(values_logits[row_index, int(pointer.item())].argmax().item())
canonical = decoded

Es la ruta inválida que ya se corrigió mediante la reparación posterior. Su presencia en el registro de fuentes no autoriza a reutilizar ese sanity.

No cuestiona los manifests ni exige volver a generarlos. El calibrador de ese archivo y su sanity son componentes distintos: puede conservarse la referencia al calibrador, pero B debe referenciar explícitamente el sanity aprobado que usa decode_states.

Antes de autorizar B, añadí un registro de procedencia que identifique esa implementación y su hash, tomando como referencia la reparación cerrada en fea9ea43f255893451230db4714360d409e0363f. Conservá intacto el freeze actual; la aclaración debe ser un artefacto adicional, no una reescritura silenciosa.

A puede comenzar sin esperar esa aclaración de B.

2. Runner fresh: adaptar entradas y rutas, no la receta

Los manifests fresh tienen un esquema distinto del empleado por los runners development. Por ejemplo, registran stages.A/B/C, mientras que el constructor de dominio base debe extraer únicamente los cuatro operadores activos. El role_order general incluye NOOP porque describe cinco semánticas de entrada, no cinco queries.

Queda autorizado crear el adaptador fresh necesario para rutas, esquema, seeds y clasificación de resultados. Debe:

Leer los manifests sellados 7801–7805, sin modificarlos, y construir en memoria las 128 filas atómicas de los cuatro roles activos.

Usar exactamente cuatro queries, para FLOOR, AVOID, MATCH y ANCHOR.

Construir sólo el vocabulario base de 37 filas:

32 ARG+4 operadores activos+LINK.

El ID externo reservado de NOOP permanece como metadata. No añadir su fila, no inicializar e
N
	​

 y no incluir NOOP en ningún batch, pérdida o contexto BG de A.

No ejecutar directamente un main() development con seeds o directorios 770x fijados. Guardar el runner fresh y su hash antes del primer optimizer.step(). La adaptación no puede alterar el forward, las pérdidas, sus reducciones ni los hiperparámetros.

3. Comprobación obligatoria antes de entrenar cada seed

Verificar exhaustivamente las 128 filas efectivamente construidas:

target(ARG
i
	​

)=permutation[i]
	​

.

Para los cinco modelos deben quedar registrados 640 controles de mapping, no sólo la existencia del campo permutation.

Si el constructor parte de un valor v, debe utilizar la permutación inversa para seleccionar su ARG. Para FLOOR/AVOID, las filas auxiliares de comportamiento deben corresponder al valor semántico permutado, no al sufijo numérico del token.

Mantener diferenciados:

token↔ID f
ı
ˊ
sico↔
ı
ˊ
ndice interno.

Una permutación ausente, un hash discrepante o un mapping inconsistente exige abortar. No utilizar identidad como fallback ni regenerar el manifest.

4. Receta A congelada

Cada seed comienza con un binder nuevo. No cargar pesos de 770x ni de otros binders históricos.

Sí se permite cargar el decoder/codebook y el supervisor congelados previstos por la receta, verificando su identidad contra las dependencias aprobadas de A development. Esa autorización no permite sustituirlos por otros checkpoints.

Mantener el core real hasheado —sin añadir una SiLU para ajustarlo a fórmulas históricas—, W
v
	​

 compartido y:

Q∈R
4×16
.

La pérdida sigue siendo:

L
A
	​

=L
behavior
F/A
	​

+L
ref
F/A/M/H
	​

+L
sep
4+BG
	​

,

con 12 términos entre roles, 4 contra background, 40 contextos BG, reducción /16 y coeficiente 1.0.

Elemento	Contrato
Filas atómicas	128: 32 por rol activo
Actualizaciones	5000
Optimizador	AdamW
Weight decay	0
Learning rate	Lineal 1e-3 → 1e-5
Catálogo contrastivo	Completo en cada actualización
Checkpoint seleccionado	El final de las 5000 actualizaciones
Datos multi-cláusula en training	Ninguno

No prolongar entrenamiento, seleccionar un checkpoint por evaluación ni ajustar la receta entre seeds. Los manifests fresh registran precisamente esta configuración de A.

5. Gates de salida de A
Exactitud atómica

Por seed, exigir:

FLOOR=AVOID=MATCH=ANCHOR=32/32,

con pointer correcto y decodificación RAW/CANON mediante la ruta aprobada.

Todavía no se exige salida NULL para roles ausentes: B aún no ha calibrado los umbrales.

Separación local

Calcular los doce certificados dirigidos:

G
r,r
′
	​

=
i
min
	​

s
r
	​

(OP
r
	​

,ARG
i
	​

)−
j
max
	​

s
r
	​

(OP
r
′
	​

,ARG
j
	​

),r

=r
′
,

y los cuatro certificados BG:

G
r,BG
	​

=
i
min
	​

s
r
	​

(OP
r
	​

,ARG
i
	​

)−
z∈S
4
	​

max
	​

s
r
	​

(z).

Exigir los 16 estrictamente positivos en cada modelo. Reportar aparte los márgenes contra ARG→LINK y conservar el gradient sanity de LINK, sin actualización adicional.

No exigir las magnitudes obtenidas en development: el criterio congelado sigue siendo >0.

Procedencia

Guardar el checkpoint final, su hash y el hash del estado de tensores. Confirmar que las auditorías posteriores no alteran ese estado. Ésa será la referencia congelada para B y C.

6. Ejecución, fallos y entrega

Ejecutar los cinco seeds secuencialmente, sin delegación ni spawning.

Si un seed falla científicamente sus gates, completar los otros cuatro bajo la misma receta y conservar el resultado. No sustituir seeds. Un defecto de implementación o integridad exige HALT operativo y preservación del intento como INVALID / HARNESS BUG.

La salida debe quedar en un directorio fresh exclusivo, por ejemplo:

campaign/t7_noop_none_lexical_stage_a_fresh/

La entrega incluirá los cinco checkpoints y sus hashes, resultados atómicos, los 16 certificados por seed con sus extremos, los 640 controles de mapping y la evidencia de exclusión de NOOP.

Si los cinco pasan:

T7 STAGE-A FRESH: PASS
	​

.

Eso no cierra todavía T7 fresh ni autoriza automáticamente B/C. El futuro PASS_STRONG pertenece al procedimiento completo A→B→C→evaluación emparejada.

Autorización concreta: iniciar ahora A sobre 7801–7805. No calibrar NULL, no inicializar ni entrenar NOOP, no abrir evaluación multi-cláusula y no modificar los artefactos 770x.