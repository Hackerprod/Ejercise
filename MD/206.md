Sí. R1 está cerrado y avanzaría directamente a R2. No pediría más de 8 updates por K.

Los 16 puntos vivos eran un gate de generalización numérica, no un experimento estadístico. Que los 16 hayan pasado PASS_PRIMARY sin usar siquiera el oracle FP64 es evidencia más fuerte de la que necesitábamos para autorizar benchmark. Más updates ahora sólo gastarían horas repitiendo una propiedad ya establecida.

Hay un detalle importante para R2: los ~94 s/update del diagnóstico K4 no son una estimación de rendimiento del runtime. Ahí se ejecutaba reference + shadow native + instrumentación/oracles. R2 debe eliminar todo eso.

R2 que autorizaría
OMEGA-NATIVE-RUNTIME-P2-R2
END-TO-END PERFORMANCE GATE

Dos rutas por K:

PYTORCH-K1
NATIVE-K1

PYTORCH-K4
NATIVE-K4

Todo lo demás idéntico:

hidden-cache RAM canónico;

teacher LM head exacto;

vocab/CE+KL PyTorch;

AdamW PyTorch;

full BPTT256;

B=8;

4/1 threads;

mismo corpus/schedule;

misma inicialización/checkpoint.

La única variable es:

PyTorch recurrent forward/backward
        vs
same standalone DLL through the temporary autograd bridge
Fundamental: build de producción

Para la ruta NATIVE:

Release
/fp:precise

y ninguna instrumentación de certificación:

FP64 oracle              OFF
sum_abs buffers           OFF
contribution counters     OFF
shadow PyTorch recurrent  OFF
golden diagnostics        OFF

Esos mecanismos pertenecen al harness R0/R1, no al hot path.

El bridge tampoco puede hacer matemática ni copias de parámetros; sólo punteros, workspace y llamadas C ABI.

Haría primero un preflight corto

Dado que todavía no conocemos la velocidad del backward C++ real:

2 warmup + 6 measured

para las cuatro rutas.

Si una ruta nativa da:

R
K
	​

=
T
PYTORCH,K
	​

T
NATIVE,K
	​

	​

>1.25

la consideraría regresión suficientemente clara para no gastar un benchmark largo todavía. Se perfila/optimiza el kernel nativo.

Si ambas quedan:

R
K
	​

≤1.25

continuar el mismo diseño con una medición más estable:

8 warmup + 32 measured

fresh process por ruta.

No necesita cientos de updates.

Fórmula, congelada explícitamente

Para evitar otro problema de orientación:

R
K
	​

=
T
PYTORCH,K
	​

T
NATIVE,K
	​

	​

	​


CANDIDATO / BASELINE.

Y:

R
joint
	​

=
T
P,K1
	​

+T
P,K4
	​

T
N,K1
	​

+T
N,K4
	​

	​

	​


Por tanto:

R=0.80 → native tarda 20% menos
R=0.90 → native tarda 10% menos
R=1.00 → empate
R=1.10 → native tarda 10% más

Canaries unitarios antes de ejecución real:

PyTorch=10, Native=8  → R=0.8
PyTorch=10, Native=11 → R=1.1
Clasificación que congelaría

No usaría sólo PASS/FAIL, porque el objetivo final también es eliminar PyTorch.

NATIVE-PERF-STRONG
    R_joint <= 0.80
    y ningún K > 1.05

NATIVE-PERF-PASS
    R_joint <= 0.90
    y ningún K > 1.05

NATIVE-PERF-NEUTRAL
    0.90 < R_joint <= 1.05
    y ningún K > 1.10

NATIVE-PERF-REGRESSION
    R_joint > 1.05
    o algún K > 1.10
Decisión posterior

Si STRONG/PASS:

R2 CLOSED
→ reprofile canonical hybrid runtime
→ decidir P2-V

Si NEUTRAL:

la arquitectura nativa es válida, pero antes de implementar vocab/loss haría un profiling del C++ para comprobar si SIMD/threading/cache tiling tienen una ganancia obvia.

Si REGRESSION:

no abandonamos el runtime standalone; detenemos la expansión a P2-V y optimizamos P2-R primero. Construir más partes nativas encima de un recurrent engine claramente más lento ocultaría el problema.

Métricas secundarias que sí quiero

Además del end-to-end:

native recurrent forward call
native recurrent backward call
bridge overhead
workspace allocation/reuse
total update
RSS

Especialmente bridge overhead.

Si el kernel es rápido pero el bridge es caro, no debemos “optimizar” la matemática equivocada; ese bridge es temporal y desaparecerá en P2-FINAL.

No introduciría timers dentro de cada small-GEMM del kernel durante el benchmark primario. Eso perturbaría el hot path. Profiling interno sólo después si R2 resulta lento.

No hace falta más correctness

Estado:

P2-R0:
    CLOSED / PASS

P2-R1:
    CLOSED / PASS
    K1 8/8
    K4 8/8
    all live points PASS_PRIMARY

More shadow updates:
    NOT REQUIRED

P2-R2:
    GO

Así que avanzaría ya. R0 demostró la matemática standalone; R1 demostró que esa misma DLL funciona correctamente dentro de un entrenamiento vivo. R2 debe responder ahora una pregunta distinta: cuánto compra realmente en wall-clock.