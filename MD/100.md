Sí. Autorizo NB5-FAILURE-ALG ahora. Ya tenemos la replicación suficiente para que el análisis transversal sea mucho más informativo que estudiar 6801 aisladamente.

Formalmente, el cuadro queda:

T2-NOBYPASS / NB5: PRIMARY FAIL
	​


y la replicación secundaria:

0/5 fresh domains con closure exacta
	​


pero con transferencia parcial alta:

5×992
877+975+976+892+985
	​

=
4960
4705
	​

=94.86%

por orden. Como natural y reverse tienen exactamente los mismos failure sets:

9920
9410
	​

=94.86%

sobre toda la batería.

Llamaría al patrón “aparente estructura de dos niveles”, no afirmaría estadísticamente “distribución bimodal” con sólo cinco seeds. Pero descriptivamente sí es llamativo:

6801/6804: ~88–90%;

6802/6803/6805: ~98–99%.

Y fail_both=0 en todos los seeds es probablemente el dato estructural más interesante.

T2-NOBYPASS-1-NB5-FAILURE-ALG: AUTHORIZED

Cero training.
Cero cambio de pesos.
Manifests v2 congelados.
Usar los checkpoints Stage A/B ya existentes de 6801–6805.

El análisis debe usar principalmente natural porque ya está demostrado que reverse reproduce exactamente el mismo conjunto (L,F,role). Reverse queda como sanity, no como duplicación estadística.

A. Reconstruir la geometría local completa

Para cada seed y cada ARG_i, calcular cuatro scores:

F
self
	​

(i)=s
F
	​

([OP
F
	​

,ARG
i
	​

])
F
cross
	​

(i)=s
F
	​

([OP
A
	​

,ARG
i
	​

])
A
self
	​

(i)=s
A
	​

([OP
A
	​

,ARG
i
	​

])
A
cross
	​

(i)=s
A
	​

([OP
F
	​

,ARG
i
	​

]).

Más:

ARG index = i;

VALUE = π(i);

gate c
i
	​

;

value-decode margin;

atomic FLOOR loss/margin;

atomic AVOID loss/margin.

Guardar las 32 filas completas por seed.

Esto es suficiente para reconstruir la matriz joint entera sin ejecutar ninguna secuencia nueva.

B. Reproducir exactamente los 992 fallos desde los scores

Para una pareja donde:

i
L
	​

 es el ARG que representa lower;

i
F
	​

 representa forbidden;

HARDPTR FLOOR falla exactamente cuando:

Δ
F
	​

(i
L
	​

,i
F
	​

)=F
self
	​

(i
L
	​

)−F
cross
	​

(i
F
	​

)≤0
	​


y AVOID falla exactamente cuando:

Δ
A
	​

(i
L
	​

,i
F
	​

)=A
self
	​

(i
F
	​

)−A
cross
	​

(i
L
	​

)≤0.
	​


Quiero que el artifact reconstruya las 992 decisiones usando sólo estas cuatro tablas de 32 elementos.

Gate obligatorio:

predicted_failure_matrix=observed_failure_matrix

bit-a-bit para los cinco seeds.

Espero:

992/992

por seed.

Si eso pasa, habremos reducido todo NB5 a un problema extremadamente simple:

solapamiento entre la distribución SELF de un rol y la distribución CROSS del mismo query sobre el operador contrario.

C. Certificado global de composición por rol

Ésta creo que será la métrica central.

Por seed:

G
F
	​

=
i

=j
min
	​

[F
self
	​

(i)−F
cross
	​

(j)]
G
A
	​

=
i

=j
min
	​

[A
self
	​

(i)−A
cross
	​

(j)].

Equivalentemente, como certificado suficiente aproximado:

minF
self
	​

−maxF
cross
	​


y lo mismo para AVOID.

La propiedad que HARDPTR necesita para composición exhaustiva es simplemente:

G
F
	​

>0∧G
A
	​

>0
	​


Nada relacionado con decoder, gate o mixture.

Esto probablemente explicará tu patrón casi por completo:

6802/6803: sospecho

G
A
	​

>0,G
F
	​

<0

pequeño → sólo FLOOR falla.

6805:

G
F
	​

>0,G
A
	​

<0

pequeño → sólo AVOID falla.

6801/6804:

G
F
	​

<0,G
A
	​

<0

→ ambos roles tienen tails de fallo y caen al clúster ~89%.

Si eso aparece, la aparente “bimodalidad” deja de ser misteriosa: sería simplemente cuántos de los dos role separators consiguen separación global en cada init.

D. Cuantificar overlap, no sólo el mínimo

Por seed y role reportar:

min/median/max SELF;

min/median/max CROSS;

percentiles;

global_gap;

número de SELF debajo del max(CROSS);

número de CROSS por encima del min(SELF);

número exacto de pares negativos.

Esto dirá si un seed de 98–99% tiene:

una frontera casi separada con sólo 1–2 ARG débiles,

mientras los seeds ~89% tienen:

solapamiento sustancial entre dos distribuciones.

E. El control clave ARG vs VALUE

Aquí usamos la permutación como instrumento causal.

Para cada seed y role calcular dos perfiles.

Alineados por ARG index

Para cada ARG_i:

veces que falla como target;

veces que provoca fallo como competitor.

Luego comparar esos vectores entre seeds manteniendo fijo i.

Alineados por VALUE ISA

Transformar mediante:

v=π
d
	​

(i)

y repetir agrupando por VALUE_v.

Entonces comparar entre seeds.

Quiero, como mínimo:

matriz de correlación Spearman ARG-aligned;

matriz de correlación Spearman VALUE-aligned;

frecuencia total de fallos por ARG;

frecuencia total por VALUE;

top-5 de ambos.

Interpretación:

Si VALUE-aligned >> ARG-aligned

los mismos VALUE_n vuelven a ser problemáticos aunque estén representados por diferentes ARGs:

value/ISA-side geometry
	​

Si ARG-aligned >> VALUE-aligned

el problema sigue el identificador lexical:

lexical/embedding-side
	​

Si ambos son débiles

la explicación principal probablemente no es una identidad particular:

seed-level role-separation geometry
	​


Mi sospecha actual es esta tercera.

F. Separar “target débil” de “competitor atractivo”

Para cada role:

FLOOR failure:

F
self
	​

(target)≤F
cross
	​

(competitor).

Hay dos causas posibles:

target tiene SELF anormalmente bajo;

competitor tiene CROSS anormalmente alto.

Por cada ARG/VALUE reportar dos propensiones distintas:

weak_self(i)

y

cross_attractor(i).

Por ejemplo:

cuántos competidores derrotan a este ARG cuando es target;

cuántos targets derrota este ARG cuando actúa como competitor cross-role.

Eso puede revelar algo como:

ARG_17 no es débil cuando es target, pero su F_cross es enorme y destruye muchos FLOOR targets.

Es mucho más informativo que contar únicamente qué (L,F) falla.

G. Atomic margin vs joint generalization

También autorizo el control que habíamos predeclarado:

para cada ARG y role, correlacionar su propensión a fallar en joint con las métricas de atomic training:

atomic reference CE;

frozen decoder margin;

behavior/action margin si existe;

SELF score.

Especialmente:

corr(F
self
	​

(i), target_failure_count
F
	​

(i))

y:

corr(F
cross
	​

(i), competitor_failure_count
F
	​

(i)).

Análogo para AVOID.

Mi expectativa es:

SELF bajo → más fallos como target;

CROSS alto → más fallos como competitor.

Pero la atomic task probablemente seguirá siendo 32/32 incluso para esos ARGs.

Eso demostraría que:

atomic correctness no identifica la separaci
o
ˊ
n necesaria para composici
o
ˊ
n
	​

H. Credit-assignment audit — muy importante

Añadiría una prueba sin updates, sólo backward diagnóstico.

Sobre un ejemplo atomic AVOID:

medir el gradiente del loss de Stage A realmente usado respecto a:

q
F
	​


y/o al score:

F
cross
	​

=q
F
⊤
	​

k(OP
A
	​

,ARG).

Y sobre un atomic FLOOR:

medir el gradiente respecto a:

q
A
	​


/ A
cross
	​

.

Quiero distinguir:

∥∇L/∇q
absent−role
	​

∥

del role presente.

Mi hipótesis de raíz ahora es:

cuando el role está ausente, el objective atomic no ejerce presión suficiente —quizás ninguna— para hacer bajo el score CROSS de la otra query.

Entonces Stage A aprende:

F
self
	​

↑

en ejemplos FLOOR y:

A
self
	​

↑

en ejemplos AVOID,

pero nadie garantiza globalmente:

F
cross
	​

↓

ni:

A
cross
	​

↓.

Development seed6705 cayó afortunadamente en una solución con separación global:

992/992.

Los fresh seeds no.

Eso explicaría perfectamente:

atomic 32/32;

0/5 exact joint closure;

roles que fallan alternadamente según seed;

aparente clúster alto/bajo;

cero necesidad de blamear decoder/gate/HARDPTR.

I. El fail_both=0

No lo dejaría como mera curiosidad.

Sobre las 992 parejas por seed clasificar en cuatro cuadrantes:

(Δ
F
	​

>0,Δ
A
	​

>0)
(Δ
F
	​

≤0,Δ
A
	​

>0)
(Δ
F
	​

>0,Δ
A
	​

≤0)
(Δ
F
	​

≤0,Δ
A
	​

≤0).

Sabemos que el último es:

0

en los cinco.

Reportar:

Pearson/Spearman entre Δ
F
	​

 y Δ
A
	​

;

distancia mínima a la región both fail;

qué relación geométrica impide que ambas desigualdades sean negativas simultáneamente.

No asumiría todavía que es una propiedad necesaria de la arquitectura, pero cinco seeds × 992 es una regularidad demasiado fuerte para ignorarla.

Mi hipótesis principal antes de ver NB5-FAILURE-ALG

La evidencia actual apunta menos a “algunos VALUE/ARG malos” y más a:

role separation is not identified strongly enough by atomic training
	​


Más específicamente:

RELKEY aprende perfectamente la asociación positiva OP_F→q_F y OP_A→q_A, pero el training atomic no obliga con suficiente fuerza a la query contraria a rechazar ese mismo ARG/operator relation.

La composición joint necesita una propiedad contrastiva entre roles:

F
self
	​

(i)>F
cross
	​

(j)∀i,j
A
self
	​

(i)>A
cross
	​

(j)∀i,j.

Atomic accuracy no exige eso.

Ese sería un caso clásico de:

training objective underdetermines the compositional invariant.
No diseño todavía la corrección

Aunque la dirección probable ya se ve —alguna forma de role-contrastive supervision usando sólo ejemplos atomic, sin mostrar joint sequences—, no la autorizaría aún.

Primero quiero que NB5-FAILURE-ALG responda:

¿las matrices de fallo son reconstruibles exactamente desde SELF/CROSS?

¿los dos clusters corresponden a G
F
	​

/G
A
	​

?

¿los fallos siguen VALUE, ARG o ninguno?

¿el cross-role score carece realmente de gradiente/supervisión atomic?

Si las cuatro convergen, entonces la próxima modificación será causal, no otro parche.

Y cualquier arquitectura/objective modificado después de estudiar NB5 no puede volver a validarse sobre 6801–6805 como fresh evidence. Esos cinco pasan formalmente a ser development/diagnostic.

La siguiente validación tendría que usar una batería nueva, por ejemplo 6901–6905, con el esquema de operator-swap estratificado que ya predeclaramos.

Resolución

NB5 primary: CLOSED / VALID FAIL.

Secondary replication: CLOSED / DIAGNOSTIC COMPLETE.

0/5 exact fresh closure.

Transfer aggregate: 94.86%, con dos niveles aparentes (~89% y ~98–99%), sin afirmar bimodalidad poblacional con n=5.

NB5-FAILURE-ALG: AUTHORIZED, cero training/cambios, análisis conjunto de los cinco domains.

El foco principal debe ser SELF-vs-CROSS role separation, con ARG-vs-VALUE alignment y gradient audit como controles causales.