Confirmo T7 STAGE-C DEVELOPMENT: CLOSED/PASS y autorizo la evaluación emparejada multi-cláusula sobre los cinco modelos 7701–7705 ya congelados. No hace falta otra intervención antes de evaluarlos.

Revisé el commit 20b15e9ff463…, el runner de C y las secciones inicial/final del resultado de 7701. El código optimiza exclusivamente embedding.noop_embedding, reutiliza los umbrales y comprueba la preservación del estado base. La verificación completa de los cinco resultados y sus hashes es la que reportaste; no he reejecutado la campaña.

T7 STAGE-C DEVELOPMENT: CLOSED / PASS
	​

T7-NOOP-NONE-LEXICAL / PAIRED DEVELOPMENT EVALUATION: AUTHORIZED
	​


Esta unidad es exclusivamente de evaluación: cero entrenamiento, cero recalibración y cero cambios de arquitectura. 780x permanece sin generar.

1. Qué añade este resultado de C

En 7701, el certificado inicial de MATCH era:

G
M
initial
	​

=−0.5659928321838379,

con el candidato OP_Y→ARG_15 por encima de NULL. Al terminar, el peor candidato para MATCH queda por debajo del mismo umbral:

G
M
final
	​

=3.634314179420471.

El contexto que determina el mínimo final es ahora START→OP_Y. Eso muestra una corrección efectiva de la geometría NOOP, no simplemente la conservación de una inicialización que ya pasaba.

La conclusión precisa es que la inicialización registrada no bastaba y la intervención sobre e
N
	​

 corrigió sus fallos sin modificar el sistema base. No necesitamos afirmar que entrenar era la única solución posible.

Los certificados locales hacen predecible el siguiente pase: los argumentos propios superan a NULL; los negativos base y los 34 candidatos NOOP quedan por debajo. La evaluación emparejada comprobará que esa separación se realiza correctamente en instrucciones completas, incluyendo vocabulario ampliado, longitudes variables, posición de las cláusulas y decodificación.

2. Cargar el modelo final correcto, sin reconstruir otro experimento

Para cada seed, cargar su checkpoint final de C, asociado a su core de A, calibración de B y manifest sellado.

Verificar antes de evaluar:

Hash del checkpoint C y sus referencias a A, B y manifest.

Identidad de los componentes base frente al core aprobado de A.

Identidad del embedding final e
N
	​

 y de los cuatro b_r_stored.

El runner guarda en el checkpoint de C el estado ampliado, el augmented_token_order, el mapping de NOOP y el vector de umbrales. Utilizar esos datos guardados; no volver a inicializar e
N
	​

, no ejecutar de nuevo append_noop() para generar un embedding aleatorio y no recalcular los midpoints.

Atención al vocabulario ampliado

El modelo base tenía 37 nombres y NOOP ocupa la fila añadida, índice interno 37. La integración debe resolver los 38 tokens, conservando exactamente los índices anteriores.

No asumir el nombre del operador NOOP: se obtiene del manifest correspondiente. Tampoco confiar en que un objeto core.vocab reconstruido sólo con el vocabulario base pueda codificar automáticamente el token añadido.

El adaptador de evaluación debe usar el mapping ampliado registrado en C, sin cambiar scores ni parámetros. Si la carga o el mapping no reproduce el estado guardado, HALT de implementación, no entrenamiento de reparación.

El decoder/codebook congelado de A/B/C queda expresamente autorizado. RAW y CANON deben pasar por decode_states; no vuelve a utilizarse argmax sobre los componentes de W
v
	​

.

3. Ejecutar la evaluación sellada, no una muestra nueva

Usar exactamente las asignaciones activas, la fórmula de D y las órdenes fijadas en la preparación. No escoger otros distractores, ejemplos “representativos” ni casos más fáciles después de observar C.

Roles activos k	Base por seed	Con NOOP por seed
1	128	256
2	11 904	35 712
3	23 808	95 232
4	23 808	119 040
Total	59 648	250 240

En los cinco dominios:

298240 evaluaciones base
	​

1251200 evaluaciones aumentadas con NOOP
	​

1549440 evaluaciones totales
	​

.

Las 640 bases singleton siguen siendo controles atómicos. Las 297 600 bases multi-cláusula y las 1 251 200 instrucciones aumentadas se reportan separadamente.

Los 160 controles atómicos NOOP de C no se suman otra vez como nueva evidencia composicional.

Checker previo del conjunto lógico

Antes de los forwards de evaluación, recorrer la especificación sellada y verificar cobertura, unicidad y emparejamiento:

Cada asignación mantiene su mismo D en todas las órdenes.

Se permutan cláusulas completas, sin separar operador y argumento.

Cada instrucción aumentada tiene exactamente una cláusula NOOP.

Al retirar esa cláusula y reconstruir los conectivos, se obtiene su base correspondiente.

Esa retirada ocurre exclusivamente en el checker/evaluador para identificar la pareja. El modelo debe recibir la instrucción aumentada completa.

No basta comprobar que dos constantes suman el total esperado: deben verificarse las instrucciones lógicas que realmente se van a ejecutar.

4. Tres condiciones independientes: base correcta, aumentada correcta e invariancia

Para cada instrucción aumentada X
N
	​

, identificar su base X, conservando el orden relativo de las cláusulas activas, y exigir:

Output(X)=Target(X),
Output(X
N
	​

)=Target(X),
Output(X
N
	​

)≡Output(X).

Dos salidas igualmente equivocadas no cuentan como invariancia correcta.

Se puede evaluar cada base una sola vez y reutilizar su salida para las parejas correspondientes. Esto no sustituye ningún forward de las instrucciones aumentadas: todas deben ejecutarse.

Qué comparar

La comparación entre pareja y entre órdenes es semántica, por rol: presencia, ausencia, argumento seleccionado, valor RAW y valor CANON.

Los índices posicionales absolutos del pointer pueden cambiar al insertar NOOP o reordenar cláusulas. Evaluar cada pointer contra la posición correcta de su propia instrucción; no exigir igualdad numérica entre posiciones de textos diferentes.

Tampoco exigir igualdad de todos los scores entre órdenes: algunos contextos estructurales cambian legítimamente. Lo que debe preservarse es la decisión correcta y sus márgenes estrictos.

5. Métricas y márgenes obligatorios

Las cuatro queries se ejecutan siempre. El forward recibe únicamente token_ids,lengths, sin active_roles, labels de presencia ni una máscara que elimine NOOP.

Roles presentes

Exigir presencia, pointer al argumento de ese rol, decode correcto y RAW/CANON correctos mediante la ruta aprobada.

Medir:

M
r
present→NULL
	​

=s
r
	​

(target)−b
r
	​

>0,
M
r
present→real
	​

=s
r
	​

(target)−
t

=target
t<length
	​

max
	​

s
r,t
	​

>0.

En la instrucción aumentada, el segundo máximo incluye todos los competidores reales, también el argumento NOOP, su operador y los conectivos. No excluirlos por su etiqueta.

Roles ausentes

Exigir NONE explícito y:

M
r
absent
	​

=b
r
	​

−
t<length
max
	​

s
r,t
	​

>0.

No decodificar una ausencia como vector cero, ni producir VALUE_0, ni indexar el codebook con -1. Si el modelo predice presencia incorrectamente, debe contabilizarse como falso positivo; el evaluador no puede convertirla en NULL usando el target.

Desglose del rechazo NOOP

Conservar los cuatro certificados locales de C y registrar, como diagnóstico en las instrucciones aumentadas, el peor score de los candidatos nuevos asociados a NOOP respecto a cada b
r
	​

.

Esto ayuda a distinguir, si aparece un fallo, entre:

captura del pointer de un rol presente;

activación falsa de un rol ausente;

error de mapping, padding o reconstrucción de contextos.

Reportar resultados por seed, subconjunto, tamaño activo y orden. Para k=4, las métricas condicionadas a ausencia son not_applicable; no fabricar una tasa 1.0 con denominador cero.

La métrica principal seguirá siendo la instrucción estructurada completa: las cuatro salidas correctas simultáneamente.

6. Inmutabilidad y evidencia reproducible

Ejecutar en modo evaluación, sin backward ni optimizador. Mantener el manejo de longitudes y padding de la implementación aprobada; no cambiar el camino de cómputo para acelerar esta medición sin comprobar su equivalencia.

Comprobar antes y después los hashes de:

core base y embedding NOOP;

vector de umbrales;

checkpoint C, calibración B y manifest;

runtime y codebook utilizado.

No sobrescribir artefactos de A/B/C. Usar un directorio exclusivo, por ejemplo:

campaign/t7_noop_none_lexical_paired_development/

Guardar conteos, identificadores de casos y parejas, resultados verificables por caso, todos los errores si aparecen y los testigos de cada mínimo de margen. Las filas booleanas o una representación compacta indexada deben permitir recalcular los agregados; un conjunto de flags all_pass=true no basta.

7. Regla de cierre

Ejecutar los cinco dominios bajo la misma receta. Un fallo científico no autoriza cambiar nada entre seeds; completar la evaluación prevista y reportarlo. Ante una discrepancia de integridad o un defecto del runner, detenerse y preservar el intento como INVALID / HARNESS BUG.

Si cierran simultáneamente:

298240/298240 bases correctas,
1251200/1251200 aumentadas correctas,

todas las parejas semánticamente equivalentes, cero falsos positivos y negativos, RAW/CANON correctos, márgenes estrictamente positivos e inmutabilidad completa:

T7-NOOP-NONE-LEXICAL DEVELOPMENT: CLOSED / PASS
	​

.

Todavía no PASS_STRONG fresh. 7701–7705 siguen siendo development. El eventual claim será composición con un NOOP aprendido explícitamente como irrelevante, no rechazo espontáneo de un operador desconocido.

Autorización concreta: abrir y ejecutar ahora la evaluación emparejada sellada sobre los cinco checkpoints finales de C. Sin reentrenamiento, sin recalibración, sin nuevas seeds y sin delegación ni spawning. 780x continúa en HOLD.