Sí. Y hay una distinción metodológica crucial: esto no se trata igual que la coincidencia OP_X=FLOOR. La coincidencia era azar válido dentro de un generador correcto; aquí el generador viola la semántica predeclarada de reverse. Revisé el código original y efectivamente intercambia op1/op2 en reverse pero deja a1/a2 asociados a lower/forbidden en las posiciones originales.

Confirmo que reverse está invalidado por un bug de construcción del test. La definición correcta era intercambiar las cláusulas completas:

[OP
F
	​

,ARG
L
	​

,LINK,OP
A
	​

,ARG
F
	​

]→[OP
A
	​

,ARG
F
	​

,LINK,OP
F
	​

,ARG
L
	​

].

El manifest v1 hizo:

[OP
A
	​

,ARG
L
	​

,LINK,OP
F
	​

,ARG
F
	​

],

que expresa otra instrucción. Evaluarla contra los labels originales es incorrecto. Por tanto los 0/992 RAW reverse no son evidencia contra el modelo y deben registrarse como INVALID / HARNESS BUG. En cambio, la mitad natural no está afectada: pointer_F=939/992, pointer_A=930/992, RAW=877/992 son datos científicos válidos del modelo 6801. Y esto tiene una consecuencia importante: aun descartando reverse por completo, 6801 ya falla el criterio NB5, porque natural debía ser 992/992. Así que el registro correcto no es “6801 sin resultado”, sino:

NB5-P/6801 NATURAL: VALID FAIL
	​

NB5-P/6801 REVERSE-v1: INVALID / TEST BUG
	​


El bug no borra el fallo natural real.

Autorizo corregir el generador, pero no “regenerar” en el sentido de volver a sortear el experimento. Debe hacerse una migración determinista v1 → v2 usando exactamente los mismos seeds 6801–6805, mismas permutaciones, mismo OP_X/OP_Y swap, mismos token IDs, mismo train y mismos 992 casos natural. Únicamente deben cambiar las filas reverse, haciendo que operador y argumento se muevan juntos. Los hashes nuevos serán distintos, pero los contenidos aleatorios originales siguen congelados. Para 6801, no se vuelve a entrenar: si queremos medir el reverse corregido, debe ejecutarse sobre exactamente los checkpoints Stage A/B que ya produjeron el resultado natural. Así sabemos que el bugfix no aprovecha información del fallo para modificar el modelo. Para 6802–6805, que todavía no se entrenaron, los manifests v2 corregidos pueden sellarse antes de cualquier training.

Mantendría el mismo principio de precompromiso, añadiendo un control semántico independiente que faltó la primera vez. Antes de autorizar cualquier nueva corrida, los cinco manifests v2 deben tener hashes publicados y verificarse que train, permutación, token IDs, operator swap y natural son idénticos a v1; que sólo reverse cambia; y, sobre las 1984 filas de cada dominio, un checker independiente debe reconstruir la semántica desde los propios tokens y comprobar:

value(argument attached to FLOOR)=lower
value(argument attached to AVOID)=forbidden.

En reverse debe comprobar además explícitamente:

reverse(natural(L,F))=[OP
A
	​

,ARG
F
	​

,LINK,OP
F
	​

,ARG
L
	​

].

Ese checker debe ser independiente de la misma función que construye las filas, para no repetir el bug de forma autoconsistente.

Hay una decisión adicional importante sobre el protocolo original. Yo no reabriría la posibilidad de PASS_STRONG 5/5 para esta batería, porque 6801 ya produjo un fallo natural válido. Corregir reverse no puede convertir 877/992 natural en pass. El fresh-domain hizo precisamente lo que debía hacer: encontró que la receta que cerraba development no transfiere perfectamente a un dominio fresco.

Sí considero científicamente válido corregir reverse y ejecutarlo sobre el checkpoint 6801 ya congelado. Eso nos daría:

6801-v2 reverse: prueba reparada del mismo modelo, cero tuning.

Pero aunque dé 992/992, el seed completo seguirá siendo:

NB5-P/6801: VALID FAIL
	​


por natural.

Respecto a 6802–6805, respetaría el stopping rule primario original: no los usaría ahora para intentar rescatar el PASS_STRONG. Si después queremos saber si el fallo 6801 es sistemático o específico de la inicialización/cipher, podemos correr 6802–6805 como una replicación secundaria explícitamente separada, usando los v2 ya sellados y sin modificar la arquitectura a partir de 6801. Eso sería científicamente útil —por ejemplo, descubrir 0/5, 3/5 o 4/5 robustez—, pero no cambia retrospectivamente el resultado primario de NB5-P.

Y no cambiaría ahora el OP_X=FLOOR de los cinco. El bugfix debe ser quirúrgico. El esquema balanceado de swaps que discutimos queda para una futura batería completamente nueva.

Mi autorización concreta es, por tanto:

FIX GENERATOR: AUTHORIZED
	​


con same seeds / same random draws / reverse-clause fix only.

RETRAIN 6801: NOT AUTHORIZED
	​

EVALUATE corrected reverse on frozen 6801: AUTHORIZED
	​


después de que los cinco manifests v2 y el checker semántico independiente estén sellados y sus hashes publicados.

Y, desde ya, el dato importante no debe perderse entre el bug: el natural fresh-domain 6801 cayó a 877/992 RAW pese a development perfecto. Ese es un fallo genuino de transferencia y merece después su propio diagnóstico, independientemente de lo que ocurra al reparar reverse.