Sí: el sanity atómico de B puede y debe cargar el mismo decoder/codebook congelado que utilizó A1. No requiere una ruta nueva ni otro modelo entrenado.

Mi expresión «cargar exclusivamente los cinco final.pt» fue ambigua: restringía los checkpoints de los binders, no excluía las dependencias congeladas necesarias para decodificar sus estados. La autorización queda aclarada explícitamente.

Hay, además, un defecto concreto en el runner que debe corregirse, no sólo una cuestión de permisos.

1. El código sí reinterpretó el estado como logits

En atomic_sanity() del commit df371328… aparecen estas operaciones:

Python
Run
values_logits = details["values"]
decoded = int(values_logits[row_index, int(pointer.item())].argmax().item())
canonical = decoded

Por tanto, hay que corregir la frase del reporte que afirma que no se reinterpretó W
v
	​

 como logits: el runner committed sí aplica argmax directamente a ese estado y, además, copia el resultado como “canonical” sin ejecutar canonicalización.

El 24/640 se conserva como salida real de ese runner, pero no es una medición válida de exactitud bajo el contrato de B. No demuestra un fallo de los umbrales, del binder ni del decoder aprobado.

La ruta utilizada por A1 era diferente: seleccionaba el estado según el pointer y llamaba a decode_states(executor, codebook, selected). Esa función realiza tanto la decodificación RAW como la comprobación CANON a través del decoder congelado.

2. Clasificación: calibración válida, sanity inválido, B pendiente

La clasificación debe quedar descompuesta:

Componente	Estado
Calibración de los 20 intervalos	PASS, evidencia preservada
Inmutabilidad de los cinco cores	PASS, según la evidencia verificada
Sanity con argmax(W_v(...)) y canonical = decoded	INVALID / HARNESS BUG
Etapa B completa	HALT / IMPLEMENTATION DEFECT, pendiente de completar el sanity

No corresponde VALID FAIL científico. Tampoco corresponde cerrar B como PASS todavía: el sanity era parte del contrato y debe ejecutarse correctamente.

Conservá el intento original y añadí su reclasificación en la procedencia del resultado corregido. No sobrescribir los números anteriores ni cambiar retrospectivamente 24 por el resultado nuevo.

Mi revisión se limita al código y contenido versionado consultados; no he recalculado los hashes ni reejecutado las calibraciones.

3. Dependencia autorizada y ruta exacta

Queda permitido cargar mediante load_executor() el mismo runtime C1 congelado y sus dependencias de carga que utilizó A1, incluyendo:

campaign/u0c_c1_lossnorm_anneal_seed101_12000/final.pt

Ese es el BASE_CHECKPOINT utilizado por el loader aprobado. El loader lo carga, establece modo evaluación y congela sus parámetros.

Verificá su identidad contra A3.decoder_checkpoint.sha256 de Stage A, no contra un checkpoint elegido por nombre parecido. No se autoriza otro binder histórico, un decoder alternativo ni modificar el codebook.

Construir el codebook exactamente como A:

Python
Run
with torch.no_grad():
    codebook = executor.token_embedding(
        torch.arange(
            VALUE_BASE,
            VALUE_BASE + VALUE_COUNT,
            dtype=torch.long,
        )
    ).detach()

Ésa es la construcción que aparece en el runner de Stage A.

Para cada presencia predicha, tomar el estado de la posición realmente seleccionada:

Python
Run
selected = details["values"][row_index, pointer].unsqueeze(0)
raw, canonical = decode_states(executor, codebook, selected)

Reutilizar la función aprobada, cuya ruta es:

r∈R
32
⟶[r,0
32
	​

]∈R
64
⟶
v
^
=argmaxdecoder([r,0],C),
v
^
CANON
	​

=argmaxdecoder(C[
v
^
],C).

CANON indexa el prototipo con 
v
^
, el índice decodificado por el modelo; nunca con el target verdadero. La función existente implementa precisamente ese recorrido.

Para ausencia predicha, conservar NULL y no invocar el decoder. No sustituir el pointer por la posición correcta conocida, ni usar la etiqueta del rol para decidir qué salidas decodificar. Las etiquetas sólo sirven para evaluar después.

Este sanity no necesita cargar el supervisor de comportamiento ni ejecutar una nueva tarea C1.

4. Siguiente unidad autorizada: reparar y completar el sanity
T7-STAGE-B-ATOMIC-SANITY-REPAIR: AUTHORIZED
	​


Reutilizar los cinco cores y las cinco calibraciones ya guardadas. No reentrenar ni recalibrar.

Leer cada b_r_stored por nombre de rol y reconstruir el vector en el orden de queries establecido. No depender del orden alfabético accidental de un diccionario JSON. Confirmar que sus valores almacenados y sus holguras permanecen idénticos.

La reparación permitida comprende únicamente la carga de la dependencia congelada, la ruta correcta RAW/CANON, el registro de resultados y la clasificación del intento. La decisión de presencia y la selección del pointer permanecen sin cambios.

Ejecutar de nuevo las 128 filas atómicas completas por seed, no sólo las que fallaron antes. Registrar separadamente:

Presencia/ausencia y pointer, para distinguirlos del readout.

Decodificación RAW y CANON de cada presencia predicha.

Corrección del resultado estructurado completo.

Los totales exigidos siguen siendo:

640 roles presentes correctamente enlazados y decodificados,
1920 roles ausentes correctamente devueltos como NULL,
640/640 instrucciones at
o
ˊ
micas completas correctas
	​

.

La igualdad RAW/CANON debe provenir de ambas rutas ejecutadas, no de asignar una variable a la otra.

Guardar el resultado en un directorio nuevo, por ejemplo:

campaign/t7_noop_none_lexical_stage_b_sanity_repair/

Referenciar allí el commit del intento inválido, los hashes de las calibraciones reutilizadas, los checkpoints de A, el runtime/decoder aprobado y el runner corregido. Confirmar inmutabilidad de cores, umbrales, runtime y codebook; mantener separados los hashes de archivos y de estados de tensores.

5. Límite de la autorización

Si la reparación completa todos los controles, B podrá cerrarse como:

T7 STAGE-B DEVELOPMENT: CLOSED / PASS
	​

.

Si persiste un error con la ruta aprobada, conservar el resultado y detenerse para diagnosticarlo; no mover los umbrales para compensarlo.

C sigue en HOLD. No inicializar e
N
	​

, no medir contextos NOOP, no abrir evaluación multi-cláusula y no generar 780x. Ejecución secuencial, sin delegación ni spawning.

La acción inmediata es, por tanto: usar el decoder/codebook congelado de A1, reparar el readout del sanity y repetirlo completo sobre los mismos cores y los mismos umbrales.