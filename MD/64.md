Sí: antes de VIEW-0 haría un diagnóstico específico de F=10. La concentración 22/22 en un único forbidden es demasiado limpia como para saltar directamente a un módulo aprendido general.

Verifiqué 7774140f00ba326c56bd0bda46bf665dc28a0dc7: 22/22 UNARY_ONLY, 0 JOINT_HARD, 0 MIXED; todos tienen forbidden=10, swap_avoid=false y swap_floor=true.

Qué significa realmente

Para el patrón mayoritario, 16/22:

identity: FLOOR=false, AVOID=true

swap: FLOOR=true, AVOID=false

Recordando cómo se asignan los slots:

identity → FLOOR usa E0, AVOID usa E1

swap → FLOOR usa E1, AVOID usa E0

eso implica:

E
1
	​

:FLOOR=✓,AVOID=✓
E
0
	​

:FLOOR=×,AVOID=×

Es decir, en esos 16 casos las dos semánticas útiles están concentradas en el mismo slot E1.

No es simplemente que “eligimos la permutación equivocada”. No existe permutación correcta porque el matching exige dos lecturas y ambas están viviendo en el mismo slot.

Los otros seis casos son variantes todavía más severas, pero comparten algo universal:

AVOID(E
0
	​

)=false

para los 22.

Eso apunta directamente al routing/binding del Writer, no al compositor.

Y R3 tiene exactamente un mecanismo capaz de producir esto

Revisé CompetitiveSemanticWriter.

Cada token genera un estado local usando:

[left, token, right]

y después cada estado compite entre tres destinos:

slot
0
	​

, slot
1
	​

, NULL

mediante softmax. Finalmente cada slot es la suma de las escrituras ponderadas de sus tokens. El propio return_details=True ya expone:

local_states

routing_probabilities

semantic_mass

values

por lo que podemos hacer la autopsia sin cambiar el Writer.

Además, R3 usa ventana local -1,0,+1, dos slots semánticos y un null sink.

Por eso F=10 podría estar produciendo, por ejemplo:

la escritura AVOID VALUE_10 se va casi toda a E1;

parte de ella se va a NULL;

el estado local de VALUE_10 cambia anormalmente al estar precedido por AVOID;

FLOOR y AVOID colisionan competitivamente por el mismo slot;

una combinación de las anteriores.

No debemos adivinar cuál.

T2-I3-R3-F10-BIND-ALG: AUTHORIZED

Cero training.
Writer 6301 congelado.
COMP fuera de esta prueba.
G5 intacto.

Usaría return_details=True.

Panel

No limitarse sólo a los 22.

Para todos los F=0…31, y todos los L válidos del dominio ya consumido:

AT_LEAST VALUE
L
	​

 AND AVOID VALUE
F
	​


obtener:

routing de cada token hacia slot0/slot1/NULL;

semantic mass;

contribución de cada token a cada slot;

norma de cada write;

output final E0/E1.

Luego comparar F=10 contra los otros 31 valores.

Eso nos dice si 10 es realmente un outlier interno y dónde aparece.

Dos controles especialmente importantes
1. Atomic vs joint

Comparar:

AVOID VALUE
10
	​


contra:

AT_LEAST VALUE
L
	​

 AND AVOID VALUE
10
	​

.

G1 ya nos dice funcionalmente que los átomos siguen funcionando; por eso, si AVOID VALUE_10 standalone tiene routing normal pero se colapsa sólo dentro del joint, descartamos bastante fuerte:

“el embedding VALUE_10 está simplemente roto”.

La conclusión sería más precisa:

la representación de VALUE_10 es válida aisladamente, pero su binding/routing contextual bajo AVOID dentro de una instrucción conjunta entra en una región degenerada.

2. Unary-on-combined evidence

Éste es muy barato y funcionalmente más importante que cosine/L2.

Para cada uno de los 22 evaluar AVOID y FLOOR usando:

E
0
	​

,E
1
	​

,B=E
0
	​

+E
1
	​


por separado.

Ya conocemos los dos primeros. Nos falta:

AVOID(B)

y

FLOOR(B).

Esto distingue dos situaciones.

Si, por ejemplo:

AVOID(E
0
	​

)=×,AVOID(E
1
	​

)=✓,AVOID(E
0
	​

+E
1
	​

)=✓

entonces la evidencia AVOID está disponible en el workspace; el defecto es sólo cómo obtener una vista apropiada.

VIEW-0 queda directamente respaldado.

Pero si para ciertos casos:

AVOID(E
0
	​

)=AVOID(E
1
	​

)=AVOID(E
0
	​

+E
1
	​

)=×

mientras AVOID VALUE_10 standalone sí pasa, entonces el joint Writer realmente está perdiendo o deformando esa evidencia.

Eso sería una razón para no esconder el problema detrás de VIEW-0.

Haría además una descomposición causal por token

Como el slot se construye aditivamente, podemos calcular exactamente:

E
s
	​

=
t
∑
	​

p(s∣t)w
t
	​

.

No es aproximación: es la ecuación que usa el código.

Reportaría para las posiciones de:

AT_LEAST | VALUE_L | AND | AVOID | VALUE_F

qué porcentaje/norma aporta cada token a:

E0

E1

NULL

y compararía VALUE_10 con valores vecinos y con la distribución de todos los F.

Particularmente quiero saber si para F=10 ocurre algo como:

p(E
0
	​

∣AVOID/VALUE
10
	​

)≈0

en los 22, porque eso explicaría inmediatamente el universal:

swap_avoid=false.
No haría todavía una intervención entrenada

No entrenaría VIEW-0 mirando los 22.

Primero quiero saber cuál de estas tres ramas tenemos:

R1 — evidencia presente, routing concentrado

Atomic10 PASS; joint E0/E1 muestran que toda la semántica útil cae en E1; B conserva AVOID/FLOOR.

Entonces:

VIEW-0 AUTHORIZED.

Y su trabajo sería realmente:

(E
0
	​

,E
1
	​

)→V
F
	​

,V
A
	​


sin alterar evidencia ni composición.

R2 — evidencia distribuida pero recuperable

Ningún slot individual sirve, pero B u otra lectura simple no destructiva sí.

Entonces quizá ni siquiera necesitamos un VIEW-0 de 1k parámetros. Podría bastar una read head tipada mínima.

R3 — información AVOID10 realmente desaparece/deforma en joint

Atomic10 funciona, pero ni E0, E1 ni B contienen una condición funcional recuperable y el detalle de routing muestra pérdida/NULL/collision.

Entonces VIEW-0 estaría intentando reconstruir información que el Writer no escribió limpiamente.

Ahí regresaría localmente al Writer, no a THINK: corregiríamos el mecanismo de escritura/binding y volveríamos a congelarlo.

Importancia del hallazgo

Esto ya no parece una limitación general de “dos slots”.

El residual después de COMP quedó:

22/853=2.58%

y ahora descubrimos que el 100% de ese residuo comparte exactamente el mismo forbidden token.

Eso es una firma demasiado localizada.

Puede resultar que la arquitectura evidence/view/composition sea correcta y que estemos frente a una peculiaridad del checkpoint 6301.

Por eso no usaría todavía esos 22 como argumento de que la arquitectura necesita necesariamente una nueva capacidad general.

Orden formal

T2-I3-COMP-0-G4-RESIDUAL-ALG: CLOSED / DIAGNOSTIC PASS_STRONG

22/22 UNARY_ONLY confirmado.

VIEW-0: EXPECTED NEXT, pero todavía NOT AUTHORIZED.

T2-I3-R3-F10-BIND-ALG: AUTHORIZED

Writer congelado, cero training, análisis atomic-vs-joint + routing/token contributions + prueba funcional E0/E1/E0+E1.

Si confirma que F=10 está presente pero mal asignado, entonces sí pasaría inmediatamente a VIEW-0. Si confirma pérdida contextual real, corregiría primero el Writer en lugar de construir una capa que compense un defecto puntual del checkpoint.