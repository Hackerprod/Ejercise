Sí. Confirmo:

T2-NOBYPASS-2-RCSEP-C-NOGATE-HARDPTR: CLOSED / PASS_STRONG
	​


9920/9920, con M_ARG>0 y M_STRUCT>0 en todas las filas, elimina funcionalmente Stage B. El resultado de 6801–6805 ya es suficiente; no quiero otro experimento científico sobre esos seeds.

La candidata final queda simplificada a:

k
t
	​

=LN(SiLU(W
k
	​

[e
t−1
	​

,e
t
	​

]))
s
r,t
	​

=q
r
⊤
	​

k
t
	​

/4
j
r
	​

=arg
t<length
max
	​

s
r,t
	​

r
r
	​

=W
v
	​

(e
j
r
	​

	​

)

Sin gate, sin NUMERIC-CONFIDENCE, sin τ, sin α, sin threshold, sin softmax/soft-mixture. Tie policy: first/min-position. Stage A mantiene:

L=L
behavior
	​

+L
ref
	​

+L
sep
	​

	​


con el L_sep all-to-all exacto de RCSEP-A, coeficiente 1.0.

Único paso antes de fresh: recipe freeze

No hace falta volver a correr 6801–6805. Creá sólo un artifact de congelación, por ejemplo campaign/t2_nobypass2_rcsep_nogate_freeze/results.json, que registre:

hashes del código de modelo, trainer RCSEP, evaluator HARDPTR y generador/checker de manifests;

5000 updates, AdamW, wd=0, LR 1e-3→1e-5, batch actual;

catálogo all-to-all de 64 relaciones atomic en cada update;

cero joint training;

forward sólo (token_ids,lengths);

Stage B ausente;

gate/teacher/tau/alpha ausentes;

soft-mixture ausente;

pointer argmax puro activo;

canonicalization sólo desde índices decodificados.

Eso es version hygiene, no otro gate científico. Después de ese hash, la receta no cambia mirando 6901–6905.

Fresh battery 6901–6905: AUTORIZADA

La llamaría:

T2-NOBYPASS-2-FRESH-690x
	​


Los cinco manifests deben volver a sellarse antes de entrenar ninguno.

Operator swap estratificado

Ahora sí aplicamos lo predeclarado. Usaría un batch_seed=6900 independiente:

Python
Run
r = random.Random(6900)
major = bool(r.getrandbits(1))
swap_plan = [major] * 3 + [not major] * 2
r.shuffle(swap_plan)

Asignar el plan resultante, en orden, a seeds 6901..6905.

Así tendremos exactamente 3/2 o 2/3, pero cuál asignación recibe 3 y en qué seeds sigue decidido aleatoriamente antes del experimento.

Cada seed individual sigue determinando su:

permutación ARG_i → VALUE_pi(i);

token IDs;

init del modelo.

No hacer un draw de operator-swap dentro del RNG individual.

Manifest

Por dominio:

32 ARG nuevos;

OP_X, OP_Y, LINK;

IDs físicamente nuevos y disjuntos de 680x y de development;

96 train:

32 FLOOR atomic,

32 AVOID atomic,

32 NOOP;

joint_train=0;

test:

992 natural;

992 reverse correcto, intercambiando la cláusula completa.

El checker semántico independiente debe pasar:

5×1984=9920/9920

antes de cualquier training.

Training fresh

Cada seed tiene fresh init total.

Usar exactamente RCSEP-A:

L
sep,F
	​

=mean
i,j
	​

 softplus(F
cross
	​

(j)−F
self
	​

(i))
L
sep,A
	​

=mean
i,j
	​

 softplus(A
cross
	​

(j)−A
self
	​

(i))
L
sep
	​

=(L
sep,F
	​

+L
sep,A
	​

)/2.

El catálogo completo de 64 relaciones atomic se usa cada update.

No Stage B después.

Contrato fresh por seed

Después de las 5000 updates fijadas, sin early stopping ni tuning por joint:

Atomic:

FLOOR=32/32,AVOID=32/32,NOOP=32/32.

En los 992 naturales y 992 reverse exigir:

pointer
F
	​

=992/992
pointer
A
	​

=992/992
decode
F
	​

=992/992
decode
A
	​

=992/992
RAW=992/992
CANON=992/992
RAW≡CANON=992/992.

Y además, sin tolerancia:

M
F
ARG
	​

>0, M
A
ARG
	​

>0
M
F
STRUCT
	​

>0, M
A
STRUCT
	​

>0

en todos los casos.

El M_STRUCT es especialmente importante porque el mínimo development fue sólo:

0.0615008
	​


positivo, pero estrecho. Fresh dirá si ese margen realmente generaliza.

Nueva regla de ejecución

Esta vez correría los cinco seeds aunque 6901 falle.

No modificar nada entre ellos. Los cinco están precomprometidos y conviene obtener directamente la tasa de robustez completa sin convertir los restantes en un análisis secundario.

Veredicto:

PASS_STRONG s
o
ˊ
lo si 5/5 dan closure exacta
	​


es decir, 9920/9920 joint frescos entre ambos órdenes y los cinco dominios, además de los márgenes estrictos.

Si incluso uno falla:

T2-NOBYPASS-2-FRESH: VALID FAIL
	​


y no se tunea usando 690x.

Los 6801–6805 quedan permanentemente development/diagnostic; los 6901–6905 serán la primera evidencia fresh de la arquitectura realmente simplificada:

RELKEY + RCSEP + pure HARDPTR
	​


Así que sí: no más controles científicos en 680x. Freeze de receta y luego preparar/sellar 6901–6905.