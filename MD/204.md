El caso revela dos cosas distintas:

La singularidad que reportaste es real bajo la fórmula que yo di.

Pero esa fórmula tenía además un error conceptual mío: el bound de Higham usa el unit roundoff u, no FLT_EPSILON. Para FP32 round-to-nearest:

u=2
−24
,

mientras que el machine epsilon es 2
−23
=2u. 
nhigham.com
+1

Por tanto, con:

n=2
23

la cuenta correcta es:

nu=2
23
2
−24
=0.5

y:

γ
n
	​

=
1−0.5
0.5
	​

=1.

Ya no da infinito, pero sigue siendo prácticamente inútil como criterio de aceptación: permitiría un error del orden de ∑∣c
i
	​

∣.

Así que no elegiría la opción 1. No pondría un cap a n, no volvería a κ, y no declararía que una acumulación secuencial de 8.4 millones de términos es aceptable sólo porque podemos fabricar un bound que la cubra.

Decisión: opción 2

Autorizo reestructurar d_depth_embedding a una reducción jerárquica/pairwise FP32.

Ésta es una mejora de la implementación matemática, no un workaround del gate.

Para una reducción pairwise, la profundidad numérica pasa de n a aproximadamente:

h=⌈log
2
	​

n⌉.

Con:

n=2
23

tenemos:

h=23.

El bound pasa a depender de γ
23
	​

, no de γ
2
23
	​

. Higham muestra precisamente que para pairwise summation el error escala con γ
log
2
	​

n
	​

∑∣x
i
	​

∣, frente al factor proporcional a n de la suma recursiva plana. 
Nick Higham

Con FP32:

γ
23
	​

≈1.3709×10
−6

y:

2γ
23
	​

≈2.7418×10
−6
.

Eso vuelve a ser un régimen numérico razonable.

No almacenaría 8.4 millones de términos

“Pairwise” no significa crear un array gigante.

Puede ser:

reducción jerárquica siguiendo los ejes naturales;

block reduction + árbol de parciales;

streaming binary-tree/carry accumulator.

La condición estructural es que un término no atraviese millones de sumas secuenciales.

Quiero una profundidad O(logn), no:

acc += contribution

8,388,608 veces sobre el mismo acumulador FP32.

El resultado final sigue siendo FP32.

No tocaría todavía ningún threshold

Después del cambio:

rerun de los 4 goldens R0 existentes, sin regenerarlos;

mismo primary gate:

∣a−b∣≤10
−5
+10
−4
∣b∣.

No relajaría nada anticipadamente.

Mi expectativa es que depth_embedding se acerque mucho más a la reducción interna de PyTorch y posiblemente pase el primary directamente.

Si todavía no pasa

Entonces no modificamos otra vez la tolerancia.

En ese punto añadiría un oracle diagnóstico FP64 exclusivamente en el harness:

g
64
	​

=
i
∑
	​

c
i
	​


con exactamente las mismas contribuciones.

Y mediríamos por separado:

∣g
cpp
	​

−g
64
	​

∣

y:

∣g
torch
	​

−g
64
	​

∣.

Eso nos dirá si:

C++ sigue siendo numéricamente malo;

PyTorch simplemente usa otro árbol de reducción;

o ambos son aproximaciones igualmente razonables del mismo sumatorio.

El FP64 oracle sería sólo de validación, no parte del runtime.

Qué pasa con Addendum 237

Lo supersedería parcialmente.

La corrección es:

OLD:
    u = 2^-23
    gamma_n based on n

CORRECT:
    FP32 unit roundoff u = 2^-24

Pero, más importante:

For extremely deep serial reductions:
    do NOT use a huge gamma_n as permission
    to accept numerically poor accumulation.

    restructure the reduction.

El fallback basado en bound puede seguir siendo útil para las otras reducciones cuyo nu≪1 y donde el bound es informativo.

Para depth_embedding, una vez convertido a pairwise, su profundidad efectiva para análisis será aproximadamente:

reduction_depth = ceil(log2(n)) = 23

no 8,388,608.

Después del R0

Si los cuatro goldens pasan tras la reducción pairwise:

R0 BACKWARD = PASS

entonces repetiría el diagnóstico vivo que habíamos acordado:

K1 updates 0..7
K4 updates 0..7

sin tocar thresholds.

Sólo si ese conjunto también pasa avanzamos a R1 real.

Cierre de decisión
P2R0 depth_embedding serial accumulation:
    REJECTED AS NUMERICALLY POOR

No kappa fallback.
No capped-n workaround.
No tolerance relaxation.

AUTHORIZED:
    hierarchical/pairwise FP32 reduction

R0:
    rerun same 4 existing goldens

Primary tolerance:
    unchanged

If residual remains:
    FP64 contribution oracle
    before ANY further gate change

Then:
    8 live K1 + 8 live K4 shadow checks

Así que sí: arreglaría la suma, no el gate. El hecho de que el contador empírico haya dado exactamente 2
23
 hizo visible que la acumulación serial de depth_embedding es una mala propiedad del kernel nativo que conviene corregir antes de convertirlo en la base del runtime final.