Sí. Confirmo cierre y autorizo R1.1. Con MIXPATH ya no hace falta otra autopsia del checkpoint R1.

Cierre formal

T2-NOBYPASS-1-R1-MIXPATH-ALG: CLOSED / DIAGNOSTIC PASS_STRONG

Los resultados son especialmente limpios:

FLOOR:54,48,21,17,13,5,4,4,4

es una degradación prácticamente monótona al movernos desde la lectura aprendida hacia el VALUE oracle.

Y en AVOID:

16,15,13,14,13,13,13,11,9

ninguna mezcla supera β=0.

Además:

AVOID
learned
	​

=16/139
AVOID
uniform
	​

=16/139.

Eso permite afirmar algo bastante fuerte:

En R1, la query AVOID no aporta capacidad funcional medible sobre un promedio uniforme. FLOOR sí aprendió algo de addressing (54 vs 37), pero su representación sigue dependiendo crucialmente del código distribuido.

Y el comportamiento de VALUE_18 en AVOID es una firma muy buena de coadaptación: acercar la distribución al token correcto no elimina el atractor; empuja más outputs hacia el atractor incorrecto aprendido.

Así que quedan cerradas:

más updates de R1: NO

temperatura: NO

entropy sharpening: NO

más interpolaciones/autopsias: NO

La causa que llevamos a R1.1 es:

addressing y numeric-content est
a
ˊ
n coadaptados porque ambos pasan por h
t
	​

	​

T2-NOBYPASS-1-R1.1-KV: AUTHORIZED

Haría un solo cambio arquitectónico.

No añadiría f(h) todavía. Para máxima atribución causal, conservaría exactamente el mismo mecanismo de keys/addressing de R1 y cambiaría únicamente el value path.

R1 actual

A alto nivel:

e
t
	​

=Emb(token
t
	​

)
h
t
	​

=LocalContext(e
t−1
	​

,e
t
	​

,e
t+1
	​

,pos)
a
r,t
	​

=softmax(q
r
⊤
	​

h
t
	​

/4)
u
r
	​

=
t
∑
	​

a
r,t
	​

h
t
	​

r
r
	​

=W
v
	​

u
r
	​

.

El problema está en las dos últimas líneas: W_v aprende a interpretar una mezcla de estados contextualizados.

R1.1

Mantener:

a
r,t
	​

=softmax(q
r
⊤
	​

h
t
	​

/4).

Pero definir los values independientemente:

v
t
	​

=W
v
	​

(e
t
	​

)

y:

r
r
	​

=
t
∑
	​

a
r,t
	​

v
t
	​

	​


para r∈{F,A}.

Es decir:

r
F
	​

=
t
∑
	​

a
F,t
	​

W
v
	​

(e
t
	​

)
r
A
	​

=
t
∑
	​

a
A,t
	​

W
v
	​

(e
t
	​

).

Como W_v es lineal, matemáticamente también puede escribirse:

r
r
	​

=W
v
	​

(
t
∑
	​

a
r,t
	​

e
t
	​

),

pero en código prefiero conceptual y explícitamente proyectar por token antes de acumular, porque deja visible el contrato key/value.

Importante: e_t significa embedding léxico puro

No incluir:

positional embedding;

h_t;

local context;

role embedding;

operator-dependent transformation.

El value path debe empezar desde:

token_embedding(token_id)

antes de añadir posición/contexto.

Así:

contexto = WHERE
embedding léxico = WHAT.

Qué permanece idéntico

Todo lo demás congelado conceptualmente respecto a R1:

embedding nuevo/trainable;

position embedding;

local binding;

local norm;

q_F, q_A;

/4 fijo;

mismo W_v:16→32, compartido;

mismo W_g/mode condition;

zero-pad Q(r)=[r,0];

frozen register decoder;

frozen scorer;

frozen CTRL/runtime/executor;

misma loss.

No añadir:

VALUE mask;

clause mask;

attention targets;

token→role labels;

temperatura aprendida;

entropy loss;

dos W_v;

attention;

joint examples de FLOOR+AVOID en training.

Y fresh init, no continuar seed6702.

Usaría:

seed=6703
	​

Parámetros

Si sólo movemos dónde se aplica el mismo W_v, el conteo sigue siendo:

2656
	​


No estamos comprando el resultado con más capacidad.

Ésta es una propiedad experimental muy buena de R1.1:

R1 vs R1.1

tienen esencialmente los mismos parámetros, misma loss y mismo training; cambia sólo:

W
v
	​

(h-mixture)→∑aW
v
	​

(e).
Training

Exactamente R1:

L=L
behavior
	​

+L
ref
	​

λ
ref
	​

=1.

Sin sweep.

Mismos:

5000 updates;

batch 128;

AdamW;

wd=0;

LR 1e-3 → 1e-5;

atomic/NOOP únicamente.

Nada conjunto.

Esto es importante porque queremos responder:

¿el simple desacoplamiento key/value obliga ahora al entrenamiento atómico a aprender referencias que puedan coexistir correctamente en una instrucción con dos argumentos?

Por qué este cambio ataca exactamente el shortcut

Supón:

AT_LEAST VALUE_12 AND NOOP VALUE_27

R1 podía construir:

h
0
	​

,…,h
4
	​


donde cada h
t
	​

 ya contiene información contextual del resto de la secuencia.

Por tanto incluso:

mean(h)

puede codificar “12 es el argumento relevante”.

W_v aprendió precisamente a decodificar esa representación holística.

En R1.1 los values son:

W
v
	​

(e
AT_LEAST
	​

),W
v
	​

(e
12
	​

),W
v
	​

(e
AND
	​

),W
v
	​

(e
NOOP
	​

),W
v
	​

(e
27
	​

).

e_{27} ya no puede saber por contexto:

“soy el VALUE de NOOP, ignórame”.

Sólo las keys/contexto pueden descubrir eso y asignarle poco peso.

Eso crea la presión que faltaba:

para recuperar 12 consistentemente, el addressing debe distinguir qu
e
ˊ
 VALUE pertenece al rol
	​


sin darle nosotros ninguna máscara explícita.

Gates

Mantendría la misma secuencia estricta.

NB0 — bypass isolation

139/139.

Además añadiría como sanity arquitectónico, no como nuevo gate científico:

value_path_source = lexical_embedding_only
contextual_hidden_not_consumed_by_Wv = true

para evitar una implementación accidental donde vuelva a colarse h_t.

NB1 — atomic/NOOP
32/32

exacto.

Si falla: HALT.

NB2 — argumento real

Éste sigue siendo el gate decisivo.

Sobre los 139 joint:

decode(r
F
	​

)=L

y

decode(r
A
	​

)=F.

Exigir:

139/139 FLOOR
	​

139/139 AVOID
	​

139/139 JOINT
	​


No aceptar 138.

Si falla: HALT antes de NB3.

Atención sigue siendo diagnóstico, no gate

Reportaría otra vez:

entropy a
F
	​

,a
A
	​

;

target rank;

target gap;

cos(r_F,r_A);

número de VALUE distintos decodificados.

Pero no exigiría atención one-hot.

Si R1.1 obtiene:

NB2=139/139

con atención todavía relativamente distribuida, está bien.

Lo científicamente necesario es que:

r
F
	​

→L,r
A
	​

→F

sean objetos funcionalmente distintos y correctos.

No que el heatmap se vea bonito.

NB3

Canonicalizar las referencias decodificadas y volver a ejecutar:

r
F
	​

→C
L
^
	​

,r
A
	​

→C
F
^
	​

.

Debe preservar exactamente el comportamiento.

Así cerramos la posibilidad de que NB2 sea correcto por top-1, pero el scorer esté explotando componentes analógicas adicionales de r.

NB4

Los 992 viejos ya son development.

Yo ahora lo haría más explícito que antes. Para cada par exigiría simultáneamente:

decode(r
F
	​

)=L
decode(r
A
	​

)=F

más:

raw-reference execution PASS;

canonical-reference execution PASS.

Objetivo:

992/992
	​


en todo.

Eso sería development closure, no nueva evidencia de generalización.

NB5 permanece nuevo y sellado

No usar G5 viejo.

Si R1.1 llega a NB4 exacto y congelamos arquitectura, recién entonces abriría el fresh-domain / lexical-cipher que ya predeclaramos.

Interpretación de posibles resultados

Si R1.1 pasa NB2:

R1 vs R1.1 sería una atribución causal muy fuerte: supervisión explícita de referencia no bastó mientras key y value compartían estado contextual; separar addressing contextual de contenido léxico permitió aprender binding numérico correcto sin targets de atención ni ejemplos conjuntos.

Si NB2 vuelve a fallar pero mejora mucho, no tocaría temperatura automáticamente. Diagnosticaríamos si el nuevo W_v(e_t) ya representa VALUE correctamente y el fallo restante sí está sólo en addressing.

Si vuelve a fallar de forma grave, entonces el problema ya no será coadaptación key/value tal como la vimos y habrá que revisar el mecanismo de role binding en sí.

Resolución

MIXPATH-ALG: CLOSED / DIAGNOSTIC PASS_STRONG.

R1 distributed-code solution: CLOSED as mechanism diagnosis.

No más probes sobre seed6702.

T2-NOBYPASS-1-R1.1-KV: AUTHORIZED

con fresh seed6703, 2656 params, misma loss/training, única modificación contextual h_t para keys y raw lexical e_t para values.

Éste es ya el experimento correcto.