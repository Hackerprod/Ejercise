El resultado técnico sigue siendo PASS, pero encontré un bug semántico en la implementación de los Gates V y VI que debe corregirse antes de declarar la unidad documentalmente CLOSED. No requiere repetir ningún benchmark. Después de esa reparación, el siguiente paso es ER32-QUALITY-SCOPING-A, y dejo fijado abajo el contrato y la decisión sobre δ_K.

La evidencia bruta es válida: integración, equivalencia, footprint y ejecución completa no están afectados.

1. Corrección necesaria: Gate V calculó R_train al revés

El contrato predeclaró:

R
train
	​

=
T
F,K1
	​

+T
F,K4
	​

T
ER32,K1
	​

+T
ER32,K4
	​

	​


pero el código implementó:

Python
Run
"R_train": (f_k1 + f_k4) / er_total

es decir, exactamente el inverso.

Y el propio reporte demuestra:

T
ER32
	​

=43.8922436s
T
F
	​

=46.4004103s

por lo que:

R
train,correcto
	​

=
46.4004103
43.8922436
	​

=0.945945

no 1.057144.

Por tanto, tu interpretación de que ER32 salió algo más rápido en esta medición sí era correcta; lo incorrecto era el nombre/valor del ratio.

Usando tus tiempos recalculados desde el ledger, excluyendo el fsync autorreferencial:

R
train,ledger
	​

≈
1.054428
1
	​

=0.948381.

Las dos vistas cuentan historias compatibles: aproximadamente 5–5.5% menos tiempo, pero la muestra sigue siendo demasiado pequeña para declarar una mejora estable de velocidad.

El gate permanece:

0.945945 <= 1.25
→ PASS

con mucho margen.

2. Gate VI tiene el mismo problema de orientación

El contrato era un ratio de coste ER32/F, donde valores mayores que 1.25 significan regresión.

Pero el código guarda:

Python
Run
ER32_tokens_per_second / F_tokens_per_second

y luego falla si ese número supera 1.25.

Eso es semánticamente incorrecto: un ratio de throughput de 1.30 significaría que ER32 es 30% más rápido, y el código lo clasificaría como regresión.

Los datos crudos ya contienen métricas de tiempo apropiadas.

Los ratios de coste correctos son:

Modo	ER32/F tiempo
K1 autoregresivo	0.878255
K1 window	1.103431
K4 autoregresivo	0.915828
K4 window	0.922930

Todos cumplen:

R
infer
	​

≤1.25.

El único caso algo más lento es K1-window, ~10.3%; sigue muy dentro del límite.

Qué reparar

No sobrescribiría los artefactos originales. Preservarlos exactamente como evidencia histórica y añadiría un artefacto del tipo:

analysis_repair.json

que documente:

benchmark_rerun = false
raw_measurements_unchanged = true

Gate V:
    old_metric = F_time / ER32_time
    corrected_metric = ER32_time / F_time

Gate VI:
    old_metric = ER32_throughput / F_throughput
    corrected_metric = ER32_time / F_time

y recalcule ambos gates desde los resultados existentes.

Corregir también el código para que futuros usos no repitan la inversión. Añadir tests-canario:

ER32 30% faster -> MUST PASS
ER32 30% slower -> MUST FAIL at threshold 1.25

para training e inference.

El problema de total_seconds/fsync puede documentarse en la misma reparación. No obliga a repetir la ejecución.

Estado que asigno ahora
ER32-INTEGRATION-AND-COST-GATE
    MEASUREMENT_RESULT = PASS
    SCIENTIFIC/ENGINEERING_DECISION = PASS

FORMAL_ARTIFACT_CLOSE
    PENDING_ANALYSIS_REPAIR

Una vez aplicado ese fix y verificados sus tests/self-hashes:

ER32-INTEGRATION-AND-COST-GATE
    = CLOSED / PASS

No necesito otra revisión antes de iniciar el quality scoping si esa reparación se limita exactamente a esta semántica.

3. Decisión final sobre δ_K ≤ 0.10

La apruebo, pero hay que definirla con precisión.

Para seed i, profundidad K y update u:

δ
K,i
	​

(u)=NLL
ER32,K,i
	​

(u)−NLL
R1,K,i
	​

(u).

Positivo = ER32 peor.

El gate final de conservación de calidad será:

δ
K,i
	​

(5000)≤0.10
	​


por seed y por profundidad, no únicamente sobre la media.

Es decir, en el eventual cierre completo de quality scoping deben satisfacerlo individualmente:

δ_K1,seed13 <= 0.10
δ_K4,seed13 <= 0.10

δ_K1,seed14 <= 0.10
δ_K4,seed14 <= 0.10

δ_K1,seed15 <= 0.10
δ_K4,seed15 <= 0.10

Eso evita que una seed con degradación grande quede escondida detrás de otra favorable.

Una diferencia de 0.10 NLL implica aproximadamente un factor:

e
0.10
≈1.105

en perplexity. Es un límite exterior, no mi definición de “sin degradación”.

Por eso registraremos también un objetivo preferido:

δ
K
	​

≤0.05

para K1 y K4.

Pero 0.05 no será un hard fail de ER32. Sirve para distinguir conservación fuerte frente a un intercambio compresión/calidad más visible.

Esto no reutiliza el antiguo gate Δ_depth≥0.05: son métricas y significados distintos.

4. Segundo gate de calidad: preservar el beneficio de recurrencia

No basta con comprimir ambos modelos.

También mediremos:

Δ
ER32,i
	​

(u)=NLL
ER32,K1,i
	​

(u)−NLL
ER32,K4,i
	​

(u).

Queremos saber si rango 32 destruye justamente la señal de K4 que observamos en R1.

Para el eventual cierre completo a 5k, predeclaro:

RECURRENCE_PRESERVED:

Δ_ER32(5000) > 0
en al menos 2 de las 3 seeds

Y

mean(Δ_ER32(5000)) > 0

Si las tres son positivas, lo registramos como evidencia más fuerte:

RECURRENCE_PRESERVED_STRONG = 3/3

No exigiría 3/3 como hard gate: con sólo tres seeds sería demasiado sensible a una diferencia minúscula de signo.

5. No entrenamos 30k updates de golpe: Quality Scoping A primero

El siguiente experimento autorizado será:

OMEGA-CORE-LM-0-ER32-QUALITY-SCOPING-A

Sólo:

seed 20260913:
    ER32-K1 0→2000
    ER32-K4 0→2000

seed 20260914:
    ER32-K1 0→2000
    ER32-K4 0→2000

Total:

2×2×2000=
8000 nuevos updates
	​


No entrenar todavía seed15.

6. R1 no se vuelve a entrenar

Ésta es una ventaja importante.

Ya tenemos las referencias R1/F correspondientes a:

seed13 K1/K4
seed14 K1/K4
seed15 K1/K4

con curvas en:

0, 500, 1000, 1500, 2000,
2500, ..., 5000

Esos resultados quedan congelados como baseline.

Quality scoping entrena solamente ER32.

Para cada seed/update comparamos contra el R1 de:

misma seed
misma K
mismo número de updates
mismos validation docs

No contra la media de las tres.

7. Inicialización exacta de ER32 para calidad

Para cada seed:

20260913
20260914

y para K1/K4:

torch.manual_seed(seed).

Crear R1 fresh exactamente como A/B/C.

Convertir a F mediante OmegaCoreLMFast.from_reference().

Construir ER32 desde ese F.

Copiar bit-a-bit todos los parámetros no léxicos.

Inicializar C/U con:

experimental_seed = seed

No usar la constante 20260917 del benchmark técnico.

Además:

ER32-K1(seed=X).C == ER32-K4(seed=X).C
ER32-K1(seed=X).U == ER32-K4(seed=X).U

exactamente.

Así K1/K4 de una misma seed comparten interfaz léxica inicial, mientras que seeds distintas siguen siendo réplicas distintas.

8. Receta de entrenamiento: idéntica a A/B/C

Nada nuevo:

CPU
FP32 eager
physical_batch = 8
effective_batch = 8
microbatches = 1

D = 128
S = 8
rank = 32

AdamW
lr = 3e-4
betas = (0.9,0.999)
eps = 1e-8
weight_decay = 0

clip_norm = 1.0

teacher:
DistilGPT2 pinned

loss:
0.5 CE + 0.5 T² KL
T = 2

chunk_size = 512

window:
256

Usar la ruta eficiente ER32 exclusivamente.

El explicit queda como oracle técnico, no como modelo de entrenamiento.

9. Datos: exactamente el mismo experimento científico R1

A diferencia del cost gate, aquí sí se usa el recorrido científico completo.

Train:

602 documentos elegibles
mismo reconstruction/dedupe
mismo orden cíclico
mismos hashes/revisions

Validation:

los mismos 8 documentos
mismo orden
4096 targets

No modificar el conjunto porque ya conocemos sus resultados R1; necesitamos comparabilidad directa.

Test sigue prohibido.

No usar los ocho documentos técnicos del cost benchmark como corpus de entrenamiento.

10. Checkpoints/evaluación

Para Quality-A:

0
500
1000
1500
2000

Checkpoint + validation curve en cada punto.

Mismo mecanismo de recuperación:

guard <1 GiB
→ detener
→ conservar evidencia
→ verificar hash del último checkpoint válido
→ reanudar desde frontera de 500

Los updates posteriores al último checkpoint válido de un segmento interrumpido no cuentan como progreso canónico.

11. Qué reportar en cada checkpoint

Para cada seed y update:

NLL_R1_K1
NLL_ER32_K1
δ_K1

NLL_R1_K4
NLL_ER32_K4
δ_K4

Δ_R1
Δ_ER32

Y además:

mean δ_K1 entre seeds
mean δ_K4 entre seeds
mean Δ_ER32

Los deltas R1 deben leerse directamente de las curvas congeladas existentes, no duplicarse a mano en constantes sin provenance.

12. Clasificación predeclarada de QUALITY-SCOPING-A

En update 2000:

QUALITY_PROMISING-A

Si:

δ_K1 <= 0.10 en ambas seeds
δ_K4 <= 0.10 en ambas seeds

Y

Δ_ER32 > 0 en ambas seeds

Esto permite plantear continuar las mismas cuatro corridas hasta 5000.

No es QUALITY_PASS.

QUALITY_NO_GO-A

Si:

mean(δ_K1) > 0.10
AND
mean(δ_K4) > 0.10
AND
mean(Δ_ER32) <= 0

Eso sería evidencia conjunta de:

peor calidad absoluta
+
pérdida de la señal recurrente

y pondría cualquier continuación en HOLD.

QUALITY_MIXED-A

Todo lo demás.

En ese caso se inspeccionan las cuatro curvas antes de autorizar más compute.

No añadir nuevas thresholds después de ver el resultado.

13. Si posteriormente llegamos a 5k y tres seeds

Ya dejamos fijado el criterio final ahora:

ER32_QUALITY_ELIGIBLE

Todos los seis:

δ
K,i
	​

(5000)≤0.10

y:

Δ_ER32 > 0 en >= 2/3 seeds
mean Δ_ER32 > 0
ER32_QUALITY_STRONG

Además:

mean δ_K1 <= 0.05
mean δ_K4 <= 0.05
Δ_ER32 > 0 en 3/3 seeds
ER32_QUALITY_HOLD

Si falla el ceiling de 0.10 o no se preserva el criterio recurrente.

Esto sigue siendo scoping, no confirmación final.

14. Dos cambios pequeños antes de iniciar Quality-A

Autorizaría un único commit de saneamiento:

Corregir Gate V/VI de la unidad anterior y emitir análisis reparado sin repetir benchmark.

Crear el nuevo directorio de quality scoping y fijar este contrato.

No modificar:

A/B/C
R1
F
ER32 cost raw results

El run 20260917T154901Z-0fb4048b queda preservado tal cual.

Veredicto formal
ER32-INTEGRATION-AND-COST-GATE

RAW EXECUTION:
    VALID

Gates I-IV:
    PASS

Gate V:
    PASS after correcting inverted ratio
    canonical aggregate cost ratio ≈ 0.945945

Gate VI:
    PASS after using time-cost ratios
    0.878255
    1.103431
    0.915828
    0.922930

GLOBAL DECISION:
    PASS

FORMAL CLOSE:
    requires analysis-repair artifact/code fix
    NO BENCHMARK RERUN

ER32:
    TECHNICALLY_ELIGIBLE_FOR_QUALITY_SCOPING

Después:

δ_K <= 0.10
    APPROVED as per-seed/per-K final 5k ceiling

OMEGA-CORE-LM-0-ER32-QUALITY-SCOPING-A
    GO after analysis repair

Seeds:
    20260913
    20260914

Variants:
    ER32-K1
    ER32-K4

Budget:
    2000 updates each
    8000 total new updates

R1 baselines:
    reuse frozen existing curves

Test:
    PROHIBITED

Quality-B/C:
    HOLD until Quality-A result

R1 confirmatory 15×20k:
    HOLD

No hace falta revisar nada más antes de calidad salvo corregir la orientación de esos dos ratios. Las mediciones ya tomadas bastan para hacerlo; no gastaría ni un minuto en repetir el cost gate.