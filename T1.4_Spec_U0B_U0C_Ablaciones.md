# T1.4 — Spec U0-B (ablaciones tipadas) y U0-C (corrección aprendida de workspace)

Fuente: MD/Respuesta_8.md (Sol), autorización explícita tras cierre de T1-U0-A.

## Decisión de Sol

**T1-U0-A queda `CLOSED / PASS_STRONG`.** Autorizado **U0-B primero**, **U0-C inmediatamente después** (no en paralelo — U0-B debe certificar causalmente el checkpoint antes de modificarlo o añadir capacidad).

### Qué quedó demostrado en U0-A

Un único checkpoint / `SharedMemoryReader` / núcleo recurrente / `TypedCommit`; routing oráculo por operación (no por tarea); un solo `optimizer.step()` por superstep; seis tareas coexistiendo; cinco semillas válidas. La arquitectura no necesitó subir `D=64` para cerrar — el colapso inicial no era falta de capacidad general del trunk.

| Falla inicial | Causa real |
|---|---|
| Workspace contaminado | Corrección no congelada + AdamW sobre componentes tipados |
| Pointer/multi-hop degradados | PAD aleatorio, reader difuso, decoder aprendido innecesario |
| Variable-binding a azar | Bug del evaluador; el modelo siempre funcionó |
| Sequential incompleto | Registro sin canonicalización + cobertura insuficiente + olvido |
| Joint a 5k insuficiente | Currículo y presupuesto inadecuados |

### Precisión obligatoria para el ledger (corrección explícita de Sol)

Workspace debe registrarse como:
```
PASS, sin degradación sistemática frente al baseline aislado post-refactor
error H6 por seed: 0.00029–0.00144
```
NO como "exactamente cero" ni "bit-exacto". Una seed supera ligeramente el antiguo umbral 1e-3, pero el criterio de U0-A era conservar el baseline aislado dentro del checkpoint conjunto — eso sí se cumplió.

También debe constar:
```
U0-A functional coexistence: PASS_STRONG
U0-A training efficiency: todavía no evaluada
```
12000 supersteps + cobertura completa ALU + replay 5:1 demuestra coexistencia, NO que la arquitectura sea más barata de entrenar.

---

## U0-B — Ablaciones tipadas

No requiere entrenamiento. Ejecutar sobre **los 5 checkpoints congelados de U0-A** y los mismos test sets sellados.

**Antes de empezar, archivar**: hash de los 5 checkpoints; commit del código; configuración exacta; datasets/test hashes; currículo; conteo de parámetros por módulo; resultado baseline sin ablación.

### Matriz mínima

- **B1 — puntero reemplazo→residual**: `P_{r+1}=Canon(Y_r)` → `P_{r+1}=Norm(P_r+Y_r)`. Esperado: pointer-chasing profundo pierde frontera, multi-hop H3/H4 degrada, `old_pointer_mass` sube, associative/ALU/workspace casi iguales. Debe reproducir la firma ya vista en P1.
- **B2 — puntero congelado**: `P_{r+1}=P_r`. Esperado: pointer-chasing/multi-hop H>0 cerca de azar/imposible; resto sin degradación material.
- **B3 — desactivar WRITE_E**: para READ_E, `E_{r+1}=E_r` o cero. Esperado: associative-recall colapsa, ATTR de variable-binding colapsa, ASSIGN en P sigue funcionando, pointer/ALU/workspace sin cambio. Reportar separado: ASSIGN accuracy, ATTR|referencia correcta, end-to-end.
- **B4 — registro sin canonicalización**: quitar softmax+re-embedding, dejar vector continuo. Esperado: H1 puede seguir alto, teacher-forcing puede seguir alto, free-running cae rápido con H, ADD/SUB degradan más que MUL, tareas no-ALU sin cambio. Firma ya conocida — ablation fuerte.
- **B5 — head incorrecta**: permutar cíclicamente ADD→SUB→MUL→ADD. Esperado: tabla H1 y composición ALU colapsan, retrieval/workspace iguales.
- **B6 — workspace residual→reemplazo**: `W_{r+1}=W_r+Y_r` → `W_{r+1}=Y_r`. Esperado (evidencias gaussianas independientes): coseno respecto a suma completa cae hacia `1/√H` (H=4→~0.50, H=6→~0.41). Resto de tareas conservado.
- **B7 — workspace congelado**: `W_{r+1}=W_r`. Esperado: workspace cosine≈0, demás tareas intactas.
- **B8 — eliminar ruta identidad payload→workspace**: mantener corrección congelada, retirar `+Y_r`. Esperado: workspace falla completo — demuestra que la precisión no viene de una ruta oculta del trunk.
- **B9 — payload del reader a cero**: para READ_P/READ_E/ACCUM_W, `Y_r=0`. Esperado: retrieval y workspace colapsan; sequential-update permanece exacto (sus operandos vienen del instruction tape, no del reader).

### Criterio de paso de U0-B

No todas las ablaciones tienen que llevar exactamente al azar — deben producir la **firma causal prevista**. Pasa si:
1. Cada ablation degrada fuertemente SOLO las tareas que dependen de esa primitiva.
2. Tareas no relacionadas cambian como máximo ~0.5pp.
3. Las 5 semillas presentan el mismo patrón cualitativo.
4. No se reentrena después de la ablation.
5. Ninguna cabeza final obtiene acceso adicional al input para compensar.

U0-B cierra una pregunta distinta a U0-A: **el checkpoint no solo funciona; usa causalmente las reglas tipadas declaradas.**

---

## U0-C — activar la corrección aprendida de W

Después de U0-B, activar la rama congelada: `W_{r+1}=W_r+Y_r+C_θ(Y_r,Norm(W_r),I_r)`. La nueva tarea debe hacer IMPOSIBLE resolver todo con la identidad.

### Tarea recomendada: acumulación transformada

Cada ronda recupera `e_r∈R^64`. La instrucción `ACCUM_W` incluye un `transform_id`, objetivo: `W_H=Σ_{r=1}^{H} A_{τ_r} e_r`, con `A_τ` transformaciones FIJAS ortogonales que preservan norma:
```
τ=0: identidad
τ=1: negación
τ=2: permutación circular de coordenadas
τ=3: permutación por pares con cambio de signo
```
Commit único: `W_{r+1}=W_r+e_r+C_θ(e_r,Norm(W_r),τ_r)`. Corrección ideal: `C_θ=(A_{τ_r}-I)e_r` (para identidad, `C_θ=0`; para las demás, la rama debe actuar).

### Restricciones

Un solo `correction_mlp`, condicionado por `transform_id`/embedding. NO crear un MLP por transformación. NO implementar `A_τ` dentro del commit — el commit solo sabe que debe acumular. Las matrices objetivo existen SOLO en generador/evaluador. El reader sigue haciendo una lectura por ronda. `R<H` sigue impidiendo observar evidencias no leídas.

### U0-C0 — learnability aislada

Partir de un checkpoint U0-A, congelar: reader, core principal, typed commits, ALU, codebooks, decoders. Entrenar SOLO: `correction_mlp` + embedding de `transform_id`. Primero una seed.

Criterios: identidad conserva el error original del workspace; transformaciones cosine>0.999; error normalizado ≤0.01 (gate causal inicial, no rechazar causalidad de una transformación aprendida no trivial solo por no llegar a 1e-3 — ese umbral queda como meta posterior); R<H degradación predecible; con corrección=0 solo identidad funciona.

### U0-C1 — coexistencia conjunta

Cuando C0 funcione: añadir la tarea a los supersteps multitarea (6 actuales + acumulación transformada = 7 batches/superstep, un `optimizer.step()`). La corrección debe activarse solo en su opcode/config — en las 6 tareas originales, `correction grad` debe ser `None` o exactamente aislado por dispatch (mismo patrón ya validado para ALU).

Criterio: las 6 tareas U0-A conservan resultados; la nueva tarea supera el gate; ablacionar la corrección destruye SOLO transformaciones no-identidad; permutar `transform_id` degrada la salida; sin drift del workspace en `ACCUM_RAW`.

Primero seed 101; las otras 4 solo después de corrida limpia.

---

## Contabilidad arquitectónica obligatoria (antes de U0-C)

Registrar: `P_core, P_reader, P_codebooks, P_decoders, P_ALU_heads, P_typed_adapters, P_correction` y proporciones `P_typed/P_core`, `P_total_no_compartido/P_total`. El pass de U0-A no debe convertirse gradualmente en varios especialistas grandes escondidos detrás de opcodes. Heads por operación son válidas (semánticas reutilizables) SI: sin parámetros por tarea, solo por primitiva/opcode; módulos tipados pequeños frente al trunk; mismos módulos en todas las rondas y programas.

## Qué viene después de U0-B/U0-C

NO corresponde aprender el router todavía. Siguiente gate: composición mixta con routing oráculo dentro del MISMO ejemplo (ej. `READ_P→READ_E→ALU_SUB→ACCUM_W transformado→EMIT`). Train/test deben tener **programas distintos**, no solo datos distintos — primera demostración de que el checkpoint cambia de semántica dentro de una sola trayectoria.

## Ledger actualizado (Sol, verbatim)

```
T0: CLOSED / PASS
T1 primitivas aisladas: CLOSED / PASS_STRONG
T1-U0-A coexistencia en un checkpoint: CLOSED / PASS_STRONG
T1-U0-A eficiencia de entrenamiento: OPEN
T1-U0-B causalidad tipada por ablación: AUTHORIZED / NEXT
T1-U0-C corrección aprendida de workspace: AUTHORIZED AFTER U0-B
Programas mixtos con routing oráculo: PENDING
Router aprendido: NO iniciar todavía
```

> Conclusión de Sol sobre U0-A: "Un núcleo, reader y commit compartidos pueden conservar simultáneamente memoria direccionable, pointer traversal, binding, ALU composicional y workspace continuo, siempre que las representaciones discretas se canonicalicen, las escrituras exactas estén protegidas y el entrenamiento use cobertura y replay adecuados." U0-B debe demostrar que cada primitiva es causalmente necesaria; U0-C, que la parte neuronal aprendida puede extender las primitivas exactas sin contaminar el resto.

---

## Addendum 1 — Cierre de U0-B y contrato definitivo de U0-C (Sol, tras revisar el reporte de B1-B9)

**Autorización**: avanzar a U0-C, empezando por **una sola seed (101)**, resto del checkpoint congelado. NO repetir U0-B. NO lanzar todavía el joint de 7 tareas.

### 1. U0-B cerrado, pero preservando resultados negativos (no reemplazarlos)

- **B4**: el resultado válido es el corregido (0.24-0.79 según operación/seed bajo la ablación) — la expectativa original de "teacher-forcing exacto" NO se confirmó en todos los casos, y eso no invalida B4. Conclusión respaldada: "quitar la canonicalización perjudica la composición; teacher-forcing y free-running deben interpretarse con el evaluador corregido." **Dejar un test PERMANENTE**: `EMIT(X)=X` — rondas de padding EMIT no pueden cambiar el estado, corregir una predicción, ni introducir targets. Archivar la comparación baseline/ablación con el evaluador corregido.
- **B5 son DOS experimentos distintos, ambos se conservan**:

| Intervención | Resultado | Conclusión |
|---|---|---|
| Permutar heads finales (`B5-head-swap`) | NO produce colapso esperado | Las heads son intercambiables/compatibles en esos estados |
| Permutar opcode que recibe el adapter (`B5-opcode-swap`) | Degradación fuerte | El condicionamiento por operación es causalmente necesario ANTES del decoder final |

  Sol: "Mi predicción de que permutar las heads necesariamente destruiría la ALU fue incorrecta." El segundo resultado NO demuestra que toda la aritmética esté solo en el adapter ni que el trunk sea prescindible — eso requeriría intervenciones adicionales, no necesarias para avanzar ahora. NO fusionar ni tocar las heads durante U0-C.

### 2. B6 cambia el diseño de C — hallazgo central

La fórmula `cos(e_H, Σe_r)≈1/√H` asumía evidencia independiente y reemplazo dejando solo la última. Si el reader mezcla filas (`Y_r=Σ_j π_rj·e_j`, atención = combinación ponderada, no selección de una fila), los payloads de rondas distintas pueden estar correlacionados y la fórmula deja de aplicar.

**B6 demuestra que acumular payloads importa. NO demuestra que cada ronda haya leído exclusivamente la fila solicitada.**

Motivo matemático (ejemplo extremo): si `Y_1=Y_2=(e_1+e_2)/2`, entonces `Y_1+Y_2=e_1+e_2` — la SUMA pasa perfecta aunque ninguna lectura individual sea exacta. Pero pedir `e_1-e_2` ya no tiene información suficiente. **Por eso no se puede trasladar automáticamente el éxito de la acumulación simple a la acumulación transformada.** También implica: `R<H` no demuestra por sí solo inaccesibilidad de evidencias futuras — puede degradar solo porque se acumularon menos copias de una mezcla. U0-C debe distinguir **acceso informativo** de **número de actualizaciones**.

### 3. Contrato de U0-C (definitivo)

Tarea: `W_H* = Σ_{r=1}^H A_{τ_r} e_{q_r}` (q_r=evidencia solicitada, τ_r=transformación, target calculado por el generador, independiente del modelo). Transición: `Δ_r=Y_r+C_ψ(Y_r,Norm(W_r),z_{τ_r})`, `W_{r+1}=W_r+Δ_r`. Corrección ideal: `C_ψ=(A_{τ_r}-I)e_{q_r}`. 4 transformaciones: identidad, negación, desplazamiento circular, permutación con cambio de signo.

**Dos restricciones críticas**:
- El target NO se construye desde el `payload_r` del modelo (eso metería el error de lectura dentro de la "respuesta correcta"). Una métrica condicionada al payload puede ser diagnóstico, nunca sustituir al target externo.
- La transformación DEBE poder cambiar entre rondas del mismo ejemplo — si todas usan la misma matriz A, `Σ_r A·Y_r = A·Σ_r Y_r` y las compensaciones entre lecturas pueden seguir ocultas. Transformaciones distintas por ronda es parte esencial del gate, no variedad decorativa.

### 4. U0-C0 (seed101) — dos condiciones etiquetadas, en orden

**C0-oracle** (primero): reader diagnóstico entrega exactamente `Y_r=e_{q_r}` (oráculo SOLO para aislar la lectura — el modelo TODAVÍA debe aprender `A_τ`, no se sustituye la transformación). D=64, H∈{1,2,4,6}, head final=Identity(W), un `correction_mlp` compartido, tabla nueva de embeddings de transformación. Congelar: reader, core, adapters ALU, codebooks, decoders, embeddings existentes. Entrenar SOLO `correction_mlp` + embeddings de transformación nuevos. La memoria completa NO entra al corrector — solo payload de la ronda + workspace + instrucción. **El acceso a evidencias no entregadas debe estar bloqueado de verdad** (para que truncar rondas pruebe falta de información, no solo falta de acumulación).

**C0-reader** (después, cuando C0-oracle funcione): volver al reader real congelado de U0-A, sin cambiar el target. Correr 4 combinaciones:

| Lectura | Transformación | Qué aísla |
|---|---|---|
| Oráculo | Exacta (solo evaluador) | Generador/aritmética de referencia |
| Oráculo | Aprendida | Capacidad del corrector |
| Reader real | Exacta (solo evaluador) | Desviación introducida por las lecturas |
| Reader real | Aprendida | Comportamiento completo |

La tercera condición (`W̃_H=Σ_r A_τr·Y_r` vs `W_H*=Σ_r A_τr·e_qr`) NO es cota universal — es control directo de cuánto error mete el reader bajo la transformación deseada. **Si esa condición ya falla ampliamente, NO aumentar steps del corrector para reconstruir información mezclada — primero localizar qué pasa con la selección de evidencia.**

### 5. Restricción de capacidad del corrector

La corrección debe poder representar las 64 direcciones del payload (ej. negación: `C(Y,τ_neg)=-2Y`, rango 64 — un corrector lineal de rango 8-16 NO puede representarlo sobre gaussianos arbitrarios de D=64). **NO reutilizar automáticamente el adapter de bajo rango de la ALU como corrector vectorial de W.** No exige ampliar el trunk — exige comprobar que `correction_mlp` no tenga cuello de botella de salida insuficiente. Registrar dimensiones/parámetros ANTES de entrenar. Un solo módulo compartido entre transformaciones. Conservar: payload crudo en ruta identidad y en el corrector; normalización solo donde estaba declarada; última capa del corrector inicializada en cero; ninguna normalización posterior que borre la magnitud de W. Las matrices `A_τ` son SOLO del generador/evaluador — NUNCA dentro de `TypedCommit`.

**Congelación correcta**: `requires_grad_(False)` en parámetros congelados + optimizador que contenga solo los parámetros nuevos. **NO envolver todo el forward recurrente en `no_grad()` ni separar W_r del grafo entre rondas** (congelar parámetros y cortar el grafo son cosas distintas en PyTorch). Para las 6 tareas anteriores, la corrección nueva debe quedar FUERA del camino ejecutado (no solo multiplicada por cero). Mantener el test `grad=None` + inmovilidad post-step ya validado en B.

### 6. Criterios de C0 (fijados antes de correr)

Por transformación Y por profundidad (no solo promedio agregado): coseno>0.999, error relativo≤0.01. La identidad es control estructural, NO cuenta como evidencia de aprendizaje de la rama correctora. Registrar también, por ronda: `ε_Δ,r = ||Y_r+C_ψ(...) - A_τr·e_qr||` (para que un resultado final bueno no oculte errores que se cancelan entre rondas).

Matriz de ablaciones de C:

| Control | Resultado esperado |
|---|---|
| Corrección forzada a cero | Identidad conserva resultado; transformaciones no triviales degradan |
| Cambiar solo `transform_id` recibido | Predicción cambia; target original intacto |
| Mismo vector, distintas instrucciones | Modelo produce las distintas transformaciones |
| Vectores nuevos, mismo programa | Conserva precisión |
| Combinaciones nuevas de transformaciones entre rondas | Conserva composición |
| Padding EMIT adicional | No cambia ningún estado ni resultado |

No exigir que una ablación continua "caiga al azar" — la referencia es error vectorial y distancia al baseline sin corrección. **Si falla C0, el siguiente diagnóstico sale de la matriz lectura/transformación — no cambiar simultáneamente reader, ancho, pérdidas y entrenamiento.**

### 7. Vuelta al joint (U0-C1)

Cuando C0 pase: incorporar al checkpoint conjunto manteniendo la receta que cerró U0-A (NO volver al currículo uniforme, conservar cobertura+replay de ALU, documentar batches tabla/composición, normalización de pérdidas, un solo `optimizer.step()` por superstep). Primero seed101; las otras 4 solo cuando la integración conserve SIMULTÁNEAMENTE: resultados discretos de U0-A, workspace original dentro de baseline por seed, precisión de acumulación transformada, aislamiento de parámetros/escrituras, firmas causales de B y C. **En C0, con todo lo viejo congelado y rutas sin cambiar, las 6 tareas anteriores DEBEN conservar exactamente sus salidas bajo evaluación determinista — si no ocurre, es regresión de integración, no olvido por entrenamiento.**

### Resolución (Sol, verbatim)

```
T1-U0-B: CLOSED, según la campaña reportada.
B5: conservar head-swap (negativo) y opcode-swap (positivo) como dos intervenciones distintas.
U0-C0: AUTORIZADO, seed101. Primero payload exacto (C0-oracle); después reader real congelado (C0-reader).
U0-C1 joint: después de separar y superar esos dos componentes.
```

> "El siguiente objetivo no es demostrar otra suma. Es demostrar que una instrucción puede transformar causalmente la evidencia correcta antes de acumularla, sin alterar las primitivas que ya funcionan. La mezcla de filas observada en B6 hace imprescindible separar esos dos problemas: qué se leyó y qué se aprendió a hacer con ello."

---

## Addendum 2 — Diagnóstico C0-reader cerrado; SELECT vs BLEND como regla arquitectónica (Sol, MD/1.md)

Fuente: `MD/1.md`. Revisión de código/resultados publicados en commit `67975ba` (no re-ejecución de checkpoints).

### 1. Diagnóstico de C0-reader queda CERRADO en esta campaña

Tabla (peor error final publicado por celda):

| Condición | Peor error final | Resultado |
|---|---|---|
| Reader soft + transformación exacta | 0.0131574 | No pasa |
| Reader soft + corrección aprendida | 0.0154133 | No pasa |
| Reader top-1 + transformación exacta | **0.0** | Pasa |
| Reader top-1 + corrección aprendida | **0.0087313** | Pasa |

Conclusión: "En las lecturas evaluadas, el reader ya selecciona correctamente la dirección. La mezcla soft introduce una distorsión que desaparece al transportar íntegramente la fila seleccionada." No es que todo el sistema pase a error cero — el corrector conserva su error propio (~nivel C0-oracle); lo que desaparece es la contribución adicional del reader. **No hace falta ejecutar C (descomposición u/v+κ) para tomar esta decisión** — sigue siendo útil como explicación cuantitativa futura, pero no bloquea el avance. Sol confirma también: el corrector oficial evaluado fue `network_trainable` (train_residual_network=True) — su análisis afín anterior NO describe esa campaña y no debe usarse para diagnosticarla.

### 2. Regla arquitectónica: seleccionar no es mezclar

Hasta ahora una misma fórmula (`Y=Σp_j·V_j`) servía para dos operaciones semánticamente distintas. Para una lectura DIRECCIONADA ("lee el contenido de esta dirección", no "promedia direcciones parecidas"): `j*=argmax_{j∈L_r} s_j`, `Y_r=V_{j*}` (L_r=filas legales). El modelo sigue decidiendo QUÉ fila leer; la primitiva de lectura conserva exactamente QUÉ CONTIENE esa fila. El índice sale de `result.attention.argmax` — nunca del índice esperado del evaluador (esa separación ya estaba bien implementada).

**Dos políticas explícitas en un mismo reader compartido** (mismo scorer, mismos codecs, mismas máscaras):

| Política | Semántica | Payload |
|---|---|---|
| `SELECT` | Recuperar un registro individual | Fila top-1 íntegra |
| `BLEND` | Combinar deliberadamente varias evidencias | Suma ponderada |

**La política pertenece a la instrucción/contrato de lectura, NO al `task_id`.** Dos tareas que piden una fila individual usan la misma política. Cambio inmediato: aplicar `SELECT` SOLO a la lectura de evidencia individual de `ACCUM_W`. NO cambiar simultáneamente `READ_P`/`READ_E` — primero esta modificación + regresiones, homogeneizar después si corresponde.

### 3. Por qué top-1 y no afilar el softmax

Afilar podría bajar el error bajo 0.01 pero no resuelve el contrato de lectura. Contaminación del payload: `Y-V_k = Σ_{j≠k} p_j(V_j-V_k)`, cota `‖Y-V_k‖ ≤ (1-p_k)·max_{j≠k}‖V_j-V_k‖` — la misma fuga de probabilidad da errores distintos según normas/distancias de los values, y más distractores puede cambiar la masa total fuera del target. Con top-1, condicionado a elegir bien la dirección, el error de transporte es CERO sin importar el contenido de las demás filas. Conservar la temperatura como parámetro de relajación de entrenamiento/control experimental, NO como mecanismo principal de precisión de una lectura individual. Recordatorio: `attention_temperature` multiplica los logits en el código actual — cualquier calibración debe respetar esa convención.

### 4. Top-1 y entrenamiento — tres situaciones distintas

- **C0 (reader/consultas congelados)**: usar top-1 directo. El corrector sigue recibiendo gradiente propio aunque el payload venga de selección discreta. **Acá NO hace falta straight-through.**
- **Joint (aprender direcciones desde la pérdida final)**: argmax no da gradiente ordinario. Candidato para C1 — **straight-through estimator (STE)**: `p=softmax(s)`, `h=onehot(argmax s)`, `a_ST=h+(p-stopgrad(p))`, `Y=a_ST·V`. Forward usa `h` (duro); backward hacia scores usa la derivada de `p`. Código de referencia (aplicar SOLO a ejemplos con al menos una fila legal):
```python
masked_scores = scores.masked_fill(~legal_rows, -torch.inf)
selected = masked_scores.argmax(dim=-1)
soft = masked_scores.softmax(dim=-1)
hard = torch.zeros_like(soft).scatter(dim=-1, index=selected.unsqueeze(-1), value=1.0)
weights = hard + (soft - soft.detach())
payload = torch.einsum("bm,bmd->bd", weights, memory_values)
```
En inferencia usar `gather` directo sobre la fila seleccionada, sin mezcla. **NO usar `gumbel_softmax`** — introduce muestreo, no es lo mismo que el argmax determinista + STE validado acá.
- **Reader congelado pero query producida por un módulo entrenable**: congelar el reader NO elimina la necesidad de gradiente hacia quien produce la consulta — si la pérdida final debe enseñar a formar `P_r`, argmax puro corta esa vía. La decisión sobre STE depende del CAMINO DE APRENDIZAJE, no solo de si `memory_reader.parameters()` está congelado.

**Tests de STE** (no un gradcheck por diferencias finitas contra argmax — no es su contrato): forward de `SELECT_ST` coincide con selección dura; gradiente hacia scores coincide con el sustituto soft declarado; gradientes hacia values corresponden a la fila seleccionada; filas enmascaradas sin contribución; sin filas legales → payload cero y `valid=False`, nunca un argmax arbitrario. Sol validó el patrón con tensores sintéticos (igualdad exacta + gradientes prescritos) — **valida el patrón básico, NO todavía la estabilidad de entrenamiento de C1.**

### 5. Límite de cobertura de la prueba actual — no generalizar de más

El evaluador usa `MEMORY_ROW_IDS = range(10,16)` fijas en todas las muestras (solo los vectores gaussianos cambian). Válido para aislar el problema observado, pero: "Tenemos muchas evidencias nuevas y programas de transformación, evaluados sobre SEIS patrones de direccionamiento fijos" — no describir esto todavía como garantía de lectura perfecta sobre todo el codebook, memorias más grandes o consultas recurrentes predichas. Antes de promover `SELECT` al reader integrado: regresión de inferencia con claves válidas MUESTREADAS del codebook, orden físico de filas PERMUTADO, valores nuevos (mantener primero 6 filas, cambiar solo la identidad de direcciones). Si top-1 falla ahí, el fallo será visible como error de SELECCIÓN (ventaja diagnóstica), no mezclado con error continuo de payload. Tampoco usar probabilidad soft alta como certificado de dirección correcta — una lectura dura incorrecta sigue siendo incorrecta; la interfaz debe conservar índice elegido, margen y validez.

### 6. El cambio sigue en el evaluador, falta integrarlo al reader compartido

`evaluate_u0c_c0_reader.py` calcula `hard_payload` como intervención EXTERNA — `SharedMemoryReader` publicado todavía hace `payload = einsum("bm,bmd->bd", attention, values)` (BLEND puro). Top-1 está validado como intervención externa, AÚN NO es la política ejecutada por el módulo compartido. Implementación pendiente: trasladar la elección de payload al reader — mismos parámetros/query/scores/máscara/filas, nuevo `read_mode` explícito. **NO cambiar temperatura, proyecciones, codebook Y política de selección en el mismo commit.** `ReadResult` debe distinguir: `payload`, `selected_index`, `attention_soft`, `selection_margin`, `valid`, `read_mode` — para que el evaluador no vuelva a confundir "masa sobre el target" con "fila seleccionada correctamente".

### 7. Secuencia autorizada (sin reentrenamiento)

1. Añadir `SELECT` a `SharedMemoryReader` (solo para `ACCUM_W`), reproducir en seed101 las condiciones top-1 desde el módulo REAL (no parche externo).
2. Verificar las 6 tareas de U0-A + test EMIT + regresión de claves permutadas/muestreadas — sin reentrenar. Condición clave: el resultado no debe depender ya de un parche exclusivo del evaluador.
3. Recién después: C1 joint, con forward duro + STE como candidato de entrenamiento (no declarar resuelto el joint solo porque el forward congelado ya pasa).

Nota de hardware: seleccionar un value evita mezclar todos los values en inferencia, pero el scorer sigue comparando la consulta con TODAS las keys — top-1 no vuelve el lookup de costo constante ni demuestra aceleración CPU por sí solo (medición para el runtime posterior).

### Ledger (Sol, verbatim)

```
C0-reader soft: FAIL de precisión, resultado archivado.
Diagnóstico del fallo: CLOSED para la campaña evaluada. Selección correcta; distorsión por mezcla soft.
C0-reader top-1: PASS en seed101, bajo las condiciones evaluadas.
Corrector oficial: network_trainable. No aplicar la simplificación afín anterior.
Integración SELECT en SharedMemoryReader: siguiente paso, sin reentrenamiento.
C1 joint: después de la regresión integrada; forward duro y STE como candidato de entrenamiento.
```

> "La decisión de fondo es esta: aprender dónde leer y copiar exactamente lo leído son responsabilidades distintas. Para una memoria direccionable, elegiría top-1 como semántica de lectura; reservaría la mezcla soft para operaciones que realmente pidan combinar evidencias, y usaría una relajación únicamente donde haga falta aprender la selección."

---

## Addendum 3 — read_mode por instrucción, no universal; BLEND se conserva (Sol, MD/2.md)

Fuente: `MD/2.md`, revisión de código/resultados en commit `03e3a09` (no re-ejecución).

### Decisión

**Conservar `workspace_accumulation` en BLEND** como baseline de compatibilidad; usar SELECT explícito solo para la evidencia individual de U0-C. NO reentrenar la tarea antigua desde cero, NO restringir su memoria a una fila legal para "hacerla pasar". La recomendación anterior de Sol (cambiar TODAS las lecturas ACCUM_W a SELECT) fue "demasiado amplia" — se retracta parcialmente.

**Corrección epistémica importante — lo que el checkpoint aprendió NO redefine el contrato original**: la tarea antigua aprendió a resolver una SUMA mediante lecturas distribuidas; NO demostró que recuperara la evidencia solicitada en cada ronda. Contraejemplo exacto: un reader que devuelve `Y_r=(1/H)Σe_j` en TODAS las rondas (uniforme, sin distinguir ronda) acumula correctamente a `W_H=Σe_j` tras H rondas Y degrada al truncar R<H — sin que ninguna lectura individual haya sido "la evidencia de esa ronda". Conclusión: "suma correcta + degradación al truncar rondas" NO basta para probar "evidencia nueva e inaccesible antes de cada ronda". **U0-A y U0-B validaron acumulación funcional y necesidad de la transición residual — NO validaron lectura secuencial exacta de las filas de workspace.** Esto no invalida T1-W original (con evidencias entregadas una por una) ni el resto de las primitivas.

### Por qué la regresión no contradice el éxito de SELECT

Las consultas son DIFERENTES entre caminos: el workspace antiguo (`parse_workspace()`) arranca con W=0, usa el WORKSPACE como fuente de consulta, comunica el índice vía argumento inmediato, y expone TODAS las filas de evidencia como legales desde el inicio (sin máscara temporal). El C0-reader aprobado construye la consulta desde una CLAVE EXPLÍCITA en P. No son equivalentes — no estamos viendo que SELECT deje de funcionar sobre las mismas consultas, sino que una consulta aprendida bajo OTRO objetivo no satisface el contrato de selección individual.

Evidencia clave: en H2, SELECT da error final=0 con top1_accuracy=0 — la suma final no identifica la trayectoria de lectura (leer e2,e1 en vez de e1,e2 también suma perfecto; con BLEND hay aún más soluciones, cualquier `Σ_r π_rj=1` por evidencia basta, sin exigir `π_rj=1[j=r]`).

### Terminología fijada

```
workspace_accumulation antiguo: agregación recurrente mediante lecturas BLEND. (solución válida para SU objetivo)
U0-C: acumulación transformada de evidencias direccionadas mediante SELECT. (exige conservar dirección+evidencia+transformación)
```

BLEND NO es "sumar todas las filas" — es mezcla ponderada normalizada (Σπ=1). No declarar BLEND obligatorio/universal para cualquier workspace; se conserva en ESTA variante y como política general para operaciones que admitan mezcla real.

### Contrato de arquitectura: instrucción separa lectura/transformación/escritura

`I_r = (write_opcode, read_mode, query_source, argumentos, transformación)`. `ACCUM_W` sigue siendo UN opcode de escritura — **especifica la escritura, no cómo se recuperó el payload**. `read_mode ∈ {BLEND, SELECT}` es un campo de la instrucción, no un opcode nuevo ni un módulo entrenable nuevo:

```
ACCUM_BLEND  → opcode=ACCUM_W, read_mode=BLEND   (alias legible, workspace histórico)
ACCUM_SELECT → opcode=ACCUM_W, read_mode=SELECT  (alias legible, U0-C)
```

Conserva IDs/embeddings actuales y compatibilidad con checkpoints — no aparece red específica de U0-C. Regla: "la política pertenece a la instrucción. El modelo NO recibe un `task_id` para decidirla." PROHIBIDO dentro del modelo: `if task_name == "workspace_accumulation": ...` para elegir implementación oculta. `TypedCommit` ya suma workspace+payload+corrección candidata — no hace falta duplicarlo para las dos políticas.

### Bug real encontrado en 03e3a09: read_mode es global-por-lote, debe ser por-instrucción

`run_rounds()` recibe un único `read_mode: str` y decide con `torch.all(...)` sobre los opcodes del lote — si el conjunto no es 100% compatible con SELECT, degrada TODO el lote/ronda a BLEND. **Insuficiente para programas mixtos.** Fix requerido: datos canónicos deben incluir `opcodes[B,R]`, `read_modes[B,R]`, `source_slots[B,R]`, `immediates[B,R]`, `transform_ids[B,R]` cuando corresponda; cada ronda pasa `read_mode = batch["read_modes"][:, round_index]`; el reader calcula scores con los MISMOS parámetros y materializa SELECT o BLEND POR MUESTRA dentro del mismo forward — nunca degradar el batch entero por una muestra distinta. Todavía NO hace falta embedding aprendido de read_mode — primero debe ser un campo de control explícito del runtime.

**Dirección de U0-C**: NO reutilizar la ruta antigua (`Norm(W)+condicionamiento por índice`) asumiendo que ahora equivale a la clave solicitada — mantener la ruta de dirección explícita que YA pasó la auditoría. El programa oráculo puede proveer esa dirección en este gate; aprender a producirla desde estado previo es OTRO experimento futuro.

### No usar una sola fila legal para "arreglar" el reader viejo

Con una sola fila legal, `argmax` siempre acierta trivialmente sin importar qué aprendió el scorer — válido solo como **control streaming** (una evidencia disponible por ronda, prueba conservación/acumulación), pero NO prueba recuperación direccionada entre candidatos. Tres categorías de test distintas, no mezclar:
- **Control streaming**: 1 fila legal por ronda.
- **Lectura direccionada**: varias filas legales + clave solicitada — prueba selección.
- **Agregación BLEND**: varias filas contribuyen — prueba combinación/acumulación.

Para U0-C: conservar varias filas legales + target externo `W_H*=Σ_r A_τr·e_qr`. El evaluador debe comprobar la respuesta final Y `j_r*=q_r, Y_r=e_qr` por ronda. La máscara puede excluir padding/tipos incompatibles/info realmente inaccesible — **NO marcar la respuesta correcta como única candidata solo para sacar 100%.**

### Corrección del ledger (tabla, Sol verbatim)

| Resultado | Estado |
|---|---|
| T1-W original con entrega secuencial | Conserva su validación |
| Workspace de U0-A bajo BLEND | Conserva su pass de resultado final |
| "Workspace U0-A lee exactamente una fila solicitada por ronda" | **No demostrado; contradicho por esta auditoría** |
| SELECT sobre claves muestreadas y filas permutadas | Pasa en la distribución evaluada (memoria de 6 filas — generalización de claves/orden, NO de tamaño de memoria) |
| Sustitución universal BLEND→SELECT para todo ACCUM_W | **Rechazada** |
| U0-C con dirección explícita y SELECT | Ruta autorizada para la integración |

Precisión menor: sequential-update NO es una "quinta tarea BLEND" — sus instrucciones ALU no requieren lectura de memoria; su resultado idéntico confirma ausencia de regresión, no una propiedad adicional de BLEND.

### Próxima ejecución autorizada (sin entrenamiento adicional)

Tres verificaciones:
1. **Compatibilidad**: las 6 tareas originales ejecutan sus instrucciones históricas sin cambio; workspace conserva BLEND; la corrección nueva NO entra en su ruta RAW.
2. **U0-C integrado**: evidencia solicitada leída vía SELECT desde dirección explícita, corrector oficial aplicado — debe reproducir el nivel de C0-oracle SIN parche externo en el evaluador.
3. **Aislamiento por instrucción**: correr ejemplos BLEND y SELECT JUNTOS y por separado debe dar los MISMOS resultados por ejemplo — detecta el bug del modo global de batch antes de entrenar programas mixtos.

NO tocar en este mismo paso: temperatura, proyecciones, codebooks, pérdida, currículo. NO usar la política nueva como excusa para dar acceso adicional a memoria desde el core. Recién después de que los 3 controles pasen: C1 con la receta de entrenamiento ya validada + el modo explícito nuevo (la estabilidad del reader bajo entrenamiento conjunto sigue siendo pregunta distinta de su corrección en inferencia congelada).

> "No forzaría al modelo antiguo a comportarse como un selector que nunca fue obligado a aprender. Tampoco llamaría selección individual a una solución distribuida porque produce la suma correcta. Conservamos la agregación que funciona, hacemos explícito su contrato y usamos SELECT donde la correspondencia exacta entre dirección, evidencia y transformación es necesaria."
