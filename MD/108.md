Sí. La preparación está visible en el repo (d90459d67aad...) y coincide con el diseño precomprometido: 5 manifests fresh, 5 asignaciones distintas de los 3 roles, D(L,F) sellado, 29760/29760 filas validadas y cero training previo.

T3-NOBYPASS-1-NOOP-DISTRACTOR: TRAINING AUTHORIZED
	​


Ejecutá los cinco seeds 7001–7005 completos, aunque alguno falle. No modificar nada entre seeds.

Receta congelada

Fresh init total por seed. Prohibido cargar cualquier checkpoint/core de 680x, 690x o T2.

Mantener exactamente T2:

k
t
	​

=LN(SiLU(W
k
	​

[e
t−1
	​

,e
t
	​

]))
s
r,t
	​

=q
r
⊤
	​

k
t
	​

/4
j
r
	​

=arg
t<length
max
	​

s
r,t
	​

r
r
	​

=W
v
	​

(e
j
r
	​

	​

)

y:

L=L
behavior
	​

+L
ref
	​

+L
sep
	​

.

L_sep sigue exclusivamente FLOOR↔AVOID:

L
sep,F
	​

=mean
i,j
	​

 softplus(F
crossA
	​

(j)−F
self
	​

(i))
L
sep,A
	​

=mean
i,j
	​

 softplus(A
crossF
	​

(j)−A
self
	​

(i)).

NOOP no entra al contrastivo.

Mantener además:

5000 updates;

AdamW;

wd=0;

LR 1e-3 → 1e-5;

coeficiente L_sep=1.0;

mismo batching;

96 ejemplos atomic únicamente;

multi_clause_train=0;

sin Stage B;

sin gate;

sin teacher;

sin threshold;

sin soft mixture;

forward sólo con token_ids,lengths.

El test manifest no puede participar en training, selección de checkpoint, early stopping ni tuning.

Gates por seed

Primero atomic:

FLOOR=32/32,AVOID=32/32,NOOP=32/32.

Después calculá el certificado exhaustivo local, independientemente del D(L,F) usado en end-to-end.

Para los 32 argumentos:

F
N
	​

(k)=s
F
	​

(OP
NOOP
	​

,ARG
k
	​

)
A
N
	​

(k)=s
A
	​

(OP
NOOP
	​

,ARG
k
	​

).

Exigir:

G
F,N
	​

=
i
min
	​

F
self
	​

(i)−
k
max
	​

F
N
	​

(k)>0
	​

G
A,N
	​

=
i
min
	​

A
self
	​

(i)−
k
max
	​

A
N
	​

(k)>0
	​


Además conservar:

G
F,A
	​

>0,G
A,F
	​

>0.

Esto es esencial: incluso si los 29760 casos sellados pasan, T3 no obtiene PASS_STRONG si algún NOOP posible fuera capaz de robar un pointer y simplemente no apareció como D(L,F) en esa fila concreta.

End-to-end

Evaluar los:

5×992×6=29760

casos ya sellados.

En cada seed y cada uno de los seis órdenes exigir:

pointer
F
	​

=100%
pointer
A
	​

=100%
decode
F
	​

=100%
decode
A
	​

=100%
RAW=100%
CANON=100%
RAW≡CANON=100%.

Y separar los márgenes de cada role contra tres clases de competidor:

M
OTHER_ARG
>0
M
NOOP
>0
M
STRUCT
>0.

Quiero el mínimo global de cada categoría, con:

seed;

orden de cláusulas;

(L,F,D);

role;

target;

competidor ganador;

score target;

score competitor.

Sanity de orden

Como RELKEY es local, espero que las seis permutaciones de cláusulas produzcan las mismas decisiones para un mismo (L,F,D).

Reportá:

six_order_outcome_equality=true/false

y mismatch_count.

Pero no lo conviertas en sustituto de ejecutar realmente las seis órdenes.

Regla de decisión

Si los cinco seeds cumplen simultáneamente:

atomic 32/32/32;

G
F,N
	​

>0;

G
A,N
	​

>0;

G
F,A
	​

>0;

G
A,F
	​

>0;

29760/29760 en las siete métricas;

todos los márgenes OTHER_ARG, NOOP, STRUCT estrictamente positivos;

entonces:

T3-NOBYPASS-1-NOOP-DISTRACTOR: PASS_STRONG
	​


Si falla una sola de esas condiciones:

VALID FAIL
	​


No ajustar nada usando 7001–7005. Los cinco se ejecutan igualmente para obtener la distribución completa del fallo.

Y especialmente: si falla por M_NOOP≤0, no agregues NOOP a RCSEP dentro de esta corrida. Ese sería precisamente el resultado científico de T3 y abriría una futura intervención separada.

Autorización explícita: iniciar ahora training fresh 7001–7005 con la receta T2 congelada, RCSEP sólo FLOOR↔AVOID y pure HARDPTR.